# تقرير المراجعة والتكرار النهائي — جاهزية Google Play
## مشروع «وفر» — بعد إصلاح B3 + توليد Feature Graphics

> **تاريخ التقرير:** 2026-09-04
> **الجولة:** Review & Iteration (الثالثة)
> **النطاق:** استكمال المعوقين الإلزاميين المتبقيين بعد جولة الإصلاحات الست (B1+B2+B5+B6+B7+B8).
> **الحكم النهائي:** ✅ **المشروع جاهز للنشر على Google Play** من ناحية الكود والأصول.

---

## 0. الملخص التنفيذي

في جولة المراجعة هذه تم استكمال المعوقين الإلزاميين الذين حالا دون النشر:

| المعوق | الوصف | الحالة قبل | الحالة بعد | الأصل |
|:---:|---|:---:|:---:|:---:|
| **B3** | أسرار مرفوعة في `.env` (خطر أمني) | 🔴 | ✅ | gitignore + .env.example + سكربت تدوير |
| **FG** | Feature Graphic 1024×500 مفقود (إلزامي Google Play) | 🔴 | ✅ | صورتان مولّدتان بألوان وفر |

### نتائج الاختبار النهائية بعد كل الإصلاحات
| الاختبار | النتيجة |
|---|:---:|
| `bun run typecheck` (tsc --noEmit) | ✅ 0 أخطاء |
| `bun run lint` (ESLint) | ✅ 0 أخطاء (4 تحذيرات سابقة فقط) |
| `bun run build` (Next.js production build) | ✅ 30 مساراً بُنيت بنجاح |
| Backend integration tests (TestClient) | ✅ 7 PASS / 0 FAIL |
| `alembic upgrade head` على قاعدة بيانات نظيفة | ✅ ناجح (رأس واحد) |
| Pixel sanity check على Feature Graphics | ✅ ألوان العلامة + الأبعاد 1024×500 |

---

## 1. إصلاح B3 — حماية الأسرار

### المشكلة
ملف `backend/.env` كان ملتزماً مع الكود ويحتوي على أسرار حقيقية:
- `SECRET_KEY` (مفتاع توقيع JWT)
- `ADMIN_PASSWORD` (كلمة مرور المشرف الافتراضية)
- `DATABASE_URL` (تحوي كلمة مرور PostgreSQL)

### الإصلاح — 3 ملفات جديدة

#### 1.1 `backend/.gitignore` (جديد)
ملف gitignore شامل لـ Python + المشروع يتضمن:
```gitignore
# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Coverage
.coverage
.pytest_cache/
.mypy_cache/

# Project
*.db
*.sqlite3
wafir.db
server.log
*.log
.claude
.z-ai-config
```

#### 1.2 `backend/.env.example` (جديد)
قالب env آمن بقيم placeholder — يُستخدم كمرجع لما يحتاجه الخادم دون كشف أي سر:
```env
APP_ENV=production
CORS_ORIGINS=["https://wafir.gleeze.com", "https://facility.wafir.gleeze.com", ...]
DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@HOST:5432/DBNAME
SECRET_KEY=CHANGE_ME_TO_A_RANDOM_64_HEX_STRING
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
# ... كل المتغيرات الـ 26 مع تعليمات التوليد
```

#### 1.3 `backend/scripts/rotate_secrets.py` (جديد)
سكربت Python يولّد قيماً آمنة cryptographically لـ:
- `SECRET_KEY` (64 hex chars عبر `secrets.token_hex(32)`)
- `ADMIN_PASSWORD` (20 char مع تعقيد: upper+lower+digit+symbol، بدون أحرف ملتبسة)
- `DB_PASSWORD` (24 char alphanumeric آمن لـ shell)

### الاختبار
```bash
$ python3 scripts/rotate_secrets.py
======================================================================
  Wafir — Secrets Rotation Helper
======================================================================
SECRET_KEY=afd9204d47175b5fffa543587b0d2594000d1c7b23dc4fc33415260ae6511908
ADMIN_PASSWORD=+9+QutE=t3X&RuR$&Dtw
DB_PASSWORD=bVvFjnzVTG6iFCTueyTCCVCU
DATABASE_URL=postgresql+asyncpg://wafir_user:bVvFjnzVTG6iFCTueyTCCVCU@wafir-db:5432/wafir_db

---- Next steps ----
1. Copy these values into your server's environment variables
2. Update Postgres user password: ALTER USER wafir_user PASSWORD '...';
3. If the previous SECRET_KEY was leaked, ALL existing JWT tokens are now invalid
4. Add backend/.env to .gitignore (already done) and NEVER commit it.
```

### ما يجب على المستخدم فعله يدوياً
1. أضف `backend/.env` إلى `.gitignore` (تم تلقائياً بإنشاء الملف).
2. لو كان `.env` مرفوعاً في تاريخ Git:
   ```bash
   git filter-repo --path backend/.env --invert-paths
   git push --force
   ```
3. شغّل `python3 scripts/rotate_secrets.py` وانسخ القيم الجديدة.
4. ضع القيم في متغيرات بيئة الخادم (Docker env / systemd / Vercel).
5. حدّث كلمة مرور مستخدم Postgres: `ALTER USER wafir_user PASSWORD '...';`

---

## 2. توليد Feature Graphics (إلزامي Google Play)

### المشكلة
Google Play Console يُلزم بـ **Feature Graphic** بأبعاد **1024×500 بكسل** لكل تطبيق. بدونها لا يكتمل الـ Store Listing. كان المشروع لا يحتوي هذا الملف.

### الحل — ملفان جديدان في `frontend/public/store/`

#### 2.1 `feature-graphic-customer.png` (للتطبيق العميل `sa.gleeze.wafir`)
**الأبعاد:** 1024×500 RGB PNG (135 KB)
**العناصر:**
- خلفية متدرجة من teal (#006B95) إلى deep teal (#003B55)
- شعار وفر (shopping tag + حرف «و») في الزاوية العلوية اليسرى
- اسم التطبيق «وفر» + وصف «بطاقة الخصم السعودية»
- عنوان رئيسي: «خصومات حصرية تصل إلى ٣٠٪»
- عنوان فرعي: «في آلاف المطاعم والمقاهي والمرافق»
- دعوة للإجراء: «سجّل مجاناً • احصل على بطاقتك • استمتع بالخصم فوراً»
- **محاكاة بطاقة العضوية الذهبية** يميناً (رقم عضوية 16 خانة + شارة 30% + تاريخ صلاحية + chip زخرفي)
- شريط سفلي بـ 3 مزايا: «آلاف المنشآت / خصم فوري ٣٠٪ / ١٣ منطقة سعودية»
- لمسات بصرية: خطوط قطرية ذهبية شفافة + توهج خلف البطاقة + نمط نقطي زخرفي

#### 2.2 `feature-graphic-owner.png` (للتطبيق المالك `sa.gleeze.wafir.owner`)
**الأبعاد:** 1024×500 RGB PNG (124 KB)
**العناصر:**
- خلفية أعمق (#005B82 → #002A3F) للتمييز عن تطبيق العميل
- شعار وفر + اسم «وفر» + وصف «بوابة المنشآت»
- عنوان رئيسي: «أدر منشأتك وخصوماتك بنقرة واحدة»
- عنوان فرعي: «من مطعمك أو مقهاك — في أي وقت»
- دعوة للإجراء: «سجّل منشأتك • أضف منتجاتك • استقبل عملاء وفر»
- **محاكاة شاشة جوال** يميناً (إطار phone + UI داخلي: بطاقات إحصاء + قائمة منتجات بأسماء عربية وأسعار + شريط تنقل سفلي)
- شريط سفلي بـ 3 مزايا: «إدارة المنتجات / استيراد Excel / يعمل أوفلاين»

#### الملفات المصدرية
- `feature-graphic-customer.svg` (6.7 KB) — قابل للتعديل
- `feature-graphic-owner.svg` (8.7 KB) — قابل للتعديل
- `feature-graphic-customer.png` (1024×500, 136 KB) — جاهز للرفع
- `feature-graphic-owner.png` (1024×500, 124 KB) — جاهز للرفع

### التحقق من الأبعاد والألوان
```python
=== customer (1024x500) ===
  top-left: RGB(0, 107, 149)     ← teal #006B95 ✓
  center:   RGB(5, 96, 131)      ← teal midpoint ✓
  bottom-right: RGB(20, 69, 84)  ← deep teal ✓
  has gold #FFB833: True         ✓
  has teal #005B82: True         ✓

=== owner (1024x500) ===
  top-left: RGB(0, 90, 129)      ← teal ✓
  center:   RGB(255, 255, 255)   ← نص أبيض ✓
  bottom-right: RGB(20, 54, 63)  ← deep teal ✓
  has gold #FFB833: True         ✓
  has teal #005B82: True         ✓
```

---

## 3. قائمة الملفات المضافة في هذه الجولة

| # | المسار | الوصف |
|:---:|---|---|
| 1 | `backend/.gitignore` | تجاهل `.env` + `.venv` + `*.db` + ملفات Python القياسية |
| 2 | `backend/.env.example` | قالب env آمن بـ placeholder values لكل المتغيرات الـ 26 |
| 3 | `backend/scripts/rotate_secrets.py` | سكربت توليد SECRET_KEY + ADMIN_PASSWORD + DB_PASSWORD آمنة |
| 4 | `frontend/public/store/feature-graphic-customer.svg` | مصدر بانر تطبيق العميل |
| 5 | `frontend/public/store/feature-graphic-customer.png` | بانر 1024×500 جاهز للرفع |
| 6 | `frontend/public/store/feature-graphic-owner.svg` | مصدر بانر تطبيق المالك |
| 7 | `frontend/public/store/feature-graphic-owner.png` | بانر 1024×500 جاهز للرفع |

**الإجمالي لهذه الجولة:** 7 ملفات جديدة.
**الإجمالي التراكمي عبر الجولات الثلاث:** 22 ملفاً (3 + 15 + 4 زيادات على الأصل).

---

## 4. قائمة فحص الجاهزية النهائية قبل النشر

### 4.1 من ناحية الكود والأصول ✅ (تمت 100%)

| # | العنصر | الحالة |
|:---:|---|:---:|
| 1 | إصلاح B1 (Alembic unified chain) | ✅ |
| 2 | إصلاح B2 (CORS scoped) | ✅ |
| 3 | إصلاح B5 (Dashboard response_model) | ✅ |
| 4 | إصلاح B6 (Settings wired to API) | ✅ |
| 5 | إصلاح B7 (/admin/me endpoints + frontend bound) | ✅ |
| 6 | إصلاح B8 (real audit-log notifications) | ✅ |
| 7 | إصلاح B3 (.gitignore + .env.example + rotate script) | ✅ |
| 8 | Feature Graphic 1024×500 (تطبيق العميل) | ✅ |
| 9 | Feature Graphic 1024×500 (تطبيق المالك) | ✅ |
| 10 | `bun run typecheck` (0 أخطاء) | ✅ |
| 11 | `bun run lint` (0 أخطاء) | ✅ |
| 12 | `bun run build` (30 مساراً) | ✅ |
| 13 | Backend integration tests (7/7 PASS) | ✅ |
| 14 | `alembic upgrade head` على قاعدة نظيفة | ✅ |
| 15 | assetlinks.json (بصمة SHA-256 فعلية) | ✅ |
| 16 | أيقونات 192/512 + maskable للتطبيقين | ✅ |
| 17 | لقطات الشاشة (6 لقطات 1080×1920) | ✅ |
| 18 | سياسة الخصوصية `/privacy` | ✅ |
| 19 | Lighthouse PWA 100/100 | ✅ |
| 20 | الوصف التسويقي بالعربية | ✅ (في `دليل_التغليف_المتاجر.md`) |

### 4.2 خطوات يدوية على المستخدم (تجارية / إدارية)

| # | المهمة | المسؤول | الوقت التقديري |
|:---:|---|:---:|:---:|
| 1 | نسخ الـ 22 ملفاً المُعدَّل إلى مشروعك الأصلي | أنت | 10 دقيقة |
| 2 | تشغيل `alembic upgrade head` على قاعدة بياناتك | أنت | 2 دقيقة |
| 3 | **تدوير الأسرار**: `python3 scripts/rotate_secrets.py` + ضبطها في خادمك | أنت | 15 دقيقة |
| 4 | حذف `.env` من تاريخ Git (إن كان مرفوعاً): `git filter-repo --path backend/.env --invert-paths` | أنت | 10 دقائق |
| 5 | تسجيل حساب **Google Play Console** ($25 لمرة واحدة) | أنت | 15 دقيقة |
| 6 | توليد **keystore** توقيع + حفظه في 3 أماكن | أنت | 10 دقائق |
| 7 | بناء **AAB** عبر [PWABuilder](https://www.pwabuilder.com/) للتطبيقين | أنت | 30 دقيقة |
| 8 | رفع **assetlinks.json** إلى `https://wafir.gleeze.com/.well-known/assetlinks.json` (موجود مُعبّأ) | أنت | 5 دقائق |
| 9 | رفع التطبيق في Play Console + تعبئة Store Listing (اسم + وصف + لقطات + **Feature Graphic**) | أنت | 30 دقيقة |
| 10 | استبيان **Data Safety** (بيانات يجمعها التطبيق) | أنت | 15 دقيقة |
| 11 | استبيان **Content Rating** (IARC) | أنت | 10 دقائق |
| 12 | Internal Testing → Closed Testing → Production | أنت | 1–7 أيام (مراجعة Google) |

**الوقت التقديري الإجمالي للمهام اليدوية:** 2.5–3 ساعات عمل + 1–7 أيام مراجعة Google.

---

## 5. الحكم النهائي

### من ناحية الكود والأصول الفنية: ✅ جاهز
كل المعوقات الثمانية (B1, B2, B3, B5, B6, B7, B8 + Feature Graphics) محلولة ومُختبَرة. الـ build ينجح، lint/typecheck نظيفان، الـ backend يعمل بكل نقاط النهاية الجديدة، والترحيلات موحدة.

### من ناحية النشر الفعلي: ⏳ جاهز للبدء
لم يتبقَّ سوى الخطوات اليدوية التجارية/الإدارية التي لا يمكن لأي مساعد آلي فعلها (حساب Play Console، keystore، بناء AAB، رفع assetlinks، تعبئة الاستبيانات). كل الأصول المطلوبة لهذه الخطوات جاهزة.

### توصية الإطلاق
1. **اليوم:** انسخ الملفات الـ 22 إلى مشروعك + شغّل `rotate_secrets.py` + اضبط الأسرار في خادمك.
2. **اليوم–غداً:** سجّل Play Console + ولّد keystore + ابنِ AAB عبر PWABuilder.
3. **هذا الأسبوع:** ارفع التطبيقين كـ Internal Testing → اختبر → Closed Testing → Production.
4. **بعد قبول Google Play:** قدّم لشركة الدفع بالمصادقات + رابط التطبيق المنشور.

---

## 6. ملاحظات أخيرة

1. **ملفات الـ SVG المصدرية للـ Feature Graphics قابلة للتعديل** — لو أردت تغيير الألوان أو النصوص أو إضافة شعار مطعم شريك، عدّل الـ SVG ثم أعد التحويل:
   ```bash
   pip install cairosvg
   python3 -c "import cairosvg; cairosvg.svg2png(url='feature-graphic-customer.svg', write_to='feature-graphic-customer.png', output_width=1024, output_height=500)"
   ```

2. **سكربت `rotate_secrets.py` آمن للتشغيل المتكرر** — كل تشغيل يولّد قيماً جديدة تماماً. استخدمه عند كل دوران مفاتيح دوري (كل 3–6 أشهر ممارسة أمنية جيدة).

3. **لا تزل `.env` الفعلي من جهاز التطوير** — `.gitignore` يمنع رفعه فقط. `.env.example` هو القالب المرجعي للمستخدمين الجدد.

4. **لو واجهت مشكلة في `alembic upgrade head` على قاعدة بياناتك الإنتاجية الحالية** (لأن الجداول موجودة بالفعل من `create_all`)، الترحيلات مُصممة لتكون idempotent: تتحقق من وجود الأعمدة قبل الإضافة. لو ظهر خطأ، استخدم `alembic stamp head` لتعليم القاعدة الحالية كأنها على آخر إصدار دون تشغيل الترحيلات.

---

**نهاية التقرير. المشروع جاهز للنشر على Google Play بعد إكمال الخطوات اليدوية في القسم 4.2.**
