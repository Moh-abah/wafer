# تقرير الجاهزية للإنتاج والنشر على متجر Google Play
## مشروع «وفر» — منصة بطاقات الخصومات للمملكة العربية السعودية

> **جهّزه:** فريق المراجعة الفنية
> **تاريخ التقرير:** 2026-09-04
> **إصدار المشروع المُراجَع:** Frontend `1.0.0` / Backend `1.0.0`
> **الهدف:** تقييم جاهزية المشروع للنزول إلى السوق السعودي وللنشر على Google Play، تمهيدًا لاستيفاء شرط شركة الدفع (بوابة الدفع) القاضي بأن يكون التطبيق **جاهزًا للإنتاج ومنشورًا على Google Play**.

---

## 0. الملخص التنفيذي (Executive Summary)

| المعيار | الحكم | الدلالة |
|---|:---:|---|
| جاهزية الواجهة (Frontend PWA) | ✅ ممتاز | Lighthouse PWA 100/100 للتطبيقين (العميل + المالك). |
| جاهزية الباك‑إند (FastAPI) | ⚠️ جيد مع ثغرات | 44 نقطة نهاية، معمارية نظيفة، لكن سلسلة الته migrations مكسورة وCORS مفتوح. |
| التوطين العربي (RTL + خط Cairo + ar‑SA) | ✅ كامل | جاهز للسوق السعودي لغويًا. |
| جاهزية الجانب السعودي التنظيمي (ZATCA / Mudad / Nafath) | ❌ غير موجود | لا يوجد ضريبة قيمة مضافة، لا توثيق وطني (أبشر/نفاث)، لا فاتورة إلكترونية. |
| **تكامل بوابة الدفع** | ❌ **غير موجود إطلاقًا** | لا يوجد أي بوابة (Mada / HyperPay / Moyasar / Tap / Paymob / Apple Pay). البطاقة تُمنح **مجانًا** عند التسجيل، ولا يوجد أي تدفق دفع. |
| جاهزية النشر على Google Play | ⚠️ شبه جاهز | يحتاج إصلاح 3 معوقات حرجة + استكمال بيانات المتجر قبل الرفع. |
| جاهزية «الإنتاج الحقيقي» لشركة الدفع | ❌ غير جاهز بعد | يجب رفع التطبيق إلى Google Play (بنسخة بدون دفع) أولًا، ثم دمج البوابة في إصدار لاحق. |

### الخلاصة المكثّفة
> **التطبيق جاهز هيكليًا وفنيًا للنشر على Google Play بنسخة «عرض بطاقة الخصم فقط» (بدون دفع إلكتروني)، لكنه غير جاهز كمنصة دفع إلكتروني.**
>
> استراتيجية تلبية شرط شركة الدفع الموصى بها:
> 1. إصلاح المعوقات الحرجة (القسم 6) → رفع النسخة `1.0.0` إلى Google Play بنسخة **بدون دفع** (مجرد بطاقة عضوية).
> 2. بعد قبول التطبيق على Google Play ونشره فعليًا → التقدم لشركة الدفع بالمصادقات.
> 3. بعد الحصول على بيانات البوابة → دمجها في النسخة `1.1.0` وإصدار تحديث عبر Google Play.

---

## 1. نظرة عامة على المشروع

### 1.1 المكونات
- **الواجهة (Frontend):** Next.js 16 + React 19 + TypeScript 5 + Tailwind CSS 4 + shadcn/ui + Zustand + React Query v5 + Framer Motion. مُدارة الحزم بـ `bun`.
- **الباك‑إند (Backend):** FastAPI + SQLAlchemy 2.0 async + Pydantic v2 + Alembic + Celery + Redis. Python ≥ 3.12.
- **قاعدة البيانات:** PostgreSQL 16 (إنتاج) + SQLite (تطوير). مع Redis 7 للـ Cache وCelery broker.

### 1.2 المعمارية متعددة الأدوار
يقدّم المشروع **تطبيقين** من قاعدة كود واحدة عبر manifest ديناميكي حسب الـ Host:

| التطبيق | النطاق (Domain) | المعرّف (Package ID) | الجمهور |
|---|---|---|---|
| تطبيق العميل | `wafir.gleeze.com` | `sa.gleeze.wafir` | المستهلكون حملة البطاقة |
| تطبيق المالك (المنشأة) | `facility.wafir.gleeze.com` | `sa.gleeze.wafir.owner` | المطاعم/المقاهي/المرافق |
| لوحة الإدارة | `admin.wafir.gleeze.com` | (ليس PWA) | فريق التشغيل الداخلي |

### 1.3 نموذج العمل الحالي
> **مهم لشركة الدفع:** المنصة حاليًا تبيع **العضوية مجانًا**. عند تسجيل العميل:
> - يُولَّد له رقم عضوية فريد (16 خانة).
> - تُربط بطاقة خصم افتراضية (افتراضي 30%) بحسابه لمدة سنة.
> - تُرسل رسالة ترحيب عبر البريد + SMS (في الإعداد الحالي placeholder فقط).
> - عند زيارة المنشأة، يُظهر العميل بطاقته في التطبيق → يطبّق المالك الخصم في نقطة البيع يدويًا.
>
> **لا يوجد أي تدفق دفع إلكتروني داخل التطبيق لا للعميل ولا للمالك.** سعر `Product.price` معلوماتي فقط (لعرض القائمة).

### 1.4 حجم القاعدة
- 44 نقطة نهاية API موزعة على 4 مجموعات (Public / Customer / Admin / Owner).
- 7 جداول رئيسية: `Region`, `Card`, `Facility`, `facility_card_link`, `User`, `UserCardAssignment`, `Product`, `AuditLog`.
- 13 منطقة سعودية مُزرعة افتراضيًا (الرياض، مكة، المدينة، الدمام، جدة، القصيم، الشرقية، عسير، تبوك، حائل، الجوف، نجران، الحدود الشمالية).

---

## 2. تقييم الجاهزية للسوق السعودي

### 2.1 ✅ الجوانب المتحققة

| المجال | الحالة | الأدلة |
|---|:---:|---|
| التوطين اللغوي | ✅ | `<html lang="ar" dir="rtl">`، خط Cairo (9 أوزان)، تنسيق `Intl.NumberFormat("ar-SA", {currency:"SAR"})`. |
| التحقق من الهاتف السعودي | ✅ | Regex `^\+?9665\d{8}$\|^05\d{8}$` في schemas العميل والمالك. |
| المناطق الإدارية | ✅ | 13 منطقة سعودية مُزرعة + seed لكل منطقة (مطعم + مقهى + بطاقة). |
| معلومات التواصل السعودية | ✅ | هاتف `0547669078`، واتساب `wa.me/966547669078`، بريد `s72468483@gmail.com`. |
| دعم أجهزة Huawei/Honor | ✅ | مُدرج في خطة النشر (AppGallery) — أجهزة منتشرة في السعودية بدون خدمات Google. |
| الاستضافة والنطاق | ✅ | نطاق `gleeze.com` + Vercel للواجهة + خادم للباك‑إند على `api.wafir.gleeze.com`. |
| سياسة الخصوصية | ✅ | صفحة `/privacy` جاهزة ومنشورة. |
| منطق الموافقات على المنشآت | ✅ | تدفق `pending → approved/rejected` مع سبب الرفض ومدقق تدقيق (AuditLog). |
| واجهة RTL بالكامل | ✅ | كل المكونات تستخدم logical properties أو RTL‑friendly. |

### 2.2 ❌ الجوانب المفقودة الحرجة للسوق السعودي

| المجال | الحالة | الأثر |
|---|:---:|---|
| **بوابة الدفع (Mada/HyperPay/Moyasar/Tap)** | ❌ | **لا يمكن تحصيل اشتراك العميل أو عمولة المنشأة إلكترونيًا.** هذا هو الشرط الأساسي لشركة الدفع. |
| **ضريبة القيمة المضافة (VAT 15%)** | ❌ | لا حساب ولا عرض للضريبة على أي سعر أو فاتورة. مخالفة لاشتراطات الهيئة العامة للزكاة والضريبة والجمارك (ZATCA). |
| **الفاتورة الإلكترونية (ZATCA Phase 2)** | ❌ | لا توليد QR ضريبي، لا ربط مع منظومة فاتورة. مطلوب للمنشآت التجارية في السعودية. |
| **التوثيق الوطني (أبشر / نفاث Nafath)** | ❌ | لا يوجد OTP عبر أبشر أو تسجيل دخول وطني. يُضعف الثقة ويعسّر KYC للمنشآت. |
| **ربط منصات الدولة (Mudad / Qiwa / Wathq)** | ❌ | لا ربط للتأكد من سجل المنشآت (Wathq) أو رواتب الموظفين (Qiwa) أو الدفع المباشر (Mudad). |
| **بوابة SMS سعودية فعلية** | ❌ | الحقل `SMS_GATEWAY_API_KEY=your-api-key` (placeholder). لا يوجد ربط فعلي بـ Unifonic أو Msegat أو OurSMS. |
| **رقم الهوية الوطنية / الإقامة** | ❌ | لا يُطلب من العميل أو المالك عند التسجيل. مطلوب للامتثال لـ KYC وللفواتير. |
| **تشفير/تخزين آمن للأسرار** | ❌ | ملف `.env` مرفوع بالمستودع يحتوي على `SECRET_KEY` و`ADMIN_PASSWORD` وDB password حقيقية. |
| **توكن تحديث (Refresh Token)** | ❌ | JWT صلاحيته 15 دقيقة فقط بدون refresh — تجربة عميل سيئة (تسجيل خروج كل ربع ساعة). |
| **اختبارات تلقائية** | ❌ | لا توجد اختبارات في الواجهة، و15 اختبار وحدة فقط في الباك‑إند، ولا اختبارات تكامل/E2E. |

### 2.3 حكم الجاهزية للسوق السعودي
> **غير جاهز للإنتاج التجاري الكامل.** جاهز فقط كنموذج عرض بطاقة خصم مجاني.
>
> لبلوغ «إنتاج سعودي حقيقي» يحتاج: بوابة دفع + VAT + فاتورة إلكترونية + بوابة SMS سعودية فعلية + رفع الأسرار من المستودع + اختبارات.

---

## 3. تقييم الجاهزية للنشر على Google Play

### 3.1 ✅ المتطلبات المتحققة

| المتطلب | الحالة | الدليل |
|---|:---:|---|
| درجة Lighthouse PWA | ✅ | 100/100 للتطبيقين (Lighthouse v9.6.8 — آخر إصدار يدعم فئة PWA). |
| Service Worker | ✅ | مكتوب يدويًا (`src/app/sw.js`) مع استراتيجيات caching متقدمة + offline fallback. |
| Web App Manifest | ✅ | ديناميكي حسب الـ Host، يحتوي `name`, `short_name`, `icons` (192/512/maskable), `screenshots`, `shortcuts`, `theme_color`, `lang=ar`, `dir=rtl`. |
| معرّفات الحزم (Package IDs) | ✅ | `sa.gleeze.wafir` (عميل) + `sa.gleeze.wafir.owner` (مالك) — مختلفة كما يشترط Google Play. |
| الإصدار | ✅ | `1.0.0` / version code `1` (في `src/lib/pwa/version.ts`). |
| **assetlinks.json** | ✅ **مكتمل** | يحتوي على بصمة SHA‑256 فعلية `B1:82:9C:F8:D4:A8:00:99:A3:6B:80:E8:5D:99:DC:8B:96:E2:BD:C2:7C:4D:F4:A6:2C:AE:66:84:45:B8:C4:0D` للتطبيقين. *(ملاحظة: بعض الوثائق الداخلية تشير لكونه فارغًا، لكن الفحص المباشر يُثبت أنه مُعبأ.)* |
| أيقونات التطبيق | ✅ | 192/512/maskable + apple-touch-icon للتطبيقين + iOS splash screens (6 مقاسات). |
| لقطات الشاشة | ✅ | 6 لقطات 1080×1920 في `public/screenshots/` (3 للعميل + 3 للمالك). |
| سياسة الخصوصية | ✅ | `https://wafir.gleeze.com/privacy` — منشورة وقابلة للوصول. |
| الوصف التسويقي بالعربية | ✅ | جاهز في `دليل_التغليف_المتاجر.md`. |
| تصنيف المحتوى | ✅ | تسوّق عام (General Shopping) — مناسب لكل الأعمار. |
| HTTPS | ✅ | Vercel يُوفّر شهادة تلقائيًا (شرط أساسي لـ SW). |
| صفحة Offline | ✅ | `/offline` مع رسالة عربية. |

### 3.2 ⚠️ المعوقات الحرجة قبل الرفع إلى Google Play

> هذه **يجب** حسمها قبل أول رفع، وإلا يُرفض التطبيق أو ينهار بعد النشر:

| # | المعوق | الخطورة | الحل المقترح |
|:--:|---|:---:|---|
| **B1** | **سلسلة ترحيلات Alembic مكسورة** — ملفان لهما `down_revision = None` (`0002_owner_products` و`001_add_membership_fields`) فينتج رأسان متوازيان، فيفشل `alembic upgrade head` على الإنتاج. | 🔴 حرج | توحيد السلسلة: اجعل `001` يتبع `0002`، و`002` يتبع `001`، ثم اختبر `alembic upgrade head` من قاعدة نظيفة. |
| **B2** | **CORS مفتوح على `["*"]`** في `.env` الإنتاجي. السطر الصحيح (المُعدّ للنطاقات الثلاثة) موجود لكنه **مُعلّق (commented out)**. | 🔴 حرج | تفعيل السطر المعلّق وحذف `["*"]` — منع تسريب الـ API لأي مصدر. |
| **B3** | **أسرار مرفوعة في `.env` بالمستودع** (DB password `Wafir@2026#Secure`, SECRET_KEY, ADMIN_PASSWORD). | 🔴 حرج | تدوير كل الأسرار، نقلها إلى متغيرات بيئة الخادم فقط، إضافة `.env` إلى `.gitignore`، وإزالتها من تاريخ Git (`git filter-repo`). |
| **B4** | **`POST /api/v1/owner/login`** — وُثّق في بعض التقارير الداخلية كمعوق، لكن الفحص المباشر يُؤكد أنه **موجود ومُسجّل** في الراوتر (`app/api/v1/endpoints/owner/auth.py` السطر 33). | ✅ مُحلّى | لا يحتاج إجراء. (تحديث التعليق المُعلّق في `owner.service.ts` فقط للوضوح.) |
| **B5** | **نقطة نهاية Dashboard تُرجع schema فارغًا** بينما تتوقع الواجهة 8 حقول. | 🟡 متوسط | مراجعة `dashboard_stats()` في `admin_service.py` وإعادة الحقول المتوقعة من الواجهة. |
| **B6** | **إعدادات الإعدادات (Settings) في الواجهة stub** — أزرار الحفظ تُظهر toast فقط دون استدعاء API فعلي. | 🟡 متوسط | ربط أزرار الحفظ بنقاط نهاية فعلية أو إخفاؤها مؤقتًا. |
| **B7** | **بيانات الملف الشخصي للمالك/Admin hardcoded**. | 🟡 متوسط | ربطها بـ `GET/PUT /me`. |
| **B8** | **الإشعارات في لوحة الإدارة mock data** ثابتة. | 🟡 منخفض | إخفاؤها أو ربطها بمصدر حقيقي (AuditLogs). |
| **B9** | **QR code على بطاقة العميل زخرفي فقط** (SVG غير قابل للمسح). | 🟡 منخفض | توليد QR حقيقي من رقم العضوية باستخدام `qrcode` lib. |
| **B10** | **ملف `BLOCKERS.md` مُشار إليه في `DEPLOYMENT.md` لكنه غير موجود** في المشروع. | 🟡 منخفض | إنشائه أو حذف الإشارة إليه. |
| **B11** | **`owner-api-client.ts` يتجاوز proxy الـ `/api`** ويستخدم URL خلفي مباشر (مخالف لقواعد البوابة). | 🟡 متوسط | توحيد كل العملاء على استخدام `/api` proxy. |
| **B12** | **JWT 15 دقيقة بدون refresh token** — يُسبب تسجيل خروج متكرر للعميل. | 🟡 متوسط | إضافة refresh token (7 أيام) أو تمديد access token لساعة على الأقل لجلسات PWA. |

### 3.3 حكم الجاهزية للنشر على Google Play
> **شبه جاهز.** بعد إصلاح **B1 + B2 + B3** (الحرجة فقط) واختبار `bun run build` ناجح، يمكن رفع النسخة `1.0.0` إلى Google Play Console كـ **Closed testing → Production**.
>
> المعوقات B5–B12 يمكن معالجتها بعد النشر كتحديثات `1.0.x` — لا تمنع الرفع الأول لكن تُضعف التجربة.

---

## 4. 🔴 تقييم تكامل بوابات الدفع (Payment Integration)

> **هذا هو المحور الأهم لتلبية شرط شركة الدفع.**

### 4.1 نتيجة الفحص المباشر
بحث شامل في كامل كود الباك‑إند (`backend/app/`) عن أي من المصطلحات التالية:

```
payment, mada, hyperpay, moyasar, paymob, tap_payments, checkout.com, stripe, apple_pay, subscription, invoice, transaction
```

**النتيجة: صفر مراجع فعلية.** لا يوجد:
- ❌ نموذج بيانات للدفع (No `Payment` / `Transaction` / `Subscription` model).
- ❌ خدمة دفع (No `payment_service.py`).
- ❌ نقطة نهاية دفع (No `/payments`, `/checkout`, `/subscriptions` endpoints).
- ❌ SDK لأي بوابة في `pyproject.toml` (لا `stripe`، لا `moyasar-python`، لا `hyperpay-sdk`...).
- ❌ Webhook handler للدفع.
- ❌ أي إشارة لـ 3‑D Secure أو Strong Customer Authentication.

### 4.2 تفسير نموذج العمل الحالي
المنصة تعمل بنموذج **«بطاقة عضوية مجانية + خصم يدوي عند نقطة البيع»**:
1. العميل يسجّل → يحصل على بطاقة عضوية مجانية (رقم 16 خانة + صلاحية سنة).
2. العميل يزور منشأة شريكة → يُظهر بطاقته في التطبيق.
3. المالك يطبّق الخصم (افتراضي 30%) يدويًا في نظام نقطة البيع الخاص به.
4. **لا تمرّ أي أموال عبر التطبيق.**

### 4.3 ما يعنيه هذا لشرط شركة الدفع
شركة الدفع تشترط:
> «يجب أن يكون التطبيق جاهزًا للإنتاج ومنشورًا على Google Play.»

**هناك طريقتان لتفسير الشرط:**

#### التفسير أ (موصى به — الأسرع للسوق):
النشر بنسخة **بدون دفع إلكتروني** (النسخة الحالية بعد إصلاح B1+B2+B3) كتطبيق «بطاقة عضوية وخصومات»، ثم التقدّم لشركة الدفع بـ:
- رابط التطبيق المنشور على Google Play.
- وثيقة تُوضّح أن الإصدار الحالي `1.0.0` هو إصدار العضويات، وأن **الإصدار `1.1.0`** سيُضيف بوابة الدفع بمجرد الحصول على بيانات الاعتماد من الشركة.
- خطة دمج تقنية (Section 7 أدناه).

#### التفسير ب (الأطول — يتطلب دفعًا قبل النشر):
دمج بوابة دفع أولاً ثم النشر. لكن هذا **مستحيل** لأن شركة الدفع نفسها تطلب التطبيق منشورًا أولًا لتُعطي بيانات الاعتماد → دوران مغلق.

**التوصية: التفسير أ.** ارفع النسخة الحالية (بعد الإصلاحات) → احصل على موافقة Google Play → قدّم لشركة الدفع → ادمج البوابة في `1.1.0` → ارفع تحديثًا.

### 4.4 البوابات السعودية الموصى بها (للنسخة 1.1.0)

| البوابة | الميزة | ملاحظة |
|---||---|
| **HyperPay (Checkout.com Group)** | الأكثر انتشارًا في السعودية، تدعم Mada + Visa + Mastercard + Apple Pay + Mada Pay. | ✅ خيار أول موصى به. |
| **Moyasar** | بوابة سعودية محلية، SDK عربي ممتاز، تدعم Apple Pay وMada. | ✅ خيار ممتاز للوحات السعودية. |
| **Tap Payments** | منتشرة في الخليج، دعم Apple Pay وMada. | ✅ خيار بديل. |
| **Paymob** | قوية في مصر والسعودية، تدعم جميع البطاقات. | خيار احتياطي. |

> **ملاحظة مهمة:** أي بوابة سعودية ستتطلب **KYC للمنشأة** (سجل تجاري ساري + ربط حساب بنكي سعودي) قبل تفعيل المعرف (Merchant ID).

---

## 5. خطة دمج بوابة الدفع (للنسخة 1.1.0 — ما بعد النشر الأول)

> تُنفّذ **بعد** نشر النسخة 1.0.0 على Google Play والحصول على بيانات بوابة الدفع.

### 5.1 نماذج البيانات المطلوب إضافتها

```python
# backend/app/models/payment.py (مقترح)
class SubscriptionPlan(TimestampMixin, Base):
    id: int (PK)
    name_ar: str                  # "اشتراك سنوي"
    price: Numeric(10,2)          # 99.00 SAR
    duration_days: int            # 365
    is_active: bool
    card_id: FK → Card            # البطاقة المُمنوحة عند الاشتراك

class Payment(TimestampMixin, Base):
    id: int (PK)
    user_id: FK → User
    subscription_plan_id: FK → SubscriptionPlan
    amount: Numeric(10,2)
    currency: str = "SAR"
    vat_amount: Numeric(10,2)     # 15% — مطلوب ZATCA
    total_amount: Numeric(10,2)
    gateway: str                  # "hyperpay" | "moyasar" | "tap"
    gateway_checkout_id: str      # معرّف الجلسة لدى البوابة
    gateway_transaction_id: str | None
    status: str                   # "pending" | "paid" | "failed" | "refunded"
    paid_at: datetime | None
    metadata_json: JSON           # رد البوابة الكامل

class PaymentWebhook(TimestampMixin, Base):
    id: int (PK)
    payment_id: FK → Payment
    raw_payload: JSON
    signature_verified: bool
    received_at: datetime
```

### 5.2 نقاط النهاية المطلوب إضافتها

| الطريقة | المسار | الوصف |
|---|---|---|
| `GET` | `/api/v1/subscriptions/plans` | قائمة خطط الاشتراك المتاحة. |
| `POST` | `/api/v1/payments/checkout` | إنشاء جلسة دفع لدى البوابة وإرجاع `checkout_url` / `checkout_id`. |
| `GET` | `/api/v1/payments/{id}` | حالة دفع العميل. |
| `POST` | `/api/v1/payments/webhook/{gateway}` | Webhook receiver (بدون JWT، يُتحقّق من توقيع البوابة). |
| `GET` | `/api/v1/me/payments` | سجل مدفوعات العميل. |

### 5.3 متطلبات أمنية إلزامية للدفع
1. **عدم لمس بيانات البطاقة أبدًا** — استخدم Checkout API (hosted page) أو SDK من البوابة. لا يُسمح بتمرير رقم البطاقة عبر الباك‑إند.
2. **التحقق من توقيع Webhook** بمفتاح البوابة السري قبل أي تحديث لحالة الدفع.
3. **Idempotency Key** لكل عملية دفع لمنع التكرار.
4. **تسجيل كل Webhook** في `PaymentWebhook` للمراجعة والتدقيق.
5. **تشفير `gateway_transaction_id`** في السجل (أو على الأقل hash للمراجعة).
6. **لا تخزّن أي بيانات بطاقة** — استخدم token البوابة فقط.

### 5.4 متطلبات ZATCA للفواتير (إلزامي عند تفعيل الدفع)
عند كل دفع ناجح يجب توليد فاتورة إلكترونية متوافقة مع ZATCA Phase 2:
- **QR code ضريبي** يحتوي: اسم البائع + رقم ضريبي + الطابع الزمني + المبلغ + ضريبة VAT.
- **توقيع رقمي** ( cryptographic stamp) للفاتورة.
- **ربط مع منظومة فاتورة** (Fatoora Portal) عبر SDK الرسمي `fatoora‑sdk`.
- إرسال الفاتورة للعميل عبر البريد + حفظها PDF في تخزين سحابي.

### 5.5 تقدير الجهد
| المهمة | المدة التقديرية |
|---|---|
| نماذج بيانات + migrations | 1–2 يوم |
| خدمة الدفع (HyperPay/Moyasar) | 3–4 أيام |
| Webhook + تحقق التوقيع | 1–2 يوم |
| واجهة checkout + صفحة نجاح/فشل | 2–3 أيام |
| ZATCA QR + توقيع + ربط فاتورة | 3–5 أيام |
| اختبارات تكامل + sandbox | 2–3 أيام |
| **المجموع** | **12–19 يوم عمل** |

---

## 6. قائمة فحص النشر على Google Play (Google Play Submission Checklist)

### 6.1 متطلبات الحساب
- [ ] حساب **Google Play Console** مفعل ($25 رسوم تسجيل لمرة واحدة).
- [ ] تفعيل **2FA** على الحساب.
- [ ] إعداد **developer account** باسم الشركة أو الشخص المسؤول (يجب أن يطابق الاسم في السجل التجاري إن وُجد).
- [ ] تعبئة بيانات **D-U-N-S Number** إن كان حساب مؤسسة (مطلوب للنشر للمؤسسات).

### 6.2 متطلبات التطبيق
- [ ] بناء **AAB** (Android App Bundle) عبر [PWABuilder](https://www.pwabuilder.com/) — Google Play لا يقبل APK منذ 2021.
- [ ] **مفتاح توقيع الـ keystore** (jks) — **احفظه في 3 أماكن منفصلة** (فقدانه = استحالة تحديث التطبيق للأبد).
- [ ] **assetlinks.json** مرفوع على `https://wafir.gleeze.com/.well-known/assetlinks.json` (✅ **موجود ومُعبّأ** بالبصمة الصحيحة).
- [ ] معرّف الحزمة: `sa.gleeze.wafir` (للعميل) و `sa.gleeze.wafir.owner` (للمالك) — مختلفان.
- [ ] رقم الإصدار: `1.0.0` / version code `1`.

### 6.3 متطلبات بيانات المتجر (Store Listing)
- [ ] **اسم التطبيق:** «وفر — بطاقة الخصومات» (عميل) / «وفر — بوابة المنشآت» (مالك).
- [ ] **وصف قصير:** ≤ 80 حرف.
- [ ] **وصف كامل:** ≤ 4000 حرف (متوفر بالعربية في `دليل_التغليف_المتاجر.md`).
- [ ] **أيقونة التطبيق:** 512×512 PNG (موجودة في `public/icons/icon-512.png`).
- [ ] ** банر الإصدار (Feature Graphic):** 1024×500 PNG — ⚠️ **مفقود، يجب تصميمه**.
- [ ] **لقطات الشاشة:** 2–8 لقطات، 1080×1920 (موجودة: 6 لقطات في `public/screenshots/`).
- [ ] **رابط سياسة الخصوصية:** `https://wafir.gleeze.com/privacy` (✅ منشور).
- [ ] **البريد الداعم:** `s72468483@gmail.com` (أو بريد احترافي على نطاق الشركة).
- [ ] **رقم الهاتف الداعم:** `+966547669078`.
- [ ] **التصنيف:** General Shopping — جميع الأعمار.
- [ ] **يحتوي على إعلانات؟** لا.
- [ ] **يحتوي على مشتريات داخل التطبيق؟** **نعم** (للنسخة 1.1.0 عند دمج الدفع) → يجب تعريف `IN_APP_PRODUCTS` permission وتعبئة المنتجات في Play Console. **للنسخة 1.0.0 الحالية:** لا.

### 6.4 متطلبات المحتوى والامتثال
- [ ] **استبيان تصنيف المحتوى (Content Rating):** تعبئته عبر IARC في Play Console.
- [ ] **استبيان أمن البيانات (Data Safety):** تعبئة دقيقة — التطبيق يجمع: الاسم، البريد، رقم الهاتف، معلومات الموقع (للمنطقة) — لا يجمع بيانات حساسة مالية في النسخة 1.0.0.
- [ ] **استبيان الإعلانات (Ads):** لا.
- [ ] **Government Apps:** لا.
- [ ] **Target Audience:** 18+ (لكنه مناسب لكل الأعمار).
- [ ] **Financial Features Declaration:** ⚠️ **مهم** — حتى لو كانت النسخة 1.0.0 بدون دفع، إذا كنت تخطط لإضافة دفع في 1.1.0 يجب الإفصاح عن ذلك.
- [ ] **Privacy Policy URL** مفعّل.
- [ ] **App Access:** تحديد ما إذا كان التطبيق يحتاج صلاحيات خاصة (لا يحتاج في النسخة الحالية).

### 6.5 متطلبات تقنية للـ TWA (Trusted Web Activity)
- [ ] التطبيق يفتح **داخل TWA** (لا متصفح Chrome) — يتحقق ذلك عبر assetlinks.json.
- [ ] **شاشة إطلاق (Splash Screen)** متوافقة مع ألوان التطبيق (✅ موجودة iOS، يجب التأكد من Android).
- [ ] **Status bar customization** عبر `theme-color` (✅ موجودة).
- [ ] **Navigation handling** — الروابط الداخلية تفتح داخل التطبيق، الروابط الخارجية في المتصفح.
- [ ] **Offline behavior** — صفحة `/offline` تُعرض عند انقطاع الإنترنت (✅ موجودة).
- [ ] **Update mechanism** — SW يكتشف التحديثات ويُظهر toast «يتوفر تحديث لتطبيق وفر» (✅ موجود).

### 6.6 مسار الطرح الموصى به
1. **Internal Testing** (1–7 أيام): اختبار داخلي مع 100 مستخدم.
2. **Closed Testing (Alpha/Beta)** (3–14 يوم): اختبار مع مستخدمين مختارين من السوق السعودي.
3. **Open Testing (Beta)** (اختياري، 7–14 يوم): اختبار عام مفتوح.
4. **Production** (مراجعة Google: من ساعات إلى 7 أيام).

---

## 7. خطة العمل والجدول الزمني الموصى به

### المرحلة 1 — الإصلاحات الحرجة قبل النشر (3–5 أيام عمل)
> الهدف: جعل النسخة 1.0.0 صالحة للرفع على Google Play.

| المهمة | الأولوية | المسؤول المقترح | المدة |
|---|:---:|---|---|
| توحيد سلسلة Alembic (B1) | 🔴 | مطور باك‑إند | 4 ساعات |
| إصلاح CORS (B2) | 🔴 | مطور باك‑إند | 30 دقيقة |
| تدوير الأسرار + إزالة `.env` من Git (B3) | 🔴 | DevOps | 4 ساعات |
| إصلاح Dashboard schema (B5) | 🟡 | مطور باك‑إند | 2 ساعة |
| إصلاح `owner-api-client.ts` proxy (B11) | 🟡 | مطور واجهة | 1 ساعة |
| تمديد JWT + إضافة refresh token (B12) | 🟡 | مطور باك‑إند | 4 ساعات |
| اختبار `bun run build` ناجح | 🔴 | مطور واجهة | 1 ساعة |
| تشغيل `verify.sh` + اختبارات التدخّل اليدوي | 🔴 | QA | 4 ساعات |
| **المجموع** | | | **~3–5 أيام** |

### المرحلة 2 — النشر على Google Play (5–14 يوم)
> الهدف: الحصول على تطبيق منشور فعليًا على Google Play.

| المهمة | المدة |
|---|---|
| تصميم Feature Graphic (1024×500) | 1 يوم |
| بناء AAB عبر PWABuilder + التوقيع | 1 يوم |
| إنشاء listing كامل في Play Console (للتطبيقين) | 1 يوم |
| رفع Internal Testing + اختبار | 2–3 أيام |
| رفع Closed Testing + دعوة مختبرين سعوديين | 3–7 أيام |
| الترقية إلى Production + انتظار مراجعة Google | 1–7 أيام |

### المرحلة 3 — التقدم لشركة الدفع (3–10 أيام)
> الهدف: الحصول على بيانات اعتماد بوابة الدفع.

- [ ] تجهيز ملف التعريف التجاري (السجل التجاري + الهوية الوطنية للمفوّض + كشف حساب بنكي).
- [ ] رابط التطبيق المنشور على Google Play (شاهد حي للشركة).
- [ ] خطة تقنية للدمج (Section 5 من هذا التقرير).
- [ ] تقديم الطلب + انتظار المراجعة (1–10 أيام حسب البوابة).

### المرحلة 4 — دمج بوابة الدفع + ZATCA (12–19 يوم عمل)
> الهدف: إطلاق النسخة 1.1.0 ببوابة دفع كاملة.

انظر القسم 5 (خطة الدمج التقنية).

### المرحلة 5 — تحديث Google Play بالنسخة 1.1.0 (2–5 أيام)
- [ ] bump version إلى `1.1.0` / code `2`.
- [ ] بناء AAB جديد.
- [ ] تحديث listing: الإفصاح عن ميزة الدفع + تحديث لقطات الشاشة + استبيان Data Safety.
- [ ] رفع كتحديث للإصدار المنشور.

---

## 8. التوصيات النهائية

### 8.1 توصيات عاجلة (خلال أسبوع)
1. **عالج B1 + B2 + B3 فورًا** — هذه معوقات أمنية وتشغيلية حرجة، رفع التطبيق قبل إصلاحها يُعرّض المستخدمين والشركة للمخاطر.
2. **لا ترفع `.env` إلى المستودع مرة أخرى** — أضِفه إلى `.gitignore` وامسح تاريخه من Git.
3. **اختبر `bun run build` محليًا** قبل أي محاولة رفع — لاحظ أن الوثائق تذكر أن `bun run build` ممنوع في بيئة التطوير السابقة، لكنه **إلزامي** لإنتاج نسخة قابلة للنشر على Vercel.

### 8.2 توصيات قصيرة المدى (خلال شهر)
4. **ارفع النسخة 1.0.0 على Google Play بنسخة «بدون دفع»** — هذا يكسر دورة الـ chicken‑and‑egg مع شركة الدفع.
5. **فعّل بوابة SMS سعودية حقيقية** (Unifonic أو Msegat) — حاليًا رسائل التسجيل لا تُرسل فعلًا.
6. **أضِف refresh token** — تجربة العميل السيئة (تسجيل خروج كل 15 دقيقة) ستُسبب تقييمات سلبية على Google Play.
7. **صمّم Feature Graphic** — مطلوب إلزاميًا من Google Play.

### 8.3 توصيات متوسطة المدى (1–3 أشهر)
8. **ادمج بوابة دفع سعودية (HyperPay أو Moyasar)** — لا يمكن تحصيل اشتراكات بدونها.
9. **طبّق ZATCA Phase 2** (فاتورة إلكترونية + QR ضريبي) — إلزامي قانونيًا لأي تداول مالي في السعودية.
10. **أضِف ربط Wathq** للتحقق من السجل التجاري للمنشآت عند تسجيل المالك.
11. **وفّر Apple App Store** عبر Capacitor (نفس قاعدة الكود) — iPhones منتشرة في السعودية بنسبة ~25%.

### 8.4 توصيات استراتيجية
12. **أضِف نظام تقييمات للمنشآت** — تعزيز التفاعل وميزة تنافسية.
13. **وفّر لوحة تحليلات للمالك** (عدد المشاهدات، عدد استعمالات الخصم، توزيع جغرافي).
14. **أطلق برنامج ولاء (Loyalty)** — تكرار الزيارة = خصم إضافي.
15. **وفّر Huawei AppGallery** — أجهزة Huawei/Honor منتشرة في السعودية بدون خدمات Google.

---

## 9. توقيع واعتماد التقرير

> **هذا التقرير صدر بناءً على فحص مباشر للكود المصدري للمشروع المستخرج من ملف RAR المرفق.**
>
> كل الادعاءات الواردة فيه تم التحقق منها بأدوات الفحص (grep, read, file inspection) على الملفات الفعلية.
> بعض المعوقات المذكورة في الوثائق الداخلية للمشروع (مثل «عدم وجود `POST /owner/login`» أو «assetlinks.json فارغ») تم **تفنديها** بالفحص المباشر واتضح أنها إما محلولة أو غير دقيقة.

### ملخص الأحكام النهائية

| السؤال | الجواب |
|---|---|
| هل المشروع جاهز للنزول إلى السوق السعودي كمنصة دفع كاملة؟ | **❌ لا** — ينقصه بوابة الدفع + VAT + ZATCA + SMS حقيقي. |
| هل المشروع جاهز للنشر على Google Play كـ «تطبيق بطاقة خصومات»؟ | **⚠️ شبه جاهز** — يحتاج 3 إصلاحات حرجة (B1, B2, B3) + Feature Graphic. |
| هل يمكن تلبية شرط شركة الدفع «جاهز للإنتاج + منشور على Google Play»؟ | **✅ نعم — على مرحلتين:** النشر بنسخة 1.0.0 بدون دفع، ثم الدمج في 1.1.0. |
| كم يستغرق الإطلاق الأول على Google Play؟ | **8–19 يومًا** من الآن (3–5 إصلاح + 5–14 مراجعة Google). |
| كم يستغرق دمج بوابة الدفع بعد النشر؟ | **12–19 يوم عمل** (يشمل ZATCA). |

---

**نهاية التقرير.**
