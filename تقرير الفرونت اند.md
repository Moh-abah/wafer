تقرير فحص وتحليل مشروع "وفر (Wafir)" — الفرونت إند
1. نظرة عامة على المشروع
اسم المشروع: وفر (Wafir)
النوع: منصة إلكترونية لبطاقات الخصم بنسبة 30%
الدور: واجهة أمامية (Frontend-Only) تستهلك API من خادم باك إند خارجي
الحجم: ~1.5 ميغابايت (بدون node_modules) | ~9,900 سطر كود مكتوب | 104 ملف TypeScript/TSX

"وفر" هي منصة تتيح للمستخدمين تصفح بطاقات الخصم المتاحة حسب المناطق الجغرافية، مع إمكانية تسجيل العضوية للحصول على بطاقة خصم 30% على المطاعم والمقاهي والمرافق العامة. المنصة مقسمة إلى واجهتين رئيسيتين:

واجهة عامة (Public): للزوار والعملاء
لوحة تحكم (Admin): لإدارة محتوى المنصة بالكامل
2. التقنيات المستخدمة (Tech Stack)
الفئة
التقنية
الإصدار
الإطار الأساسي	Next.js 16 (App Router)	^16.1.1
اللغة	TypeScript 5	^5
مكتبة React	React 19	^19.0.0
التنسيق	Tailwind CSS 4 + tw-animate-css	^4
مكونات UI	shadcn/ui (48 مكوّن)	New York Style
أيقونات	Lucide React	^0.525.0
إدارة الحالة (Client)	Zustand	^5.0.6
جلب البيانات	TanStack React Query	^5.82.0
نماذج النماذج	React Hook Form + Zod 4	^7.60.0 / ^4.0.2
تغيير الثيم	next-themes	^0.4.6
الحركات	Framer Motion	^12.23.2
خط عربي	Google Fonts — Cairo	—
الإخراج	Standalone mode (للنشر)	—

مكتبات غير مستخدمة (تم تثبيتها لكنها ليست جزءًا من الكود الفعلي):
@dnd-kit/* — سحب وإفلات (غير مستخدم)
@mdxeditor/editor — محرر MDX (غير مستخدم)
next-auth — مصادقة (غير مستخدم، تم بناء نظام مصادقة مخصص)
next-intl — تدويل (غير مستخدم، اللغة عربية ثابتة)
@reactuses/core — هوكات React عامة (غير مستخدمة)
@tanstack/react-table — جدول متقدم (غير مستخدم، تم استخدام shadcn Table)
react-markdown + react-syntax-highlighter — عرض Markdown (غير مستخدم)
recharts — رسوم بيانية (غير مستخدم)
sharp — معالجة صور (غير مستخدم)
sonner — إشعارات (تم استبداله بـ shadcn Toast)
prisma — ORM (غير مستخدم في هذا الفرونت إند)
⚠️ ملاحظة: توجد حزمة كبيرة من المكتبات غير المستخدمة في package.json تزيد حجم المشروع بشكل غير ضروري. يُنصح بإزالتها لتقليل حجم node_modules وتسريع التثبيت.

3. بنية المجلدات (Directory Structure)
text

src/
├── app/                           ← مسارات Next.js App Router
│   ├── layout.tsx                 ← التخطيط الجذري (RTL + Cairo Font)
│   ├── providers.tsx              ← ThemeProvider + QueryClient + Toaster
│   ├── globals.css                ← متغيرات الألوان + الأنماط الأساسية
│   ├── (public)/                  ← مجموعة المسارات العامة (بدون بادئة URL)
│   │   ├── layout.tsx             ← تخطيط عام (Header + Footer)
│   │   ├── page.tsx               ← الصفحة الرئيسية (/)
│   │   └── register/page.tsx      ← صفحة التسجيل (/register)
│   ├── (admin)/admin/             ← مجموعة مسارات لوحة التحكم
│   │   ├── layout.tsx             ← تخطيط الأدمن (Sidebar + AuthGuard)
│   │   ├── page.tsx               ← لوحة المعلومات (/admin)
│   │   ├── regions/page.tsx       ← إدارة المناطق
│   │   ├── cards/page.tsx         ← إدارة البطاقات
│   │   ├── facilities/page.tsx    ← إدارة المنشآت
│   │   ├── users/page.tsx         ← إدارة العملاء
│   │   └── audit-logs/page.tsx    ← سجل العمليات
│   ├── admin/login/page.tsx       ← تسجيل دخول الأدمن (خارج المجموعة)
│   └── api/route.ts               ← API Route بسيط (Hello World)
│
├── components/
│   ├── admin/                     ← مكونات لوحة التحكم (5 ملفات)
│   │   ├── AdminAuthGuard.tsx     ← حماية المسارات
│   │   ├── RegionForm.tsx         ← نموذج إضافة/تعديل منطقة
│   │   ├── CardForm.tsx           ← نموذج إضافة/تعديل بطاقة
│   │   ├── FacilityForm.tsx       ← نموذج إضافة/تعديل منشأة
│   │   ├── UsersTable.tsx         ← جدول العملاء
│   │   └── AuditTable.tsx         ← جدول سجل العمليات
│   ├── layout/                    ← مكونات التخطيط (3 ملفات)
│   │   ├── MainHeader.tsx         ← الهيدر العام
│   │   ├── Footer.tsx             ← الفوتر
│   │   └── AdminSidebar.tsx       ← الشريط الجانبي (Desktop + Mobile)
│   ├── public/                    ← مكونات الواجهة العامة (6 ملفات)
│   │   ├── RegionSelector.tsx     ← منتقي المنطقة
│   │   ├── CardItem.tsx           ← بطاقة واحدة
│   │   ├── CardsGrid.tsx          ← شبكة البطاقات
│   │   ├── FacilitiesList.tsx     ← قائمة المنشآت
│   │   └── RegisterForm.tsx       ← نموذج التسجيل
│   ├── theme/                     ← مكونات الثيم (1 ملف)
│   │   └── ThemeToggle.tsx        ← زر تبديل الوضع المظلم/الفاتح
│   └── ui/                        ← مكتبة shadcn/ui (48 مكوّن)
│
├── hooks/                         ← هوكات React مخصصة (10 هوكات)
│   ├── useRegions.ts              ← جلب المناطق + اختيار تلقائي
│   ├── useCards.ts                ← جلب البطاقات حسب المنطقة
│   ├── useFacilities.ts           ← جلب المنشآت حسب المنطقة
│   ├── useRegister.ts             ← تسجيل عضوية جديد
│   ├── useAdminAuth.ts            ← تسجيل دخول/خروج الأدمن
│   ├── useAdminRegions.ts         ← CRUD المناطق
│   ├── useAdminCards.ts           ← CRUD البطاقات
│   ├── useAdminFacilities.ts      ← CRUD المنشآت
│   ├── useAdminUsers.ts           ← قائمة العملاء (مع بحث)
│   └── useAdminAuditLogs.ts       ← سجل العمليات
│
├── services/                      ← طبقة الخدمات (API) (6 ملفات)
│   ├── api-client.ts              ← عميل HTTP موحد مع مصادقة
│   ├── auth.service.ts            ← خدمة المصادقة
│   ├── region.service.ts          ← خدمة المناطق
│   ├── card.service.ts            ← خدمة البطاقات
│   ├── facility.service.ts        ← خدمة المنشآت
│   ├── user.service.ts            ← خدمة المستخدمين
│   └── admin.service.ts           ← خدمة سجل العمليات
│
├── store/                         ← إدارة الحالة (Zustand) (3 مخازن)
│   ├── auth.store.ts              ← مخزن المصادقة + إدارة الكوكيز
│   ├── region.store.ts            ← مخزن المنطقة المختارة
│   └── ui.store.ts                ← مخزن حالة واجهة المستخدم
│
├── types/                         ← تعريفات TypeScript (2 ملف)
│   ├── api.generated.ts           ← أنواع البيانات الأساسية
│   └── api.ts                     ← نوع Paginated<T>
│
├── lib/                           ← أدوات مساعدة (2 ملف)
│   ├── utils.ts                   ← دالة cn() لدمج الأصناف
│   └── db.ts                      ← ملف Prisma (غير مستخدم فعليًا)
│
└── middleware.ts                   ← حماية مسارات /admin/*
4. البنية المعمارية (Architecture)
4.1 نمط الطبقات (Layered Architecture)
المشروع يتبع نمط طبقات واضح ومنظم:

text

[Pages/Components]  →  [Hooks]  →  [Services]  →  [API Client]  →  [Backend]
       ↓                    ↓           ↓
  [Zustand Stores]    [React Query Cache]
طبقة العرض (Pages/Components): مكونات React تعرض البيانات فقط ولا تتواصل مباشرة مع الـ API
طبقة الهوكس (Hooks): تغلف منطق جلب البيانات (React Query) والعمليات (Mutations) مع إبطال الكاش تلقائيًا
طبقة الخدمات (Services): تعرف نقاط النهاية (Endpoints) وأنواع البيانات لكل كيان
عميل API الموحد (API Client): يتعامل مع المصادقة (Bearer Token)، الأخطاء، وتنسيق الطلبات
البروكسي (Next.js Rewrites): يمرر طلبات /api/v1/* إلى الباك إند على localhost:8000 لتجنب مشاكل CORS
4.2 إدارة الحالة
الحالة
الأداة
الوصف
بيانات الخادم	TanStack React Query	جلب وتخزين مؤقت + إبطال تلقائي بعد التعديل
المصادقة	Zustand (auth.store)	توكن JWT في الذاكرة + كوكيز (7 أيام)
المنطقة المختارة	Zustand (region.store)	رقم المنطقة الحالية (يؤثر على البطاقات والمنشآت)
واجهة المستخدم	Zustand (ui.store)	حالة القائمة الجانبية
الثيم	next-themes	مظلم/فاتح (افتراضي: مظلم)

4.3 نظام المصادقة
آلية: JWT Token مخزن في كوكيز wafir_admin_token (7 أيام) + في Zustand (ذاكرة)
Middleware: يحمي مسارات /admin/* (ما عدا /admin/login) عبر فحص وجود الكوكيز
Client Guard: AdminAuthGuard يتحقق من hydrate + token على الجانب العميل
عند 401: يتم مسح التوكن تلقائيًا وإعادة التوجيه لصفحة الدخول
كلمة مرور: ليست مشفرة محليًا (تُرسل كنص عادي إلى الباك إند)
5. تحليل الواجهة العامة (Public Interface)
5.1 الصفحة الرئيسية (/)
قسم Hero: شعار وفر (باستخدام CSS mask) + عنوان "وَفِّر أكثر مع بطاقات الخصم" + زر CTA
قسم البطاقات: شبكة متجاوبة (1/2/3 أعمدة) تعرض بطاقات الخصم المتاحة حسب المنطقة
قسم المنشآت: شبكة مطاعم/مقاهي/مرافق مع شارات ملونة حسب النوع
كل قسم يحتوي على: Skeleton أثناء التحميل → رسالة خطأ مع زر إعادة المحاولة → حالة فارغة
5.2 صفحة التسجيل (/register)
نموذج يضم 6 حقول: الاسم الكامل، البريد، الجوال، المنطقة (من Zustand)، كلمة المرور، تأكيد كلمة المرور
تحقق من الصحة باستخدام Zod (أسماء عربية، تحقق من تطابق كلمتي المرور)
مزامنة حقل المنطقة بين RegionSelector وreact-hook-form عبر useEffect
تُرسل password_confirm مع البيانات (يتوافق مع الباك إند)
5.3 التخطيط العام
Header: ثابت أعلى الصفحة (sticky) مع تأثير backdrop-blur، يحتوي: شعار + شارة "خصم 30%" + منتقي المنطقة + زر التسجيل + زر الثيم
Footer: يلتصق بالأسفل (sticky) عند قلة المحتوى وينزل طبيعيًا عند وجود محتوى كثير
6. تحليل لوحة التحكم (Admin Dashboard)
6.1 صفحة تسجيل الدخول (/admin/login)
صفحة مستقلة (خارج مجموعة admin) بتصميم centered
حقلان: اسم المستخدم + كلمة المرور مع تحقق Zod
يُحوَّل تلقائيًا إلى /admin إذا كان مسجل الدخول بالفعل
6.2 لوحة المعلومات (/admin)
4 بطاقات إحصائية: عدد المناطق، البطاقات، المنشآت، العملاء
جميعها تعرض Skeleton أثناء التحميل و"—" عند عدم توفر البيانات
6.3 إدارة المناطق (/admin/regions)
جدول كامل: الاسم، المعرّف (slug)، الحالة (مفعّلة/معطّلة)، إجراءات (تعديل/حذف)
نموذج Dialog لإضافة/تعديل (حقل واحد: الاسم)
حذف مع تأكيد AlertDialog
6.4 إدارة البطاقات (/admin/cards)
جدول: الاسم، المنصة، المنطقة (محلولة من Map)، الترتيب، الحالة (منشورة/مخفية)
نموذج Dialog: 5 حقول + Switch للنشر
يحلل أسماء المناطق باستخدام useMemo لتحويل المصفوفة إلى Map
6.5 إدارة المنشآت (/admin/facilities)
جدول: الاسم، النوع (شارة ملونة)، المنطقة، الترتيب، الحالة
نموذج Dialog متقدم: اسم، نوع (Select)، منطقة، وصف، ترتيب، ظاهرة/مخفية، ربط بطاقات متعددة (Checkboxes)
6.6 إدارة العملاء (/admin/users)
جدول يعرض: الاسم، البريد، الجوال، أدمن؟، تاريخ التسجيل
بحث مُبطأ (debounced 300ms) مع أيقونة Search
تواريخ معروضة بالعربية (toLocaleString("ar-SA"))
6.7 سجل العمليات (/admin/audit-logs)
جدول: نوع العملية (شارة عربية)، التفاصيل، IP، التاريخ
يتحديث تلقائيًا عند العودة للنافذة (refetchOnWindowFocus)
قابل للتمرير بحد أقصى 70vh
6.8 الشريط الجانبي (Sidebar)
Desktop: ثابت (sticky) على اليمين (RTL)، قابل للطي/التوسيع (w-64 ↔ w-16) مع رسوم حركة
Mobile: Sheet (درج) يُفتح بالنقر على زر القائمة في الهيدر
6 عناصر تنقل مع تمييز الصفحة النشطة + زر تسجيل الخروج + ThemeToggle
7. نظام الألوان والتصميم
7.1 لوحة الألوان
المشروع يستخدم نظام ألوان emerald/أخضر كعنصر أساسي (primary) مع تدرجات oklch الحديثة:

Primary (فاتح): oklch(0.40 0.18 210) — أخضر داكن
Primary (مظلم): oklch(0.6 0.15 210) — أخضر متوسط
لا يوجد ألوان Indigo أو أزرق (كما هو مطلوب في المعايير)
7.2 الخطوط
الخط الرئيسي: Cairo (خط عربي من Google Fonts) — متغير --font-cairo
الخط الأحادي: Geist Mono — متغير --font-geist-mono
اتجاه النص: RTL (من اليمين لليسار) على مستوى <html>
7.3 الثيم
الافتراضي: الوضع المظلم (Dark Mode)
التبديل: عبر زر ThemeToggle (أيقونة شمس/قمر) في كل صفحة
التطبيق: عبر class attribute على <html> (next-themes)
8. نقاط القوة
النقطة
التفصيل
بنية نظيفة	فصل واضح بين المكونات والهوكس والخدمات والمخازن
تعامل ذكي مع الأخطاء	كل قسم يحتوي على 4 حالات: تحميل → خطأ+إعادة محاولة → فارغ → بيانات
متجاوب بالكامل	تصميم Mobile-First مع breakpoints sm/md/lg
حماية جيدة	Middleware + Client Guard مزدوجة لمسارات الأدمن
كاش ذكي	إبطال تلقائي لكاش admin + public بعد أي عملية تعديل
واجهة عربية أصيلة	RTL كامل، أخطاء تحقق بالعربية، تواريخ عربية
بدون console.log	لا توجد أي عمليات طباعة للكونسول في الكود
لا يوجد TODO/FIXME	الكود نظيف من التعليقات المعلقة
استخدام useWatch	توافق مع React Compiler عبر استخدام useWatch بدلاً من watch

9. المشاكل والملاحظات (Bugs & Issues)
🔴 مشاكل حرجة
9.1 تعارض نوع في useRegister.ts
typescript

// الخطأ: الدالة onSuccess تصل إلى response.detail لكن النوع RegisterResponse
// لا يحتوي على خاصية detail، بل يحتوي على message
onSuccess: (response) => {
  toast({
    title: "تم التسجيل بنجاح",
    description: response.detail || "أهلاً بك",  // ❌ should be response.message
  });
},
الحل: تغيير response.detail إلى response.message

9.2 استخدام any في useRegister.ts
typescript

onError: (error: any) => {  // ❌ نوع any محظور
الحل: استخدام نوع Error أو ApiError

9.3 كود محجوب (Dead Code) في FacilityForm.tsx
الملف يحتوي على 281 سطرًا من الكود المحجوب (النسخة الأولى كاملة محجوبة بالكامل) مما يجعله 562 سطرًا بدلاً من ~280 سطرًا. هذا يزيد حجم الملف بشكل مضاعف ويخلق ارتباكًا.

9.4 كود محجوب في ملفات الخدمات
facility.service.ts: 17 سطر محجوب (النسخة الأولى)
user.service.ts: 11 سطر محجوب (النسخة الأولى)
🟡 مشاكل متوسطة
9.5 reactStrictMode: false في next.config.ts
تم تعطيل Strict Mode، وهو ميزة مهمة لاكتشاف المشاكل أثناء التطوير مثل التأثيرات الجانبية المزدوجة والتحديثات القديمة.

9.6 ignoreBuildErrors: true في next.config.ts
تم تعطيل فحص أخطاء TypeScript أثناء البناء، مما قد يسمح بأخطاء نوعية بالمرور إلى الإنتاج.

9.7 api/route.ts غير مستخدم
typescript

// src/app/api/route.ts — مجرد Hello World بلا فائدة
export async function GET() {
  return NextResponse.json({ message: "Hello, world!" });
}
9.8 src/lib/db.ts غير مستخدم
الملف موجود لكن لا يُستخدم في أي مكان (المشروع لا يستخدم Prisma).

9.9 عدم وجود تحقق من صحة بيانات الأدمن في forms
نماذج الأدمن (CardForm, FacilityForm) تعتمد فقط على Zod ولا تُظهر أخطاء لكل حقل بشكل منفصل عند فشل mutateAsync من الباك إند.

🟢 ملاحظات تحسينية
9.10 مكتبات غير مستخدمة في package.json
~15 مكتبة مثبتة لكنها غير مستخدمة في الكود الفعلي. يُنصح بإزالتها:

text

@dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities,
@mdxeditor/editor, next-auth, next-intl, @reactuses/core,
@tanstack/react-table, react-markdown, react-syntax-highlighter,
recharts, sharp, sonner, @prisma/client, prisma
9.11 شعار CSS Mask بدلاً من <img>
الشعار يُعرض باستخدام mask-image + background-color مما يعني أنه يظهر بلون primary فقط. هذا نهج ذكي لتغيير اللون تلقائيًا مع الثيم، لكنه لا يعمل إذا كان الشعار ملونًا.

9.12 عدم وجود SEO متقدم
لا توجد صفحات sitemap.xml أو robots.txt مخصصة (يوجد robots.txt بسيط في public). لا توجد بيانات منظمة (JSON-LD).

9.13 عدم وجود تحميل كسول (Lazy Loading)
جميع الصفحات تُحمّل فورًا. يمكن تحسين الأداء عبر dynamic imports لصفحات الأدمن.

9.14 عدم وجود معالجة للاتصال بالإنترنت
لا توجد حالة "أنت غير متصل بالإنترنت" أو إعادة محاولة تلقائية.

10. ملخص جودة الكود
المعيار
التقييم
الملاحظة
نظافة الكود	⭐⭐⭐⭐☆	جيد، لكن يوجد كود محجوب يجب إزالته
بنية المجلدات	⭐⭐⭐⭐⭐	ممتازة، فصل واضح للمسؤوليات
أنواع TypeScript	⭐⭐⭐⭐☆	جيدة عمومًا، مع بعض التعارضات
التعامل مع الأخطاء	⭐⭐⭐⭐⭐	ممتاز، كل حالة مغطاة
التصميم المتجاوب	⭐⭐⭐⭐⭐	ممتاز، Mobile-First كامل
إمكانية الوصول	⭐⭐⭐⭐☆	جيد (ARIA labels, roles)، يمكن تحسينه
الأمان	⭐⭐⭐⭐☆	جيد (JWT + cookie + middleware)، بدون CSRF protection
الأداء	⭐⭐⭐☆☆	متوسط، لا يوجد lazy loading أو code splitting
الصيانة	⭐⭐⭐⭐☆	جيد، يمكن تحسينه بإزالة المكتبات غير المستخدمة

الحجم الفعلي للكود المكتوب (بدون shadcn/ui):
الصفحات: 10 ملفات (~1,500 سطر)
المكونات المخصصة: 15 ملف (~1,800 سطر)
الهوكس: 10 ملفات (~400 سطر)
الخدمات: 7 ملفات (~200 سطر)
المخازن: 3 ملفات (~100 سطر)
الأنواع: 2 ملفات (~80 سطر)
الإعداد: 4 ملفات (~150 سطر)
المجموع الفعلي: ~4,230 سطر
11. الخلاصة والتوصيات
"وفر" هو مشروع فرونت إند منظم جيدًا ومبني على معايير حديثة. البنية المعمارية نظيفة ومفصولة بشكل صحيح، والتعامل مع الأخطاء شامل، والتصميم متجاوب بالكامل ويدعم العربية بشكل أصيل. المشروع جاهز للاتصال بباك إند يعمل على localhost:8000 وتقديم منصة كاملة.

أهم التوصيات:
إزالة الكود المحجوب من FacilityForm.tsx وملفات الخدمات
إصلاح تعارض الأنواع في useRegister.ts (detail → message)
إزالة المكتبات غير المستخدمة من package.json
تفعيل reactStrictMode في next.config.ts
إضافة تحميل كسول لصفحات لوحة التحكم
إضافة حماية CSRF للنماذج الحساسة
تم إعداد التقرير الشامل أعلاه. إليك ملخص سريع:

ما هو المشروع؟ منصة "وفر" — واجهة أمامية (Frontend-Only) مبنية بـ Next.js 16 لعرض بطاقات خصم 30% على المطاعم والمقاهي والمرافق العامة، مع لوحة تحكم كاملة للأدمن. تتصل بباك إند خارجي عبر API proxy.

البنية: 104 ملف TypeScript، ~4,230 سطر كود فعلي (بدون shadcn/ui)، بُنيت بمعمارية طبقات نظيفة (Components → Hooks → Services → API Client).

نقاط القوة الرئيسية: بنية منظمة، تعامل ممتاز مع الأخطاء (4 حالات لكل قسم)، تصميم متجاوب كامل، RTL عربي أصيل، نظام مصادقة JWT مزدوج (middleware + client guard)، كاش ذكي مع إبطال تلقائي.

أهم المشاكل المكتشفة: كود محجوب (~300 سطر) يجب إزالته، تعارض نوع في useRegister.ts، ~15 مكتبة غير مستخدمة، Strict Mode معطّل، لا يوجد تحميل كسول لصفحات الأدمن.



