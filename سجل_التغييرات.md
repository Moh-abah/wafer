# سجل التغييرات — تحويل «وفر» إلى تطبيقي PWA

> سجل زمني لكل مرحلة من مراحل العمل، بالملفات الناتجة عنها.
> التفاصيل التقنية الكاملة في `تقرير_PWA.md` والسجل التفصيلي في `worklog.md`.

---

## المرحلة 0 — نقل المشروع وتهيئة البيئة ✅

**الملفات:** بنية المشروع كاملة من `wafir.rar` → `/home/z/my-project` + `package.json` مدموج

- فك ضغط الأرشيف، قراءة الأصول المحمية قبل أي تعديل، نقل src/public/الإعدادات
- دمج الحزم: أساس وفر + سكربتات البيئة (dev مع dev.log، typecheck، icons) + sharp لتوليد الأيقونات
- تصحيح 3 إصدارات حزم غير موجودة في المستودع المحلي (react-toggle/aspect-ratio/avatar) من ملف القفل المرفق
- التحقق: كل المسارات 200 (الرئيسية، الدخولان، المتاجر) + `/api/regions` 200 عبر الـ rewrite للباك إند الحي
- تثبيت خطوط Cairo في fontconfig ليعمل تحويل SVG→PNG بالنص العربي

## المرحلة 1-أ — الأيقونات ✅

**الملفات:** `scripts/generate-icons.ts` + 16 ملفاً في `public/icons/` (منها `splash/`)

- أيقونات العميل (تاغ ذهبي مبتسم + «وفر») عادية + maskable بخلفية بيضاء وحشوة أمان 80%
- أيقونات المالك المميزة: حلقة ذهبية + حرف «و» على تدرج سماوي→محيط→عميق
- apple-touch-icon للعميل والمالك + 6 شاشات انطلاق iOS للعميل
- ضبط التمركز بالتحقق البكسلي (±3px) بعد اكتشاف خطأ «فر» البيضاء على الأبيض عبر VLM

## المرحلة 1-ب — الـ Manifest الديناميكي والميتا ✅

**الملفات:** `src/app/manifest.webmanifest/route.ts` (جديد)، حذف `src/app/manifest.ts`، تعديل `layout.tsx` × 3، `middleware.ts`

- مسار يقرأ Host (+`?app=` للفرض): العميل/المالك بكل الحقول الإلزامية (description تسويقية، categories، screenshots، shortcuts للعميل، prefer_related_applications:false)
- ميتا ديناميكية: manifest + theme-color + apple-touch-icon + appleWebApp حسب البوابة
- layout بوابة المالك Server component (ميتا المالك) + OwnerPortalShell عميل
- توسيع استثناءات middleware لتخدم أصول PWA على كل النطاقات

## المرحلة 2 — Service Worker والأوفلاين والتحديثات ✅

**الملفات:** `src/lib/pwa/sw-source.ts`، `src/app/sw.js/route.ts`، `src/lib/pwa/version.ts`، `src/components/pwa/ServiceWorkerRegistrar.tsx`، `src/app/offline/page.tsx`

- SW عبر route handler مع حقن NODE_ENV والنسخة (قرار موثق: يدوي بدل serwist لعدم توافقه مع Turbopack)
- الاستراتيجيات: SWR للكتالوج (بقاء أوفلاين للأبد) / NetworkOnly للتوكنات والعمليات / CacheFirst للصور بحد 50 / NetworkFirst للتنقلات + /offline / كاش RSC
- رسائل الأوفلاين العربية من الـ SW نفسه (503 detail) بلا تعديل عملاء API المحميين
- شريحة «يتوفر تحديث لتطبيق وفر» + «تحديث الآن» (skipWaiting + reload واحد) + فحص دوري وعند عودة الاتصال
- صفحة /offline بالهوية + OfflineBanner واعٍ بالبوابة (نُقل إلى Providers)

## المرحلة 2 (تتمة) — زر التثبيت ✅

**الملفات:** `src/components/pwa/PWAInstallButton.tsx`، `src/hooks/usePwaInstall.ts`، `src/store/pwa.store.ts` + تعديلات المواضع

- مواضع العميل: حسابي (زائر+مسجل) + بطاقة موبايل بالرئيسية | المالك: فوق نموذج الدخول + الإعدادات
- beforeinstallprompt للمطالبة الفورية، إرشاد iOS (شارك ← إضافة إلى الشاشة الرئيسية)، إخفاء دائم بعد الرفض (localStorage لكل بوابة)

## المرحلة 3 — تجهيزات المتاجر ✅

**الملفات:** `public/.well-known/assetlinks.json`، `src/app/(public)/privacy/page.tsx`، إعادة كتابة `settings/page.tsx` للمالك، تعديل `Footer.tsx`

- assetlinks قالب فارغ يُخدم 200 (خطوات تعبئته في دليل التغليف)
- سياسة الخصوصية العربية بالهوية + رابط الفوتر
- إعدادات المالك: صفر ميزات وهمية — زر تثبيت + تواصل حقيقي + إصدار + خروج

## المرحلة 4 — التحقق الإلزامي ✅

**الأدلة:** نتائج Lighthouse الخام + لقطات الفحص في `worklog.md`

- lint 0 أخطاء + typecheck ناجح
- **Lighthouse PWA 100/100 للتطبيقين** (v9.6.8)
- تدفق التحديث مُثبت 3 مرات فعلياً (بانر → تحديث الآن → إعادة تحميل)
- الأوفلاين فعلياً: كاش البيانات يُخدم مع خادم مقتول + الرسالتان العربيتان في الواجهة + صفحة /offline + عودة الاتصال تطلق تحديثين للبيانات
- فحص أمان الكاشات: صفر توكنات/طلبات حساسة
- 6 لقطات متجر 1080×1920 بمحتوى حقيقي + الوضعان سليمان + 360px بلا overflow
- إصلاحان اكتُشفا بالاختبار: سباق clone في كاش التنقلات + precache لـ api/regions + تثبيت المنطقة المختارة

## المرحلة 5 — التوثيق ✅

**الملفات:** `تقرير_PWA.md` + `دليل_النشر_PWA.md` + `دليل_التغليف_المتاجر.md` + `سجل_التغييرات.md` (هذا الملف)

- التقرير: جداول Added/Modified/Deleted + نتائج كل بند تحقق بالأدلة + BLOCKERS وحلولها
- دليل النشر: Vercel + الدومينات + اختبار التثبيت + آلية التحديث + قيود iOS
- دليل التغليف: PWABuilder خطوة بخطوة + مفتاح التوقيع وassetlinks بمثال كامل + Play/Huawei/Apple + قائمة تحقق نهائية
