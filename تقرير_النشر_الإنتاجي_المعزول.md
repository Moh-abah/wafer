# تقرير النشر الإنتاجي المعزول — مشروع وفر (Wafir Backend)
## تجهيز الباك‑إند للنشر على سيرفر الإنتاج مع Docker + PostgreSQL

> **تاريخ التقرير:** 2026-09-05
> **الهدف:** تجهيز الباك‑إند للنشر الإنتاجي المعزول على سيرفر `169.58.114.92` باستخدام Docker Compose + PostgreSQL 16 + Redis 7 + Celery (worker + beat).
> **المتطلب:** عزل كامل — شبكة خاصة، ports مختلفة، قاعدة بيانات منفصلة، لا تعارض مع المشاريع الأخرى على السيرفر.

---

## 1. بنية النشر المعزول

```
┌─────────────────────────────────────────────────────────┐
│                   سيرفر الإنتاج                          │
│                   169.58.114.92                          │
│                                                         │
│  ┌───────────┐  ┌───────────┐  ┌──────────────────────┐│
│  │ wafir2-db │  │wafir2-redis│  │  wafir2-backend      ││
│  │ PostgreSQL│  │  Redis 7  │  │  FastAPI + Uvicorn   ││
│  │   :5434   │  │   :6381   │  │  :3006 → :8000       ││
│  └─────┬─────┘  └─────┬─────┘  └──────────┬───────────┘│
│        │              │                   │              │
│        │              │      ┌────────────┴───────────┐  │
│        │              │      │ wafir2-celery-worker   │  │
│        │              ├──────│  (مهام خلفية)          │  │
│        │              │      └────────────────────────┘  │
│        │              │      ┌────────────────────────┐  │
│        │              └──────│ wafir2-celery-beat     │  │
│        │                     │  (جدولة دورية)        │  │
│        │                     └────────────────────────┘  │
│        │                                                   │
│  ┌─────┴─────────────────────────────────────────────┐   │
│  │           شبكة wafir2-net (bridge معزولة)         │   │
│  └───────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────────┐│
│  │  volumes: wafir2-db-data + wafir2-redis-data        ││
│  │  (بيانات دائمة، لا تُحذف عند إيقاف الحاويات)       ││
│  └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```

### المنافذ المعزولة (لا تتعارض مع المشاريع الأخرى)

| الخدمة | المنفذ الخارجي | المنفذ الداخلي | الحاوية |
|---|:---:|:---:|---|
| Backend (API) | **3006** | 8000 | `wafir2-backend` |
| PostgreSQL | **5434** | 5432 | `wafir2-db` |
| Redis | **6381** | 6379 | `wafir2-redis` |
| Celery Worker | — | — | `wafir2-celery-worker` |
| Celery Beat | — | — | `wafir2-celery-beat` |

> **ملاحظة:** المنافذ 3005, 5433, 6380 مستخدمة من قبل النسخة القديمة من وفر. المنافذ 3020, 5440, 6390 مستخدمة من قبل مشروع tawfir. المنافذ 8000, 5432, 6379 مستخدمة من قبل furateen. لذا استخدمنا 3006, 5434, 6381.

---

## 2. الملفات المُنشأة للنشر

### 2.1 `docker-compose.yml` (مُحدّث)
- **5 خدمات:** PostgreSQL 16 + Redis 7 + Backend + Celery Worker + Celery Beat
- **شبكة معزولة:** `wafir2-net` (bridge driver)
- **Volumes معزولة:** `wafir2-db-data` + `wafir2-redis-data`
- **Health checks** لكل خدمة (PostgreSQL: `pg_isready`, Redis: `redis-cli ping`, Backend: `curl /health`, Celery: `inspect ping`)
- **depends_on مع condition: service_healthy** — لا يبدأ Backend حتى تصبح DB + Redis سليمتين
- **env_file:** `.env.production` — كل الأسرار من ملف واحد
- **restart: unless-stopped** — إعادة تشغيل تلقائية عند الفشل

### 2.2 `manage.sh` (سكربت إدارة شامل)
سكربت Bash كامل بـ 17 أمراً:

| الأمر | الوظيفة |
|---|---|
| `deploy` | بناء + تشغيل + انتظار الصحة + ترحيل + seed (أمر واحد للنشر الكامل) |
| `up` | تشغيل الحاويات فقط |
| `down` | إيقاف + حذف الحاويات (يحافظ على الـ volumes) |
| `down-all` | ⚠️ حذف كل شيء بما فيه الـ volumes (بيانات القاعدة تُمحى) |
| `restart` | إعادة تشغيل الحاويات |
| `rebuild` | إعادة بناء بدون cache + تشغيل (بعد تعديل الكود) |
| `logs [svc]` | متابعة السجلات (كل الخدمات أو خدمة محددة) |
| `status` | عرض حالة الحاويات + فحص الصحة |
| `migrate` | `alembic upgrade head` (مع fallback إلى `stamp head`) |
| `seed` | زرع المشرف + 13 منطقة سعودية + بطاقات + منشآت تجريبية |
| `reset-db` | ⚠️ حذف volume القاعدة + إعادة زرع كاملة |
| `backup [f]` | نسخة احتياطية `pg_dump` مضغوطة |
| `psql` | دخول `psql` داخل حاوية القاعدة |
| `shell [svc]` | غلاف `bash` داخل أي حاوية |
| `cmd <c>` | تنفيذ أمر داخل حاوية backend |
| `health` | فحص `/health` عبر `curl` |
| `rotate-secrets` | توليد أسرار جديدة (SECRET_KEY + ADMIN_PASSWORD + DB_PASSWORD) |

### 2.3 `Dockerfile` (مُحدّث للإنتاج)
- **Non-root user:** `wafir` (UID 1000) — أمان أفضل
- **APP_ENV=production** بشكل افتراضي
- **Healthcheck** مع `start-period=40s` (وقت كافٍ لبدء Uvicorn)
- **4 workers** لـ Uvicorn (استفادة من multi-core)

### 2.4 `.env.production` (قالب الإعدادات الإنتاجية)
- كل المتغيرات الـ 26+ في ملف واحد
- تعليقات عربية واضحة لكل قسم
- `CORS_ORIGINS` محدد بالنطاقات الفعلية (لا wildcard)
- `CELERY_EAGER=false` (مهام خلفية حقيقية)
- تعليمات توليد `SECRET_KEY`

### 2.5 `.env.docker` (إعدادات Docker)
- المنافذ الخارجية القابلة للتغيير
- بيانات اعتماد PostgreSQL

---

## 3. تحسينات الباك‑إند للإنتاج

### 3.1 PostgreSQL حقيقي (ليس SQLite)
- `DATABASE_URL` يُحقن من `docker-compose.yml` كـ `postgresql+asyncpg://`
- `config.py` يحوّل تلقائياً `postgresql://` → `postgresql+asyncpg://`
- `database.py` يستخدم connection pool (`pool_size=20`, `max_overflow=10`, `pool_pre_ping=True`)
- `alembic/env.py` يحوّل `postgresql+asyncpg` → `postgresql+psycopg2` للترحيلات المتزامنة
- `psycopg2-binary` موجود في التبعيات

### 3.2 Celery Worker + Beat
- **Worker:** يستهلك المهام (البريد + SMS + التنظيف)
- **Beat:** يجدول مهمة `cleanup_expired_refresh_tokens` يومياً الساعة 3:00 صباحاً (توقيت الرياض)
- `celery_app.py` مُحدّث: استيراد `cleanup_tasks` + `beat_schedule` + `crontab`
- `CELERY_BROKER_URL` + `CELERY_RESULT_BACKEND` منفصلان عن `REDIS_URL` (DBs مختلفة)

### 3.3 init_db مُحدّث
- يستورد **كل** النماذج الجديدة: `favorite`, `review`, `refresh_token`
- `create_all(checkfirst=True)` آمن حتى مع وجود الجداول

### 3.4 migrations/env.py مُحدّث
- يستورد صراحة كل النماذج للتأكد من تسجيلها في `Base.metadata`
- يضمن أن `alembic upgrade head` ينشئ كل الجداول (7 migrations)

### 3.5 Health endpoint مُحسّن
- يرجع `version: "1.0.0"` بالإضافة إلى `db` و `cache_memory`

---

## 4. خطوات النشر على السيرفر

### الخطوة 1: رفع الملفات
```bash
# على جهازك المحلي:
scp -r waffr_extracted/backend/ root@169.58.114.92:/root/wafir2/

# أو: ارفع الملف المضغوط وفكّه على السيرفر
scp wafir-final.zip root@169.58.114.92:/root/
ssh root@169.58.114.92
unzip wafir-final.zip
cd waffr_extracted/backend
```

### الخطوة 2: إعداد الأسرار
```bash
cd /root/waffr_extracted/backend  # (أو المسار الذي رفعت فيه المشروع)

# توليد أسرار جديدة
./manage.sh rotate-secrets

# نسخ القيم المولّدة إلى .env.production
nano .env.production
# حدّث: SECRET_KEY, ADMIN_PASSWORD, DB_PASSWORD
```

### الخطوة 3: النشر بأمر واحد
```bash
./manage.sh deploy
```

هذا الأمر ينفّذ تلقائياً:
1. ✅ بناء الصور (`docker compose build --pull`)
2. ✅ تشغيل الحاويات (`docker compose up -d`)
3. ✅ انتظار صحة كل حاوية (DB → Redis → Backend → Celery)
4. ✅ تنفيذ الترحيلات (`alembic upgrade head`)
5. ✅ زرع المشرف + 13 منطقة سعودية + بطاقات + منشآت
6. ✅ عرض حالة الحاويات + فحص `/health`

### الخطوة 4: التحقق
```bash
./manage.sh status
./manage.sh health
```

### الخطوة 5: ربط Nginx/Caddy (اختياري)
```nginx
# /etc/nginx/sites-available/wafir2
server {
    listen 80;
    server_name api2.wafir.gleeze.com;  # أو نطاقك

    location / {
        proxy_pass http://127.0.0.1:3006;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## 5. الأوامر اليومية المفيدة

```bash
./manage.sh logs              # متابعة كل السجلات
./manage.sh logs wafir2-backend  # سجلات Backend فقط
./manage.sh status            # حالة الحاويات + الصحة
./manage.sh backup            # نسخة احتياطية تلقائية
./manage.sh psql              # دخول قاعدة البيانات
./manage.sh shell             # bash داخل Backend
./manage.sh rebuild           # بعد تعديل الكود: إعادة بناء + تشغيل
./manage.sh down              # إيقاف مؤقت (البيانات محفوظة)
./manage.sh up                # إعادة التشغيل
```

---

## 6. إعداد الواجهة الأمامية (Frontend)

الواجهة منشورة على Vercel. حدّث `next.config.ts` للإشارة إلى الـ API الجديد:

```typescript
// frontend/next.config.ts
async rewrites() {
  return [
    {
      source: '/api/:path*',
      destination: 'https://api2.wafir.gleeze.com/api/v1/:path*',
      // أو: destination: 'http://169.58.114.92:3006/api/v1/:path*',
    },
  ];
}
```

ثم اضبط CORS في `.env.production`:
```
CORS_ORIGINS=https://wafir.gleeze.com,https://facility.wafir.gleeze.com,https://admin.wafir.gleeze.com
```

---

## 7. الأمان

| الجانب | الحالة |
|---|---|
| Non-root user في Docker | ✅ `wafir` (UID 1000) |
| CORS محدد بالنطاقات | ✅ لا wildcard |
| SECRET_KEY قابل للتدوير | ✅ `manage.sh rotate-secrets` |
| أسرار في `.env.production` | ✅ (لا يُرفع لـ Git) |
| JWT مع DB-stored jti | ✅ تدوير + إلغاء |
| Logout-all-devices | ✅ |
| Rate limiting | ✅ 120 req/min/IP |
| bcrypt password hashing | ✅ |
| HTTPS (عبر Nginx/Caddy) | ⏳ تحتاج إعداد SSL |

---

## 8. استكشاف الأخطاء

### المشكلة: فشل `alembic upgrade head`
```bash
./manage.sh migrate
# إذا فشل: alembic stamp head (يختم القاعدة كأنها على آخر ترحيلة)
```

### المشكلة: Celery worker غير سليم
```bash
./manage.sh logs wafir2-celery-worker
./manage.sh restart
```

### المشكلة: لا يمكن الوصول للـ API
```bash
./manage.sh health
./manage.sh status
# تأكد أن المنفذ 3006 مفتوح في الجدار الناري:
ufw allow 3006
```

### المشكلة: إعادة زراعة القاعدة من الصفر
```bash
./manage.sh reset-db
# ⚠️ يحذف كل البيانات ويعيد الزراعة
```

---

**نهاية التقرير.**
