# دليل تغليف تطبيقي «وفر» ونشرها على المتاجر

> هذا الدليل هو الخطوة الثانية بعد النشر (راجع `دليل_النشر_PWA.md` أولاً). يأخذ بيدك من موقع يعمل على الإنترنت إلى **تطبيقين مثبتين من متاجر التطبيقات**: تطبيق العميل «وفر — بطاقة الخصومات» وتطبيق المالك «وفر — بوابة المنشآت».
>
> **ترتيب الأولويات الموصى به:** Google Play أولاً ← Huawei AppGallery ثانياً ← Apple App Store أخيراً وعلى مهل.

---

## بطاقة الحقائق قبل البدء

| | تطبيق العميل | تطبيق المالك |
|---|---|---|
| الرابط المُغلَّف | `https://wafir.gleeze.com` | `https://facility.wafir.gleeze.com` |
| **Package ID** | `sa.gleeze.wafir` | `sa.gleeze.wafir.owner` |
| **App name** | وفر — بطاقة الخصومات | وفر — بوابة المنشآت |
| الإصدار (Version) | `1.0.0` | `1.0.0` |
| رقم الإصدار (Version Code) | `1` | `1` |
| الاسم المختصر على الجهاز | وفر | وفر مالك |
| الأيقونة | تاغ ذهبي مبتسم + كلمة «وفر» | حلقة ذهبية + تاغ + حرف «و» |

> ⚠️ **انتبه:** الـ Package ID للتطبيقين **مختلف إلزامياً** — لا يجوز أبداً استخدام `sa.gleeze.wafir` للتطبيقين معاً، وإلا رفضت المتاجر الحزمة الثانية. الـ Package ID **لا يمكن تغييره بعد النشر** على Google Play، فاكتبه بحروفه الإنجليزية تماماً كما في الجدول أعلاه.

### المتطلبات قبل البدء

- [ ] الموقع منشور ويعمل على `https://wafir.gleeze.com` و`https://facility.wafir.gleeze.com` (دليل النشر)
- [ ] جهاز أندرويد بين يديك لتجربة الحزم قبل الرفع للمتاجر
- [ ] بريد إلكتروني لإنشاء حسابات المتاجر

---

## القسم أ) المفاهيم بإيجاز — افهمها في 5 دقائق

### 1. ما هو PWA وService Worker ولماذا اخترناه؟

- **PWA (تطبيق الويب التقدمي):** موقع ويب مبني بطريقة تجعله يتصرف كتطبيق حقيقي: أيقونة على الشاشة الرئيسية، فتح بملء الشاشة بلا شريط عنوان، وعمل بلا اتصال.
- **Service Worker:** «موظف خفي» يعمل داخل المتصفح/التطبيق، وظيفته تخزين الملفات والبيانات لعرضها فوراً وبلا اتصال، وسحب الجديد منها في الخلفية.
- **لماذا اخترناه؟** لأن التحديث **فوري**: أي تعديل على المنشآت والمنتجات والعروض يصل للمستخدمين مباشرة من الويب **بلا أي مراجعة من متجر التطبيقات** — بينما التطبيقات الأصلية التقليدية تنتظر موافقة المتجر أياماً لكل تعديل.

> حالة مشروعنا موثقة بالفحص: تطبيقا وفر حصلا على **Lighthouse PWA 100/100** للتطبيقين (فحص الإصدار 9.6.8) — أي أن كل شروط التثبيت والتغليف متحققة.

### 2. ما هو PWABuilder؟

أداة **مجانية من Microsoft** على الرابط:

```
https://www.pwabuilder.com
```

دورها على خطوتين:

1. **تفحص** جاهزية الـ PWA الخاص بك (هل الـ Manifest سليم؟ هل الـ Service Worker يعمل؟ هل التطبيق قابل للتثبيت؟).
2. **تولّد** حزم `APK` و`AAB` جاهزة لرفعها على Google Play وHuawei — بلا كتابة سطر كود واحد.

### 3. ما هو TWA وassetlinks.json؟

- **TWA (Trusted Web Activity):** التقنية التي تجعل حزمة أندرويد المولّدة تفتح موقعك **بملء الشاشة بلا شريط عنوان** — يبدو المستخدم في تطبيق أصلي وهو في الحقيقة داخل موقعك المؤمّن.
- **assetlinks.json:** ملف «عقد الثقة» بين التطبيق والموقع. يوضع في الموقع ويحتوي **بصمة SHA-256 لمفتاح توقيعك**، وبه تثق جوجل أن هذا التطبيق مرخّص بفتح هذا الموقع بلا شريط عنوان.
  - المشروع يحتوي الملف جاهزاً (حالياً **مصفوفة فارغة** بانتظار بصمتك) على المسار:

    ```
    public/.well-known/assetlinks.json
    ```

| المصطلح | بمعنى بسيط |
|---|---|
| PWA | موقع يتصرف كتطبيق |
| Service Worker | طبقة كاش ذكية تعمل في الخلفية |
| PWABuilder | أداة Microsoft المجانية لتوليد الحزم |
| TWA | تقنية فتح الموقع كتطبيق بملء الشاشة |
| assetlinks.json | ملف ربط بصمة التوقيع بين التطبيق والموقع |
| APK | ملف تثبيت أندرويد (للتوزيع المباشر وHuawei) |
| AAB | حزمة متجر Google Play **حصراً** |
| Keystore | «خزنة» مفتاح التوقيع الرقمي للتطبيق |
| SHA-256 | بصمة المفتاح (سلسلة أحرف بنقطتين) |

---

## القسم ب) خطوات التغليف — للتطبيقين

### الخطوة 1: افتح PWABuilder

افتح في المتصفح:

```
https://www.pwabuilder.com
```

### الخطوة 2: افحص تطبيق العميل

1. الصق رابط تطبيق العميل في حقل الفحص (تأكد أنه الرابط الإنتاجي النهائي وليس رابط معاينة):

   ```
   https://wafir.gleeze.com
   ```

2. اضغط زر **Start**.
3. انتظر انتهاء الفحص — **يجب أن تكون كل الفحوصات خضراء**:

   | الفحص | معناه |
   |---|---|
   | Manifest | ملف تعريف التطبيق (الاسم، الأيقونات، الألوان) سليم ومكتمل |
   | Service Worker | العامل الخفي يستجيب ويعمل |
   | Installability | التطبيق قابل للتثبيت (يشمل وجود أيقونات 192/512) |

> مشروعنا مُختبَر مسبقاً بنتيجة 100/100، فالفحوصات **ينبغي أن تكون خضراء من أول مرة**. إن ظهر فحص أحمر، راجع الجدول التالي:

**ماذا تفعل لو رأيت فحصاً أحمر؟**

| الفحص الأحمر | ماذا يعني بالضبط؟ | أين يُصلح في المشروع؟ |
|---|---|---|
| **Manifest** | ملف التعريف لا يستجيب أو فيه خلل | افتح في المتصفح: `https://wafir.gleeze.com/manifest.webmanifest` — يجب أن يظهر JSON سليم يبدأ بـ `{"id":...`. إن ظهر خطأ فتأكد أن آخر نشر على Vercel نجح. المصدر في المشروع: `src/app/manifest.webmanifest/route.ts` |
| **Service Worker** | العامل لا يستجيب | افتح في المتصفح: `https://wafir.gleeze.com/sw.js` — يجب أن يظهر كود جافاسكربت. المصدر في المشروع: `src/app/sw.js/route.ts` و`src/lib/pwa/sw-source.ts` |
| **Installability** | غالباً نقص أيقونات | تأكد من وجود الملفات في مجلد `public/icons/`: بالذات `icon-192.png` و`icon-512.png` (موجودة فعلاً في المشروع). وإن ظل أحمر فامسح كاش المتصفح وأعد الفحص |

### الخطوة 3: ولّد حزمة الأندرويد لتطبيق العميل

1. اضغط زر **Package for stores**.
2. اختر **Android**.
3. املأ الحقول **بالضبط** كما يلي:

   | الحقل | القيمة |
   |---|---|
   | Package ID | `sa.gleeze.wafir` |
   | App name | `وفر — بطاقة الخصومات` |
   | Version | `1.0.0` |
   | Version Code | `1` |
   | Signing key | استخدم خيار PWABuilder لتوليد/إدخال المفتاح (الشرح الكامل في **القسم ج** أدناه — **اقرأه قبل هذه الخطوة**) |

### الخطوة 4: حمّل الحزمتين

بعد اكتمال التوليد، نزّل **الحزمتين معاً**:

| الحزمة | استخدامها |
|---|---|
| **APK** | للتوزيع المباشر والاختبار (ترسلها واتساب/بريد وتُثبَّت فوراً على أي جهاز أندرويد) |
| **AAB** | **لرفعها على Google Play حصراً** — لا تُثبَّت مباشرة على الأجهزة |

> جرّب الـ APK فوراً على جهازك (ثبّته وافتحه — راجع قائمة التحقق النهائية في القسم هـ).

### الخطوة 5: كرر نفس الخطوات لتطبيق المالك

1. ارجع لصفحة البداية والصق رابط بوابة المنشآت:

   ```
   https://facility.wafir.gleeze.com
   ```

2. اضغط **Start** — يجب أن تكون الفحوصات خضراء أيضاً (نفس آلية الإصلاح أعلاه إن ظهر أحمر، لكن على رابط المالك).
3. اضغط **Package for stores** → اختر **Android** → املأ **بالقيم المختلفة للتطبيق الثاني**:

   | الحقل | القيمة |
   |---|---|
   | Package ID | `sa.gleeze.wafir.owner` ⚠️ **إلزامي مختلف عن العميل** |
   | App name | `وفر — بوابة المنشآت` |
   | Version | `1.0.0` |
   | Version Code | `1` |

4. نزّل حزمتي **APK** و**AAB** للمالك أيضاً.

> **النتيجة النهائية لهذا القسم:** 4 ملفات — `APK` و`AAB` للعميل + `APK` و`AAB` للمالك.

---

## القسم ج) مفتاح التوقيع — أهم قسم في الدليل كله

### لماذا هذا القسم أهم من غيره؟

مفتاح التوقيع هو **هوية تطبيقك لدى Google Play للأبد**. Google Play لا يقبل تحديثاً لتطبيقك إلا إذا كان موقّعاً **بنفس المفتاح الذي وُقّع به أول إصدار**. فقدان المفتاح = استحالة تحديث التطبيق نهائياً (التطبيق القديم يبقى، وعليك إنشاء تطبيق جديد باسم مختلف).

### لك خياران لتوليد المفتاح

| الخيار | الطريقة | لمن؟ |
|---|---|---|
| **1) واجهة PWABuilder** | أثناء خطوة التغليف (الخطوة 3 في القسم ب) اختر توليد مفتاح من الواجهة، ثم **نزّل ملف المفتاح واحفظه** | الأسهل — موصى به للمشرف غير التقني |
| **2) أمر keytool بنفسك** | تولّده بنفسك على جهازك (يتطلب تثبيت Java/JDK) | لمن يريده مُداراً خارج أي أداة |

> **توصية:** استخدم مفتاحاً واحداً (ملف keystore واحداً) **للتطبيقين معاً** — أبسط للإدارة، وسنستخدم نفس البصمة للاثنين في ملف الرباط.

### توليد المفتاح يدوياً بأمر keytool (الخيار 2)

افتح موجه الأوامر (Terminal على ماك/لينكس، أو cmd على ويندوز) في مجلد تحفظ فيه ملفات مهمة، وانسخ الأمر التالي كاملاً:

```bash
keytool -genkeypair -v -keystore wafir-release.keystore -alias wafir -keyalg RSA -keysize 2048 -validity 10000
```

**تفسير كل جزء من الأمر:**

| الجزء | معناه |
|---|---|
| `-genkeypair` | توليد زوج مفاتيح جديد (خاص + عام) |
| `-v` | عرض التفاصيل أثناء العمل |
| `-keystore wafir-release.keystore` | اسم ملف «الخزنة» الذي سيُحفظ فيه المفتاح |
| `-alias wafir` | الاسم المستعار للمفتاح داخل الخزنة (لتعرفه لاحقاً) |
| `-keyalg RSA` | نوع خوارزمية التشفير (المعيار المطلوب للمتاجر) |
| `-keysize 2048` | حجم المفتاح بالبت (2048 هو المعيار الآمن) |
| `-validity 10000` | مدة صلاحية المفتاح بالأيام (أكثر من 27 سنة — يغطي عمر التطبيق المتوقع) |

- سيطلب منك **كلمة مرور** للخزنة — اختر كلمة قوية **واكتبها في ورقة منفصلة واحفظها**.
- ثم سيطرح أسئلة (اسمك، مؤسستك، مدينتك…) — يمكنك الضغط Enter لتجاوزها، ثم اكتب `نعم` (أو `yes`) عند التأكيد النهائي.
- النتيجة: ملف اسمه `wafir-release.keystore` في المجلد الحالي.

> **إن اخترت الخيار 1 (واجهة PWABuilder):** نزّل ملف المفتاح الذي تولّده الأداة واحفظه بنفس الأهمية — كل ما يلي (استخراج البصمة والربط) ينطبق عليه تماماً.

### ⛔ تحذير — اقرأه مرتين

> **فقدان مفتاح التوقيع = استحالة تحديث التطبيق على Google Play للأبد.**
>
> احفظ الملف `wafir-release.keystore` **وكلمة مروره** في **3 نسخ آمنة منفصلة**:
>
> 1. نسخة في بريد إلكتروني **مشفّر** (ملف مضغوط بكلمة مرور تُرسل عبر قناة مختلفة عن كلمة المرور نفسها)
> 2. نسخة في وحدة تخزين سحابية (Google Drive / Dropbox بحسابك الخاص)
> 3. نسخة محلية على وحدة USB خارجية
>
> **لا تشارك الملف أو كلمة المرور مع أي أحد** — من يملكهما يستطيع نشر تحديثات باسم تطبيقك.

### استخراج بصمة SHA-256 من المفتاح

هذه البصمة هي التي ستُربط بالموقع. انسخ الأمر التالي (من نفس المجلد الذي فيه ملف الخزنة):

```bash
keytool -list -v -keystore wafir-release.keystore -alias wafir | grep SHA256
```

- سيطلب كلمة مرور الخزنة — أدخلها.
- ستظهر سلسلة بهذا الشكل (مثال توضيحي فقط — بصمتك ستختلف):

  ```
  SHA256: AA:BB:CC:DD:EE:FF:00:11:22:33:44:55:66:77:88:99:AA:BB:CC:DD:EE:FF:00:11:22:33:44:55:66:77:88:99
  ```

- **انسخ السلسلة كاملة** (من أول `AA` إلى آخر حرفين، شاملةً النقطتين `:`).

> ملاحظة ويندوز: إن ظهر خطأ بأن `grep` غير معروف، احذف الجزء `| grep SHA256` من الأمر وشغّله — سيظهر ناتج طويل، ابحث فيه بسطر يبدأ بـ `SHA256:` وانسخ ما بعده.

### خطوات الربط بين التطبيقين والموقع (6 خطوات)

**الخطوة 1:** انسخ بصمة SHA-256 الناتجة من الأمر أعلاه.

**الخطوة 2:** افتح في المشروع الملف التالي (المسار بالضبط):

```
public/.well-known/assetlinks.json
```

محتواه الحالي مصفوفة فارغة:

```json
[]
```

استبدلها بالشكل النهائي التالي — **بعنصرين: عنصر لكل تطبيق** — بعد وضع بصمتك مكان النقاط:

```json
[
  {
    "relation": ["delegate_permission/common.handle_all_urls"],
    "target": {
      "namespace": "android_app",
      "package_name": "sa.gleeze.wafir",
      "sha256_cert_fingerprints": ["AA:BB:CC:...(ضع بصمتك هنا)"]
    }
  },
  {
    "relation": ["delegate_permission/common.handle_all_urls"],
    "target": {
      "namespace": "android_app",
      "package_name": "sa.gleeze.wafir.owner",
      "sha256_cert_fingerprints": ["AA:BB:CC:...(نفس البصمة)"]
    }
  }
]
```

> **لماذا عنصران في نفس الملف؟** لأن ملف الرباط يُخدم من نطاقي `wafir.gleeze.com` و`facility.wafir.gleeze.com` — وكلاهما يُخدمان من **نفس النشر**، فملف واحد بعنصرين يكفي للنطاقين معاً: عنصر لتطبيق العميل (`sa.gleeze.wafir`) وعنصر لتطبيق المالك (`sa.gleeze.wafir.owner`). وبما أننا نوقّع التطبيقين بمفتاح واحد، تكون **نفس البصمة** في العنصرين.

> ملاحظة تقنية بسيطة: استخدمت أداة PWABuilder مفتاحاً مختلفاً لكل تطبيق؟ إذن ضع بصمة كل مفتاح في عنصر تطبيقه فقط.

**الخطوة 3:** ارفع التعديل إلى GitHub → **Vercel يعيد النشر تلقائياً** خلال دقائق.

**الخطوة 4:** تأكد أن الملف يُخدم فعلاً على النطاقين — افتح الرابطين في المتصفح ويجب أن يظهر محتوى الملف الذي كتبته (وليس `[]`):

```
https://wafir.gleeze.com/.well-known/assetlinks.json
```

```
https://facility.wafir.gleeze.com/.well-known/assetlinks.json
```

**الخطوة 5:** تحقق رسمياً بأداة فحص Google الرقمية (Statement List Generator):

```
https://developers.google.com/digital-asset-links/tools/generator
```

- الصق في الأداة اسم النطاق (مثل `wafir.gleeze.com`) والبصمة — يجب أن تؤكد الأداة نجاح الربط. كرر للنطاق الثاني (`facility.wafir.gleeze.com`).

**الخطوة 6:** **أعد توليد الحزم في PWABuilder الآن بنفس المفتاح** (كرر خطوات القسم ب مع إدخال مفتاحك في خطوة التوقيع).

> **لا توقّع يدوياً:** الحزم تُولَّد في PWABuilder **موقّعة بمفتاحك تلقائياً** إذا أدخلته في خطوة التوقيع — لا حاجة لاستخدام أداة apksigner أو أي توقيع يدوي إضافي.

**النتيجة المتوقعة بعد إكمال الخطوات الست:** تفتح التطبيقات المثبتة **بملء الشاشة بلا شريط عنوان** — وإلا (أي ظهر شريط عنوان) فراجع بصمة الملف مرة أخرى: أي حرف ناقص أو زائد في البصمة يفسد الربط.

---

## القسم د) خطوات كل متجر

### 1. Google Play — الأولوية الأولى ⭐

**أ. أنشئ حساب مطور Google Play:**

1. ادخل إلى:

   ```
   https://console.play.google.com
   ```

2. سجّل بحساب Google، وادفع رسوم التسجيل **25$ مرة واحدة فقط** (بطاقة ائتمانية/مدى مدفوعة مسبقاً) — الحساب مدى الحياة.

**ب. أنشئ التطبيق وارفع الحزمة:**

1. من لوحة التحكم: **Create app** → اختر اللغة العربية واكتب اسم التطبيق.
2. من قسم **Production** ارفع حزمة **AAB** (وليس APK! Google Play يقبل حصراً صيغة `AAB`):

   | التطبيق | الحزمة المرْفَعة |
   |---|---|
   | وفر — بطاقة الخصومات | حزمة AAB للعميل (Package ID: `sa.gleeze.wafir`) |
   | وفر — بوابة المنشآت | حزمة AAB للمالك (Package ID: `sa.gleeze.wafir.owner`) |

3. ستحتاج إنشاء **تطبيقين منفصلين** في لوحة Play — واحد لكل حزمة.

**ج. املأ بيانات المتجر (Store Listing):**

- **الوصف العربي — جاهز للنسخ:**

  لتطبيق العميل:

  ```
  وفر — بطاقة الخصومات الرقمية في جيبك

  بطاقة وفر تمنحك خصماً حصرياً حتى 30% في المطاعم والمقاهي والمرافق العامة المشتركة بجميع مناطق المملكة. اختر منطقتك، استعرض المتاجر، واعرض بطاقتك الرقمية عند الدفع لتوفير حقيقي في كل مرة.

  • بطاقة عضوية رقمية تعرضها من جوالك في أي منشأة مشاركة
  • تصفح المنشآت حسب منطقتك واختر ما يناسبك
  • اطّلع على منتجات كل منشأة المشمولة بالخصم قبل زيارتها
  • سجّل حسابك في دقيقة واحصل على رقم عضوية خاص بك
  • يعمل بلا اتصال: آخر البيانات التي ظهرت لك تبقى متاحة أينما كنت
  • سريع وخفيف ولا يستهلك مساحة جوالك

  حمّل وفر الآن وابدأ التوفير من أول زيارة.
  ```

  لتطبيق المالك:

  ```
  وفر — بوابة المنشآت

  بوابة أصحاب المنشآت في منصة وفر: أدر منشآتك ومنتجاتك وعروضك من جوالك، واستورد قوائمك بضغطة واحدة، وتابع كل شيء لحظة بلحظة أينما كنت. تطبيقك الرسمي لإدارة مشاركتك في بطاقة وفر.

  • تسجيل دخول آمن خاص بمنشأتك
  • عرض منشآتك وتعديل بياناتها من جوالك
  • إدارة المنتجات: إضافة وتعديل وتفعيل أو إيقاف أي منتج
  • استيراد المنتجات دفعة واحدة من ملف Excel مع تقرير نتيجة الاستيراد
  • واجهة عربية كاملة تعمل بسلاسة على الجوال

  إن كانت منشأتك مشاركة في بطاقة وفر، فهذا تطبيقك اليومي.
  ```

- **اللقطات:** ارفعها من مجلد `public/screenshots` في المشروع — كلها بمقاس 1080×1920:

  | التطبيق | الملف | المحتوى |
  |---|---|---|
  | العميل | `customer-home.png` | الصفحة الرئيسية والعروض |
  | العميل | `customer-card.png` | بطاقة العضوية الرقمية |
  | العميل | `customer-facility.png` | صفحة المنشأة ومنتجاتها |
  | المالك | `owner-login.png` | تسجيل دخول بوابة المنشآت |
  | المالك | `owner-products.png` | إدارة منتجات المنشأة |
  | المالك | `owner-import.png` | استيراد المنتجات من Excel |

- **تصنيف المحتوى:** أكمل استبيان تصنيف المحتوى (IARC) — التطبيق **تسوقي عام**، لا محتوى حساس، مناسب لكل الأعمار.
- **سياسة الخصوصية:** الصق الرابط:

  ```
  https://wafir.gleeze.com/privacy
  ```

  (صفحة جاهزة في الموقع تحدد البيانات المجموعة — الاسم/البريد/الجوال/رقم العضوية — وطريقة طلب الحذف عبر التواصل: هاتف 0547669078 / بريد s72468483@gmail.com)

**د. أرسل للمراجعة:** اضغط **Submit for review**. المدة المتوقعة: **من ساعات إلى 7 أيام**. ستصلك نتيجة المراجعة على بريدك.

**هـ. ⭐ ميزة TWA الجوهرية التي عليك فهمها:**

> بعد نشر التطبيق لأول مرة، **تحديثات المحتوى (منشآت جديدة، منتجات، أسعار، عروض) تصل للمستخدمين فوراً عبر الويب بلا أي مراجعة من المتجر** — لأن التطبيق يعرض موقعك الحي. الحزم نفسها تُحدَّث (برفع حزمة AAB جديدة) **فقط عند تغيير الأيقونة أو الاسم أو الإصدار** — أي نادراً جداً.

### 2. Huawei AppGallery — الأولوية الثانية

المتجر البديل المهم للأجهزة التي لا تحمل خدمات جوجل (هواوي/هونر — منتشرة في السعودية).

1. أنشئ حساب مطور في AppGallery Connect:

   ```
   https://developer.huawei.com
   ```

   (رسوم سنوية رمزية أو مجاني للأفراد حسب حالة حسابك — راجع صفحة التسجيل).

2. أنشئ تطبيقاً جديداً واملأ بياناته — **نفس الأصول التي جهزتها لـ Google Play**: الوصف العربي، اللقطات من `public/screenshots`، رابط سياسة الخصوصية `https://wafir.gleeze.com/privacy`، نفس الـ Package IDs.
3. **الفرق الوحيد عن Google Play:** ارفع ملف **APK** (وليس AAB) — AppGallery لا يستقبل صيغة AAB.
4. أرسل للمراجعة — الخطوات مشابهة تماماً لـ Play.

> أنشئ تطبيقين منفصلين هنا أيضاً (عميل + مالك) بنفس الطريقة.

### 3. Apple App Store — تقييم صادق، ولا تتعجّل

**الحقيقة التي يجب أن تعرفها مسبقاً:**

آبل تقيم تطبيقات الويب المغلفة بصرامة بموجب **القاعدة 4.2 (Minimum Functionality — الحد الأدنى من الوظائف)**، وترفض التطبيق إن بدا **نسخة طبق الأصل من الموقع بلا قيمة أصلية إضافية** — وهذا احتمال حقيقي ووارد مع تطبيقات TWA عموماً.

**الحل الجذري إن رُفض التطبيق:**

إعادة التغليف بتقنية **Capacitor** — غلاف WebKit **محلي داخل التطبيق** بدل TWA (فتح الويب الحي)، يمنح آبل ما تعتبره «تطبيقاً» أكثر اكتمالاً. هذا مسار تطوير إضافي يتطلب مطوراً.

**تذكّر دائماً:**

> **«لا تدع آبل تعطل إطلاق أندرويد — انشر على Google Play وHuawei أولاً وامضِ في مسار آبل على مهل».**

**إن قررت خوض مسار آبل، اعرف تكلفته مسبقاً:**

| المتطلب | التفصيل |
|---|---|
| حساب مطور Apple | **99$ سنوياً** (تجديد مستمر — بلا حساب لا يمكن النشر إطلاقاً) |
| جهاز Mac | حاسوب ماك مع برنامج Xcode — **إلزامي** لطريق Capacitor (لا يمكن البناء والتقديم من ويندوز بشكل عملي) |
| مدة المراجعة | عادة من يوم إلى أيام قليلة، والرفض وارد — خطّط لجولة أو جولتين |

> في كل الأحوال: مستخدمو الآيفون **لن يُحرموا من وفر** — التطبيق يعمل لديهم فوراً كـ PWA عبر سفاري («مشاركة ← إضافة إلى الشاشة الرئيسية») كما هو مشروح في دليل النشر. مسار App Store تحسين لاحق وليس شرطاً للانطلاق.

---

## القسم هـ) قائمة التحقق النهائية للمشرف

نفّذها على جهاز أندرويد بعد تثبيت حزمتي APK (العميل + المالك) وقبل الرفع للمتاجر — علّم على كل بند:

- [ ] **APK عميل مثبت** → يفتح standalone (بملء الشاشة) **بلا شريط عنوان** على الصفحة الرئيسية
- [ ] **APK مالك مثبت بجانبه** → أيقونة مختلفة (حلقة ذهبية) + يفتح مباشرة على بوابة المالك
- [ ] **الطيران (وضع عدم الاتصال) مفعّل** → كلا التطبيقين يفتح → العميل يعرض آخر بيانات ظهرت سابقاً
- [ ] **محاولة عملية بلا اتصال** (مثل تسجيل دخول أو حفظ تعديل) → الرسالة العربية الصحيحة تظهر:
  - في تطبيق العميل: «يتطلب هذا الإجراء اتصالاً بالإنترنت»
  - في تطبيق المالك: «تتطلب بوابة المنشآت اتصالاً بالإنترنت»
- [ ] **عودة الاتصال** → البيانات تتحدث تلقائياً + رسالة «يتوفر تحديث لتطبيق وفر» تعمل عند نشر نسخة جديدة (اختبرها: ارفع رقم الإصدار في `src/lib/pwa/version.ts` وانشر، ثم افتح التطبيق وانتظر حتى ساعة أو أعد فتحه)

**كل البنود معلَّمة؟ التطبيقان جاهزان للرفع على Google Play. 🎉**

---

## خريطة الطريق الكاملة في أسطر

1. انشر الموقع على Vercel واربط النطاقات (`دليل_النشر_PWA.md`)
2. ولّد مفتاح التوقيع واحفظه في 3 نسخ (القسم ج)
3. عبّئ التطبيقين عبر PWABuilder بمفتاحك (القسم ب)
4. اربط بصمة SHA-256 في `public/.well-known/assetlinks.json` وأعد النشر (القسم ج)
5. اختبر بقائمة التحقق (القسم هـ)
6. ارفع AAB على Google Play ← ثم APK على Huawei ← ثم قرار مدروس بشأن آبل (القسم د)
7. بعد النشر: المحتوى يتحدث فوراً عبر الويب — والحزم فقط عند تغيير الأيقونة/الاسم/الإصدار
