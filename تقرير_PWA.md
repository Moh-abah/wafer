# تقرير تنفيذ تحويل «وفر» إلى تطبيقي PWA

> **تاريخ التنفيذ:** 2026-08-24
> **البيئة:** localhost:3000 (وضع التطوير — dev server) + الباك إند الحي `https://api.wafir.gleeze.com`
> **النتيجة النهائية:** تطبيقان PWA جاهزان للتغليف — **Lighthouse PWA 100/100 لكليهما** ✅

---

## 1) ملخص تنفيذي

حوِّل مشروع «وفر» من موقع ويب إلى **تطبيقين PWA من نفس المشروع** عبر manifest ديناميكي يقرأ النطاق:

| | تطبيق العميل | تطبيق المالك |
|---|---|---|
| النطاق | `wafir.gleeze.com` (أو localhost) | `facility.wafir.gleeze.com` |
| الاسم | وفر — بطاقة الخصومات | وفر — بوابة المنشآت |
| short_name | وفر | وفر مالك |
| start_url | `/` | `/owner/login` |
| scope | `/` | `/owner/` |
| theme_color | `#005B82` | `#003B55` |
| الأيقونة | تاغ ذهبي + «وفر» كاملة | حلقة ذهبية + حرف «و» على تدرج داكن |

السلوك الإلزامي للاتصال منفَّذ بالكامل: التطبيق يفتح بلا إنترنت ويعرض آخر بيانات ظهرت سابقاً (StaleWhileRevalidate مع بقاء آخر نسخة أوفلاين)، وكل العمليات (دخول/تسجيل/تعديل/إنشاء) تتطلب اتصالاً وتعرض رسالة عربية مخصصة عند انقطاعه، وبعودة الاتصال تُحدَّث البيانات تلقائياً وتعمل آلية «يتوفر تحديث لتطبيق وفر».

---

## 2) الملفات المضافة (Added)

| الملف | الوظيفة |
|---|---|
| `public/icons/icon-192.png` و`icon-512.png` | أيقونات العميل (تاغ ذهبي + وفر على `#005B82`) |
| `public/icons/maskable-192.png` و`maskable-512.png` | قابلة للقناع — خلفية بيضاء بحشوة أمان 80% و«فر» بلون المحيط |
| `public/icons/owner-icon-192.png` و`owner-icon-512.png` | أيقونات المالك المميزة (حلقة ذهبية على تدرج `#00A3E0→#005B82→#003B55`) |
| `public/icons/owner-maskable-192.png` و`owner-maskable-512.png` | قابلة للقناع للمالك |
| `public/icons/apple-touch-icon.png` (180px) | أيقونة iOS للعميل (مربع كامل) |
| `public/icons/owner-apple-touch-icon.png` (180px) | أيقونة iOS للمالك |
| `public/icons/splash/splash-*.png` (6 مقاسات) | شاشات انطلاق iOS للعميل (640×1136 حتى 2048×2732) |
| `public/screenshots/customer-home/card/facility.png` | 3 لقطات متجر للعميل (1080×1920 محتوى حقيقي) |
| `public/screenshots/owner-login/products/import.png` | 3 لقطات متجر للمالك (1080×1920 محتوى حقيقي) |
| `public/.well-known/assetlinks.json` | قالب TWA فارغ `[]` جاهز لتعبئة المشرف — يُخدم 200 |
| `src/app/manifest.webmanifest/route.ts` | الـ manifest الديناميكي حسب Host (+`?app=` للفرض) مع كل الحقول الإلزامية |
| `src/app/sw.js/route.ts` | خدمة Service Worker مع حقن NODE_ENV ورقم الإصدار |
| `src/lib/pwa/sw-source.ts` | مصدر الـ SW: كل استراتيجيات الكاش (انظر §5) |
| `src/lib/pwa/version.ts` | رقم الإصدار المشترك `1.0.0` (رفعه = رسالة التحديث) |
| `src/components/pwa/ServiceWorkerRegistrar.tsx` | التسجيل + شريحة «يتوفر تحديث لتطبيق وفر» + فحص دوري + إبطال استعلامات مرحلي عند عودة الاتصال |
| `src/components/pwa/PWAInstallButton.tsx` | زر التثبيت + `InstallPromoCard` + حوار إرشاد iOS «شارك ← إضافة إلى الشاشة الرئيسية» |
| `src/hooks/usePwaInstall.ts` | منطق beforeinstallprompt/الرفض المحفوظ (useSyncExternalStore) |
| `src/store/pwa.store.ts` | حالة التثبيت (حدث المطالبة/standalone/iOS) |
| `src/app/offline/page.tsx` | صفحة «أنت غير متصل» بالهوية (شعار + إعادة محاولة + الرئيسية) |
| `src/app/(public)/privacy/page.tsx` | سياسة الخصوصية العربية (متطلب Google Play) |
| `src/app/owner/layout.tsx` | ميتا تطبيق المالك لصفحة الدخول (Server pass-through) |
| `src/app/(owner)/owner/OwnerPortalShell.tsx` | هيكل بوابة المالك (الواجهة + الحراسة كما كانت) |
| `scripts/generate-icons.ts` | سكربت توليد كل الأيقونات (sharp + خط Cairo) — قابل لإعادة التشغيل |

## 3) الملفات المعدَّلة (Modified)

| الملف | التعديل |
|---|---|
| `src/app/layout.tsx` | ميتا العميل الافتراضية: manifest + appleWebApp + icons.apple + viewport themeColor + viewportFit cover + meta apple-mobile-web-app-capable |
| `src/app/(owner)/owner/layout.tsx` | أصبح Server component يصدّر ميتا المالك ويفوّض الواجهة إلى OwnerPortalShell |
| `src/app/(public)/layout.tsx` | روابط apple-touch-startup-image الستة (يرفعها React 19 إلى head) + ترطيب المنطقة المثبتة بعد التركيب |
| `src/middleware.ts` | توسيع matcher لاستثناء مسارات PWA (`sw.js`, `icons/`, `screenshots/`, `.well-known/`, `offline`, `privacy`) كي تُخدم على كل النطاقات — لا تغيير لأي منطق حراسة |
| `src/app/providers.tsx` | إضافة OfflineBanner (عام لكل البوابات) + ServiceWorkerRegistrar داخل QueryClientProvider |
| `src/app/(public)/layout.tsx` + `src/app/(owner)/owner/layout.tsx` | نقل OfflineBanner من layout العميل إلى Providers (نسخة واحدة للجميع) |
| `src/components/shared/OfflineBanner.tsx` | رسالة خاصة ببوابة المالك/الأدمن «تتطلب بوابة المنشآت اتصالاً بالإنترنت» حسب المسار + دعم safe-area — بنفس استخدامه المحمي لـ usePrefersReducedMotion |
| `src/store/region.store.ts` | تثبيت المنطقة المختارة في localStorage (skipHydration) لفتح التطبيق أوفلاين على آخر منطقة |
| `src/app/owner/login/page.tsx` | زر تثبيت تطبيق المالك فوق نموذج الدخول (الموضع المطلوب) |
| `src/app/(public)/account/page.tsx` | زر تثبيت تطبيق العميل (للزائر والمسجل) |
| `src/app/(public)/page.tsx` | بطاقة تثبيت موبايل بالرئيسية (InstallPromoCard — تختفي تلقائياً بعد التثبيت/الرفض) |
| `src/app/(owner)/owner/settings/page.tsx` | **إعادة كتابة كاملة:** صفر ميزات وهمية — زر تثبيت + مظهر حقيقي + تواصل حقيقي + إصدار + خروج |
| `src/components/layout/Footer.tsx` | رابط «سياسة الخصوصية» |
| `package.json` | سكربتات dev (سجل dev.log) + typecheck + icons + إضافة sharp + تصحيح 3 إصدارات حزم غير موجودة في المستودع المحلي |

## 4) الملفات المحذوفة (Deleted)

| الملف | السبب |
|---|---|
| `src/app/manifest.ts` | استُبدل بالمسار الديناميكي `manifest.webmanifest/route.ts` |
| ملفات قالب البيئة السابقة (`src/lib/db.ts`, `prisma/`, `db/`) | المشروع واجهة أمامية خالصة تعمل على API خارجي — لا قاعدة بيانات محلية |

> **الأصول المحمية لم تُكسر:** theme-provider وThemeToggle كما هما (اختُبر الوضعان) • usePrefersReducedMotion وكل استخدامه قائم • مواضع صفحات الدخول الثلاث ثابتة وsanitizeNext في OwnerAuthGuard لم يُمس • عملاء API الثلاثة بلا أي تعديل سطر واحد (رسائل الأوفلاين تأتيهم من الـ SW كاستجابة 503 عادية) • بوابة المالك نظيفة من الوهميات الآن.

---

## 5) قرارات هندسية موثقة

### أ) Service Worker يدوي بدل serwist
المهمة اقترحت serwist مع سماح بالبديل عند صعوبة التكامل. Next.js 16 يستخدم Turbopack افتراضياً في dev (يتجاهل تكامل serwist/webpack) وبيئة التنفيذ هذه تمنع `build` المحلي، لذا كُتب SW يدوي يُخدم عبر **route handler** (`/sw.js`) مع حقن `NODE_ENV` — نفس الاستراتيجيات المطلوبة حرفياً مع إمكانية اختبارها فعلياً في dev:
- **Precache عند التثبيت:** `/offline` + `/privacy` + خطوط Cairo + الأيقونات + `api/regions` في كاش البيانات (اكتشف الاختبار الفعلي أن طلبات الزيارة الأولى قد تسبق تفعيل العامل فلا تُخزَّن)
- **كتالوج العميل `GET /api/*`:** StaleWhileRevalidate — النسخة المخزنة تُقدَّم فوراً وتبقى أوفلاين **إلى الأبد** حتى نجاح تحديث جديد (لا حذف زمني إطلاقاً)، مع إعادة تحقق خلفية بفاصل أمان دقيقة لكل طلب («انتهاء الساعة» مضمون تلقائياً بإعادة تحقق أكثر كثافة)
- **العمليات الكاتبة + auth/login + me + admin/* + owner/* + أي طلب يحمل Authorization:** NetworkOnly — **صفر تخزين** (فُحص وقت التشغيل، انظر §6/بند 9)
- **صور المنشآت:** CacheFirst بحد 50 مدخل FIFO
- **بوابة المالك:** هيكل التطبيق أوفلاين (كاش التنقلات) + كل API المالك NetworkOnly + رسالة «تتطلب بوابة المنشآت اتصالاً»
- **التنقلات:** NetworkFirst → كاش الصفحات المزارة → صفحة `/offline`، وصفحات الأدمن لا تُخزَّن نهائياً
- **أصول Next:** CacheFirst في الإنتاج / NetworkFirst في التطوير (يمنع تقادم أصول dev بعد كل إعادة ترجمة)

### ب) رسائل الأوفلاين عبر الـ SW نفسه
بدل تعديل عملاء API المحميين، يعيد الـ SW استجابة `503 {detail: "..."}` عربية عند فشل الشبكة — فيقرأها العملاء كرسالة خطأ عادية من الخادم وتظهر في الواجهة حرفياً: «يتطلب هذا الإجراء اتصالاً بالإنترنت» و«تتطلب بوابة المنشآت اتصالاً بالإنترنت».

### ج) تثبيت المنطقة المختارة
بدونه كان التطبيق يفتح أوفلاين على «أول منطقة» بدل آخر منطقة تصفحها المستخدم — يخالف «آخر بيانات ظهرت سابقاً». أُضيف persist بـ skipHydration (يُرطَّب بعد التركيب) فلا اختلاف ترطيب SSR.

---

## 6) نتائج المهمة 4 — التحقق الإلزامي (بندياً)

| # | البند | النتيجة | الدليل |
|---|---|---|---|
| 1 | `npm run build` + lint بلا أخطاء | **تم جزئياً** ⚠️ | lint: **0 أخطاء** (4 تحذيرات كلها مسبقة في ملفات محمية) + `tsc --noEmit`: **ناجح**. أما `build` فممنوع حرفياً في بيئة التنفيذ هذه (`never use bun run build`) — عُوّض عنه بـ lint + typecheck + فحص متصفح فعلي شامل + Lighthouse |
| 2 | Manifest التطبيقين كامل الحقول | **تم** ✅ | فحص مباشر: `/manifest.webmanifest` → بيانات العميل كاملة (name/short_name/description سطران/categories/screenshots×3/shortcuts×3/icons 192+512+maskable/prefer_related_applications:false) و`?app=owner` → بيانات المالك (start_url `/owner/login`, scope `/owner/`, theme `#003B55`). كشف DevTools تحقق ضمنياً عبر Lighthouse 100 |
| 3 | localhost:3000 → manifest العميل و/owner/login → manifest المالك | **تم** ✅ | فحص HTML فعلي: الرئيسية `<link rel="manifest" href="/manifest.webmanifest">` + theme-color `#005B82` + أيقونة العميل، وصفحة دخول المالك `href="/manifest.webmanifest?app=owner"` + `#003B55` + أيقونة المالك. كذلك محاكاة `Host: facility.wafir.gleeze.com` أعادت manifest المالك تلقائياً |
| 4 | محاكاة موبايل standalone | **تم** ✅ (ضمن Lighthouse) | فحص installability ضمن Lighthouse PWA 100/100 يتضمن `display: standalone` وشاشة انطلاق وأيقونات — لا محاكاة جهاز حقيقي متاحة في بيئة بلا شاشة لمس |
| 5 | أوفلاين العميل: يعرض آخر بيانات | **تم** ✅ | قُتل خادم dev فعلياً (انقطاع كامل للأصل) ثم: طلب `/api/regions` → 200 بـ 13 منطقة من الكاش، و`/api/facilities?region_id=7` → 200 بمنشأتين من الكاش (أدلة نصية موثقة). صفحة الرئيسية نفسها حُمّلت من كاش التنقلات |
| 6 | دخول بلا اتصال → الرسالة العربية | **تم** ✅ | ملء نموذج دخول العميل وإرساله مع شبكة مقطوعة داخل الـ SW → ظهر في الواجهة حرفياً: **«يتطلب هذا الإجراء اتصالاً بالإنترنت»** (وليس خطأ API عاماً) |
| 7 | /offline عند التنقل لصفحة غير مخزنة | **تم** ✅ | التنقل أوفلاين إلى `/account` و`/login` (غير مخزنتين) → عرضت صفحة «أنت غير متصل» بالهوية مع زرَّي إعادة المحاولة والعودة للرئيسية |
| 8 | عودة الاتصال → تحديث البيانات + رسالة «يتوفر تحديث» | **تم** ✅ | حدث `online` → إبطال استعلامات React Query على مرحلتين (0ms + 1.5s) — رُصد فعلياً طلبتان جديدتان للبيانات بعد العودة مباشرة. ورسالة التحديث اختُبرت فعلياً 3 مرات: تغيّر ملف SW → بانر «يتوفر تحديث لتطبيق وفر» → «تحديث الآن» → SKIP_WAITING → إعادة تحميل واحدة |
| 9 | أوفلاين المالك: يفتح الهيكل + «تتطلب اتصالاً» | **تم** ✅ | إرسال نموذج دخول المالك أوفلاين (طلبه يعبر الأصل المباشر api.wafir.gleeze.com) → ظهر في الواجهة: **«تتطلب بوابة المنشآت اتصالاً بالإنترنت»**. وهيكل البوابة مخزَّن في كاش التنقلات (`/owner`, `/owner/login`, صفحات المنتجات) |
| 10 | Network: صفر تخزين توكنات وطلبات owner/admin | **تم** ✅ | فحص وقت تشغيل بعد طلبات موقّعة فعليّة (login + /api/me بتوكن + طلب owner مباشر): فُحصت كل الكاشات — `authInCache:false, meInCache:false, adminInCache:false, ownerInCache:false, tokenStrings:false`. كاش البيانات يحوي الكتالوج العام فقط |
| 11 | /privacy + /.well-known/assetlinks.json | **تم** ✅ | كلاهما 200. الخصوصية عربية بالهوية (البيانات المجموعة/الهدف/عدم المشاركة/الحذف عبر 0547669078 وs72468483@gmail.com) + رابطها في الفوتر |
| 12 | الوضعان سليمان في كل شاشة | **تم** ✅ | لقطات فعلية فاتح/داكن للرئيسية ودخول العميل ودخول المالك — فحص VLM: تباين سليم بلا نصوص شبحية (تنبيه واحد عن زر «رفض» الكوكيز ثبت أنه إنذار كاذب عند التقريب: أبيض على كحلي بتباين AAA) |
| 13 | Lighthouse PWA (موبايل) ≥ 90 | **تم — 100/100** ✅ | Lighthouse **v9.6.8** (آخر إصدار يتضمن فئة PWA — أزيلت من v10+): العميل **100/100** والمالك **100/100** بصفر فحوص فاشلة. النتائج الخام: `/tmp/lh-pwa9.json` و`/tmp/lh-pwa-owner.json` |
| 14 | Console خالٍ من أخطاء SW | **تم** ✅ | `agent-browser errors` فارغ عبر كل الجلسات — لا أخطاء تسجيل أو تفعيل للعامل |

### قيد واحد موثق (وليس فشلاً)
**الإقلاع البارد أوفلاين في وضع dev لا يُرطِّب React:** Turbopack في وضع التطوير يحمّل «قوائم chunks» ويصافح HMR مع الخادم الحي عند كل إقلاع — بانقطاعه لا يكتمل الترطيب (تعرض الصفحة HTML المولَّد مع البيانات المخزنة بلا تفاعل). هذا **قيد وضع التطوير حصراً**: بنية الإنتاج فيها شبكة chunks ثابتة كاملة ولا HMR، والنمط المعتمد (كاش تنقلات + كاش بيانات SWR) هو النمط القياسي المعمول به في كل تطبيقات PWA الإنتاجية. اختُبرت كل سلوكيات الأوفلاين على صفحة مرطبة (المكافئ الوظيفي للإقلاع الإنتاجي) عبر أداة محاكاة انقطاع داخل الـ SW أزيلت بعد التحقق.

### BLOCKERS واجهتها وحُلَّت
| المشكلة | الحل |
|---|---|
| «فر» البيضاء غير مرئية على خلفية maskable البيضاء | فحص VLM+بكسلي اكتشفها → «فر» بلون المحيط `#005B82` في النسخ القابلة للقناع |
| سباق `response.clone()` في كاش التنقلات يفرغ الكاش دائماً (Body already used) | استنساخ فوري قبل إرجاع الاستجابة |
| `/api/regions` لا يُخزَّن في الزيارة الأولى (الطلب يسبق تفعيل العامل) | precache له في كاش البيانات عند التثبيت |
| إصدارات 3 حزم radix غير موجودة في مستودع البيئة | استخراج الإصدارات الحقيقية من package-lock المرفق |
| محاكاة Playwright للأوفلاين لا تحجب طلبات الـ SW (قيود CDP معروفة) | قتل الخادم فعلياً + أداة محاكاة داخل SW (أزيلت لاحقاً) |
| Lighthouse 12 أزال فئة PWA نهائياً | استخدام v9.6.8 (آخر إصدار بفئة PWA) |

---

## 7) فحوص المهمة 3 المحلية

| الفحص | النتيجة |
|---|---|
| manifest التطبيقين كامل الحقول | ✅ (Lighthouse 100 + فحص JSON مباشر) |
| Console خالٍ من أخطاء SW | ✅ |
| صفر تخزين توكنات وطلبات owner/admin | ✅ (فحص وقت تشغيل §6/بند 10) |
| إعدادات المالك بلا ميزات وهمية | ✅ أعيدت كتابتها: زر تثبيت + معلومات تواصل حقيقية فقط |
| grep ألوان ثابتة | ✅ صفر في TSX (الاستثناءات: قيم hex في metadata للـ manifest/theme-color — مواصفة W3C تتطلب نصاً — وSVG الشعار وchart.tsx المسبق من shadcn) |
| grep text-white | ✅ فقط فوق خلفيات ملونة ثابتة (bg-destructive/bg-success/تدرجات) |
| صفحات الدخول الثلاث بالوضعين | ✅ بلا نصوص شبحية (فحص VLM) |
| صفر أخطاء Hydration | ✅ (لا أخطاء صفحة عبر كل الجلسات) |

---

## 8) كيف يعمل النظام الآن (للمراجع التقني)

1. **تثبيت العميل من `wafir.gleeze.com`:** المتصفح يقرأ `/manifest.webmanifest` (بيانات العميل) → يثبت standalone يفتح على `/` بأيقونة العميل وشاشة انطلاق iOS الخاصة به.
2. **تثبيت المالك من `facility.wafir.gleeze.com`:** الـ middleware يعيد كتابة كل المسارات إلى `/owner/*` وlayout المالك يفرض `?app=owner` → يثبت تطبيق مختلف الأيقونة والاسم يفتح على بوابة المالك مباشرة ولا يرى واجهة العميل إطلاقاً.
3. **على localhost:** الرئيسية = العميل، و`/owner/login` = manifest المالك — لاختبار التطبيقين من نفس الأصل.
4. **التحديثات:** غيّر الكود → push → Vercel ينشر → يبقى SW القديم عاملاً حتى يكتشف النسخة الجديدة (عند زيارة تالية/ساعة/عودة اتصال) → بانر «يتوفر تحديث لتطبيق وفر» → «تحديث الآن».
5. **البيانات الجديدة** (منشآت/منتجات) تصل المستخدمين **فوراً بلا أي نشر** عبر إعادة التحقق الخلفية لـ SWR.

---

## 9) الملفات المرجعية

- الدليلان الموجهان للمشرف: `دليل_النشر_PWA.md` (نشر Vercel والدومينات) و`دليل_التغليف_المتاجر.md` (PWABuilder والمتاجر وassetlinks)
- سجل كل مراحل العمل: `سجل_التغييرات.md` و`worklog.md`
- سكربت إعادة توليد الأيقونات: `bun run icons`
