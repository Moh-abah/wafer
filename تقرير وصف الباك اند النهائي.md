وصف النظام النهائي — منصة «وفر» (Wafir Backend)
وصف كامل ومحدّث للمشروع بناءً على ما تم تنفيذه فعلياً بعد إضافة وحدة المالكين والمنتجات. يعكس هذا الملف الصورة النهائية للنظام الجاهز للإنتاج.

1. الهوية والرؤية
مشروع «وفر» (Wafir) هو منصة خلفية (Backend) متكاملة ومتعددة الأدوار، صُممت لتكون العمود الفقري لبطاقة خصم افتراضية موحّدة (خصم 30%) تغطي قطاع الضيافة (مطاعم ومقاهي) والمرافق العامة في المملكة العربية السعودية.

تم تطويره بـ FastAPI + SQLAlchemy 2.0 (async) + Pydantic v2، وبمعمارية متعددة الطبقات النظيفة (Layered / Clean Architecture) تسمح بـ التدهور الأنيق (Graceful Degradation)، مما يضمن استمرارية الخدمة حتى في بيئات التشغيل المحدودة الإمكانيات (التحول التلقائي إلى SQLite / الكاش الداخلي / Celery المتزامن عند غياب البنية التحتية الخارجية).

2. الأدوار والصلاحيات (Roles)
أُضيفت أداة UserRole كـ Enum (admin / owner / customer) كآلية الصلاحيات الرئيسية، مع الإبقاء على is_admin كعلم (flag) احتياطي للتوافق مع البيانات القديمة.

الدور
الوصف
الوصول
العميل (Customer)	المستخدم العادي الذي يسجل عبر التطبيق، تُسند له البطاقة الافتراضية تلقائياً (30%)، ويستطيع تصفّح المرافق والمنتجات المتاحة في منطقته.	/api/v1/regions, /cards, /facilities, /facilities/{id}/products, /auth/register
مالك المنشأة (Owner)	صاحب المطعم أو المقهى أو المرفق. يدخل إلى لوحة تحكم خاصة لإدارة بيانات منشأته (الاسم، العنوان، الإحداثيات، الهاتف، ساعات العمل، الصورة، الظهور) وإدارة قائمة المنتجات (الوجبات والمشروبات) بشكل كامل (CRUD + تبديل التوفر)، مع استيراد المنتجات بسهولة عبر رفع ملف Excel.	/api/v1/owner/*
المشرف (Admin)	يدير النظام بأكمله: المناطق، البطاقات، المرافق (وتعيين المالكين لها)، المستخدمين (ترقية العميل إلى مالك)، ومراجعة سجلات التدقيق الشاملة.	/api/v1/admin/*

التحقق من الصلاحيات يتم عبر ثلاث تبعيات (dependencies) في app/api/v1/deps.py:

get_current_admin — يقبل role == admin أو is_admin == true.
get_current_owner — يقتضي role == owner.
get_current_owner_or_admin — يقبل المالك أو المشرف (المشرف يصل لأي مرفق؛ المالك يُقيَّد بمنشآته فقط داخل طبقة الخدمة عبر فحص facility.owner_id == owner.id).
3. الوحدات الوظيفية الرئيسية (Modules)
3.1 وحدة العميل العامة (Public)
عرض المناطق النشطة، البطاقات المنشورة لمنطقة، المرافق الظاهرة لمنطقة.
عرض منتجات مرفق معين (GET /facilities/{facility_id}/products) مع دعم التصفية حسب الفئة والبحث، وقائمة الفئات المتاحة.
تسجيل عميل جديد مع إسناد البطاقة الافتراضية تلقائياً وإطلاق إشعارات بريد/SMS غير متزامنة.
3.2 وحدة الإدارة (Admin)
لوحة تحكم إحصائية تتضمن الآن: المناطق، البطاقات، البطاقات المنشورة، المرافق، العملاء، المالكين، المنتجات، المنتجات المتاحة.
إدارة CRUD كاملة للمناطق والبطاقات والمرافق (مع تعيين owner_id عند إنشاء المرفق).
إدارة المستخدمين (بحث + تفاصيل + ترقية دور المستخدم عبر PATCH /admin/users/{id}/role).
سجل تدقيق تفصيلي قابل للتصفّح (GET /admin/audit-logs).
3.3 وحدة المالك (Owner) — الجديدة
إدارة المنشأة: قائمة منشآتي / تفاصيل منشأة / تحديث بياناتها.
إدارة المنتجات (CRUD كامل): قائمة بترقيم صفحات وبحث وتصفية، تفاصيل، إنشاء، تحديث، حذف، وتبديل سريع للتوفر (availability).
استيراد Excel: رفع ملف .xlsx/.xls (حتى 2 ميجابايت) يُحلَّل عبر openpyxl، مع التحقق الصارم من الأعمدة الإلزامية (name, price, category)، تحويل القيم النصية (نعم/لا، 1/0) إلى boolean، تحويل الأسعار إلى Decimal لضمان الدقة، إدراج جزئي مع تقرير أخطاء لكل صف فاشل، وإبطال الكاش وكتابة سجل تدقيق PRODUCT_IMPORT.
4. نموذج البيانات (Data Model)
text

┌──────────────┐     ┌──────────────┐     ┌────────────────────┐
│   regions    │1───N│    cards     │N───M│    facilities      │
│ id           │     │ id           │     │ id                 │
│ name (uniq)  │     │ name         │     │ name               │
│ slug  (uniq) │     │ platform_name│     │ type (enum)        │
│ is_active    │     │ discount_rate│     │ region_id (FK)     │
└──────────────┘     │ region_id FK │     │ description        │
                     │ is_published │     │ is_visible         │
                     │ display_order│     │ display_order      │
                     └──────┬───────┘     │ owner_id (FK→users)│
                            │             │ address            │
                            │             │ latitude/longitude│
                            │             │ phone              │
                            │             │ working_hours      │
                            │             │ image_url          │
                            │             └─────────┬──────────┘
                            │                       │
                            │                  1    │ N
                            │                       ▼
                            │             ┌────────────────────┐
                            │             │     products       │  ← جديد
                            │             │ id                 │
                            │             │ facility_id (FK)   │
                            │             │ name (uniq per fac) │
                            │             │ description         │
                            │             │ price Numeric(10,2)│
                            │             │ category           │
                            │             │ image_url          │
                            │             │ is_available       │
                            │             │ display_order      │
                            │             └────────────────────┘
                            │
┌──────────────┐     ┌──────────────┐     ┌────────────────────┐
│    users     │1───N│ user_card_   │     │    audit_logs      │
│ id           │     │ assignments  │     │ id                 │
│ full_name    │     │ user_id FK   │     │ user_id (FK null)  │
│ email (uniq) │     │ card_id FK   │     │ action_type        │
│ phone (uniq) │     │ assignment_  │     │ details JSONB      │
│ password_hash│     │   date       │     │ ip_address         │
│ role (enum)  │← جديد│              │     │ created_at         │
│ is_admin     │     └──────────────┘     └────────────────────┘
│ facilities ←─┼─── relationship
└──────┬───────┘
       │1
       │N
       ▼
   facilities.owner_id
الجداول: regions, cards, facilities, facility_card_link (M2M), users, user_card_assignments, audit_logs, products (جديد).

الأعمدة المضافة:

users.role (Enum: admin/owner/customer, default customer).
facilities.owner_id (FK→users, ondelete SET NULL) + address, latitude, longitude, phone, working_hours, image_url.
جدول products كامل مع UniqueConstraint(facility_id, name).
5. واجهات برمجة التطبيقات (API) — قائمة كاملة (25 مساراً)
جميع المسارات تحت البادئة /api/v1.

5.1 المسارات العامة (عامة بدون مصادقة)
الطريقة
المسار
الوظيفة
GET	/regions	المناطق النشطة
GET	/cards?region_id=	البطاقات المنشورة لمنطقة (مُخزَّنة)
GET	/facilities?region_id=	المرافق الظاهرة لمنطقة (مُخزَّنة)
GET	/facilities/{facility_id}/products	جديد — منتجات مرفق (عام، مع فلترة category/search)
GET	/facilities/{facility_id}/products/categories	جديد — فئات المنتجات (مُخزَّنة)
POST	/auth/register	تسجيل عميل + إسناد البطاقة + إشعارات

5.2 مسارات المشرف (محمية بـ JWT admin)
الطريقة
المسار
الوظيفة
POST	/admin/login	دخول المشرف
GET	/admin/dashboard	إحصائيات اللوحة (تشمل المالكين والمنتجات)
GET/POST/PUT/DELETE	/admin/regions[/{id}]	إدارة المناطق
GET/POST/PUT/DELETE	/admin/cards[/{id}]	إدارة البطاقات
GET/POST/PUT/DELETE	/admin/facilities[/{id}]	إدارة المرافق (مع تعيين owner_id)
GET	/admin/users	قائمة المستخدمين (بحث)
GET	/admin/users/{id}	تفاصيل مستخدم + بطاقته
PATCH	/admin/users/{id}/role	جديد — ترقية/تخفيض دور مستخدم
GET	/admin/audit-logs	سجل التدقيق

5.3 مسارات المالك (محمية بـ JWT owner أو admin) — جديدة
الطريقة
المسار
الوظيفة
GET	/owner/facility	قائمة منشآتي
GET	/owner/facility/{facility_id}	تفاصيل منشأتي
PUT	/owner/facility/{facility_id}	تحديث بيانات المنشأة
GET	/owner/{facility_id}/products	قائمة منتجاتي (ترقيم + فلترة + بحث)
POST	/owner/{facility_id}/products	إنشاء منتج
GET	/owner/{facility_id}/products/{product_id}	تفاصيل منتج
PUT	/owner/{facility_id}/products/{product_id}	تحديث منتج
DELETE	/owner/{facility_id}/products/{product_id}	حذف منتج
PATCH	/owner/{facility_id}/products/{product_id}/availability	تبديل التوفر
POST	/owner/{facility_id}/products/import	استيراد Excel

6. المميزات التقنية الجاهزة للإنتاج
6.1 التدهور الأنيق (Graceful Degradation)
المكوّن
الوضع الكامل
الوضع الاحتياطي
قاعدة البيانات	PostgreSQL 16 (asyncpg + connection pool)	SQLite (aiosqlite) محلي
التخزين المؤقت	Redis 7 (redis.asyncio)	كاش داخلي في الذاكرة (_MemoryBackend مع TTL)
المهام غير المتزامنة	Celery 5 + عامل منفصل عبر Redis broker	وضع task_always_eager (تنفيذ متزامن داخل الطلب)

6.2 الأمان متعدد الطبقات
المصادقة: Bearer JWT (HS256) بصلاحية 15 دقيقة، يضمّن sub, role, is_admin, email.
كلمات المرور: bcrypt مع salt عشوائي.
الصلاحيات: ثلاث طبقات (admin / owner / customer) مع عزل ملكية المرفق في طبقة الخدمة.
تحديد المعدل: 120 طلب/دقيقة/IP (sliding window) → 429 عند التجاوز.
التدقيق الشامل: كل فعل إداري/مالكي يُسجَّل في audit_logs مع action_type, details (JSONB), ip_address, وcreated_at. أنواع الأحداث الجديدة: OWNER_LOGIN, OWNER_FACILITY_UPDATED, PRODUCT_CREATED, PRODUCT_UPDATED, PRODUCT_DELETED, PRODUCT_AVAILABILITY_TOGGLED, PRODUCT_IMPORT, USER_ROLE_UPDATED.
6.3 التخزين المؤقت الذكي
cards:region:{id} وfacilities:region:{id} — قوائم البطاقات/المرافق لكل منطقة.
products:facility:{id} (مع متغيرات للفئة والتوفّر) — قوائم منتجات المرفق.
products:facility:{id}:categories — فئات المنتجات.
البحث الديناميكي يتجاوز الكاش مباشرة (لأن نتائجه متغيّرة).
كل عملية تعديل تستدعي invalidate_products(facility_id) أو invalidate_region(region_id) لحذف البادئة المعنية.
6.4 استيراد Excel — تفاصيل التنفيذ
المكتبة: openpyxl (read_only, data_only).
أعمدة إلزامية: name, price, category. اختيارية: description, image_url, is_available, display_order.
تحويل قيم is_available: يقبل true/false/1/0/yes/no/نعم/لا (أي قيمة غير صريحة بـ false تُعتبر true).
تحويل price إلى Decimal لضمان الدقة (لا floating-point drift).
استراتيجية الإدراج الجزئي: الصفوف الصحيحة تُدرج، والصفوف الفاشلة تُجمّع في errors مع رقم الصف والحقل والرسالة.
فحص الامتداد (.xlsx/.xls) وحجم الملف (افتراضي 2 ميجابايت) قبل المعالجة.
منع تكرار الأسماء داخل نفس الملف وأيضاً مع المنتجات الموجودة في قاعدة البيانات.
6.5 التوافق مع البوابة (Gateway)
الخادم مُهيّأ للعمل خلف Caddy/NGINX عبر بارامتر XTransformPort=8001.
واجهة Swagger UI مخصّصة (RTL، عربية) تضيف البارامتر تلقائياً لكل طلب عبر requestInterceptor.
ترويسة X-Request-ID تُولَّد لكل طلب وتُعاد في الاستجابة للتتبّع.
6.6 الترقيات والتوسع
إضافة دور owner دون المساس بالكود القديم (is_admin بقي كعلم احتياطي).
نقطة PATCH /admin/users/{id}/role تتيح للمشرف ترقية العميل إلى مالك.
المشرف يستطيع تعيين owner_id عند إنشاء/تحديث مرفق.
المخطط مفتوح للتوسع المستقبلي: مكونات المنتج، طلبات الشراء، دعم عملات متعددة، استيراد غير متزامن عبر Celery.
7. خطة النشر والتشغيل
7.1 متطلبات مسبقة
Python ≥ 3.12
PostgreSQL 16 و Redis 7 (أو الاكتفاء بالبدائل المدمجة للتطوير).
7.2 خطوات النشر عبر Docker Compose
bash

# 1. نسخ ملف البيئة وتعديل المتغيرات الحساسة
cp .env .env.local
# عدّل: SECRET_KEY, ADMIN_PASSWORD, بيانات SMTP/SMS, DATABASE_URL, REDIS_URL

# 2. تشغيل جميع الخدمات (Postgres + Redis + Backend + Celery Worker)
docker-compose up --build -d

# 3. تشغيل الترحيلات (للإنتاج على PostgreSQL)
docker-compose exec wafir-backend alembic upgrade head

# 4. (اختياري) زرع البيانات الأولية
# سكربت seed_data.ps1 يزرع 13 منطقة سعودية + بطاقة + مطعم + مقهى لكل منطقة
7.3 التشغيل المحلي (دون Docker)
bash

pip install -e .
# الإعداد الافتراضي يستخدم SQLite + كاش ذاكرة + Celery eager — لا حاجة لأي بنية خارجية
uvicorn app.main:app --host 0.0.0.0 --port 8001
7.4 المراقبة
GET /health → {status, app, cache_memory, db}.
GET /docs → Swagger UI (عربية RTL).
GET /openapi.json → مخطط OpenAPI الكامل.
8. البنية المعمارية (Layered Architecture)
text

┌────────────────────────────────────────────────────────────┐
│                  العميل / Caddy Gateway                     │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│    Middlewares (RateLimit → Logging → CORS)                 │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│    API Layer (Routers)   app/api/v1/endpoints/            │
│      ├── customer/   (عام)                                  │
│      ├── admin/      (JWT admin)                            │
│      └── owner/     (JWT owner/admin)  ← جديد              │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────────┐
│    Service Layer (Business Logic)   app/services/         │
│      registration_service · filter_service · admin_service  │
│      product_service · facility_owner_service  ← جديد       │
└──────────────┬───────────────────────────┬────────────────┘
               ▼                            ▼
┌──────────────────────────┐  ┌───────────────────────────┐
│  CRUD / Repository Layer  │  │  Workers (Celery Tasks)   │
│  app/crud/ (base + 6)     │  │  email_tasks · sms_tasks  │
└──────────────┬────────────┘  └─────────────┬─────────────┘
               ▼                              ▼
┌────────────────────────────────────────────────────────────┐
│    Core Infrastructure   app/core/                        │
│    config · database · security · redis_client · celery   │
└───────────────────────────┬────────────────────────────────┘
                            ▼
┌──────────────┬───────────────┬────────────────────────────┐
│ PostgreSQL / │ Redis         │ SMTP / SMS Gateway         │
│   SQLite     │ (Cache + MQ)  │                            │
└──────────────┴───────────────┴────────────────────────────┘
9. نقاط القوة المستمرة
التدهور الأنيق: يعمل بكفاءة مع PostgreSQL/Redis أو يتحول تلقائياً إلى SQLite/الكاش الداخلي/Celery المتزامن بحسب توفر البنية التحتية.
أمان متعدد الطبقات: JWT (15 دقيقة) + bcrypt + تحديد معدل + عزل ملكية + تدقيق JSONB كامل.
التوافق مع البوابة: مُهيّأ لخلف Caddy مع دعم XTransformPort وX-Request-ID.
التوسع والمرونة: إضافة دور owner دون كسر الكود القديم؛ المخطط مفتوح لمكونات المنتج وطلبات الشراء مستقبلاً.
جاهزية Docker: docker-compose.yml يضم 4 خدمات (Postgres، Redis، Backend، Celery Worker) لتشغيل فوري في الإنتاج.
استيراد Excel متين: معالجة جزئية مع تقرير أخطاء دقيق، فحص أنواع وامتدادات، دعم قيم عربية (نعم/لا).
10. الخلاصة
أصبح نظام «وفر» بعد إضافة وحدة المالكين والمنتجات منصة خلفية كاملة الأطراف تجمع ثلاثة أدوار متكاملة في منتج واحد:

العميل يتصفّح ويُسجّل ويحصل على بطاقة الخصم.
المالك يدير منشأته ومنتجاته ويستورد بياناته بسهولة عبر Excel.
المشرف يدير النظام بالكامل ويراقب كل العمليات عبر سجل تدقيق شامل.
النظام متين، قابل للتوسع، جاهز للإنتاج، ويحافظ على جميع معايير الأمان والكاش والتدقيق والتدهور الأنيق المطبَّقة في النظام الأساسي، مع إضافة طبقة جديدة كاملة لوظائف المالك دون أي كسر للتوافق.

