# تقرير التعديلات والإصلاحات — المرحلة الأولى قبل النشر على Google Play
## مشروع «وفر» — إصلاح المعوقات B1 + B2 + B5 + B6 + B7 + B8

> **تاريخ التقرير:** 2026-09-04
> **النطاق:** إصلاح المعوقات الستة المحدّدة قبل رفع النسخة 1.0.0 على Google Play.
> **الحالة العامة بعد الإصلاح:** ✅ كل المعوقات محلولة ومُختبَرة (26 اختبار تكاملي ناجح، 0 فشل).

---

## 0. ملخص النتائج

| المعوق | الوصف | الحالة قبل | الحالة بعد | الاختبار |
|:---:|---|:---:|:---:|:---:|
| **B1** | سلسلة ترحيلات Alembic مكسورة (رأسان متوازيان) | 🔴 | ✅ | `alembic heads` → رأس واحد + `alembic upgrade head` ناجح |
| **B2** | CORS مفتوح `["*"]` في `.env` الإنتاجي | 🔴 | ✅ | فحص ملف `.env` → نطاقات محددة فقط |
| **B5** | `GET /admin/dashboard` بدون `response_model` مُوثَّق | 🟡 | ✅ | `DashboardStatsOut` + 8 حقول موثقة في OpenAPI |
| **B6** | أزرار الحفظ في إعدادات المشرف stub (toast فقط) | 🟡 | ✅ | مربوطة بـ `PUT /admin/me` و `POST /admin/me/password` |
| **B7** | بيانات الملف الشخصي للمشرف hardcoded | 🟡 | ✅ | مربوطة بـ `GET /admin/me` حقيقي من قاعدة البيانات |
| **B8** | الإشعارات في لوحة الإدارة mock data ثابت | 🟡 | ✅ | تستمد من `GET /admin/audit-logs` (سجل النشاط الحقيقي) |

### ملخص الاختبارات
- **الواجهة الخلفية (Backend):** 26 اختبار تكاملي ناجح / 0 فشل (عبر FastAPI TestClient).
- **الواجهة الأمامية (Frontend):**
  - `bun run typecheck` (tsc --noEmit): ✅ ناجح بدون أخطاء أنواع.
  - `bun run lint` (ESLint): ✅ 0 أخطاء (4 تحذيرات سابقة فقط في ملفات لم تُمَس).

---

## 1. إصلاح B1 — توحيد سلسلة ترحيلات Alembic

### المشكلة
ملفّا الترحيل `0002_owner_products.py` و `001_add_membership_fields.py` كانا оба لهما `down_revision = None`، ما ينتج رأسين متوازيين (two heads) فيخفق `alembic upgrade head` على الإنتاج.

### الإصلاح
**الملف المُعدَّل:** `backend/migrations/versions/001_add_membership_fields.py`

```diff
 revision = "001_add_membership_fields"
-down_revision = None
+down_revision = "0002_owner_products"
 branch_labels = None
 depends_on = None
```

### السلسلة بعد الإصلاح
```
<base> → 0002_owner_products → 001_add_membership_fields → 002_add_facility_approval_fields (head)
```

### الاختبار
```bash
$ cd backend && python3 -m alembic heads
002_add_facility_approval_fields (head)   # ✅ رأس واحد فقط

$ DATABASE_URL="sqlite+aiosqlite:////tmp/test.db" python3 -m alembic upgrade head
INFO  [alembic.runtime.migration] Running upgrade  -> 0002_owner_products
INFO  [alembic.runtime.migration] Running upgrade 0002_owner_products -> 001_add_membership_fields
INFO  [alembic.runtime.migration] Running upgrade 001_add_membership_fields -> 002_add_facility_approval_fields
# ✅ نجح التطبيق الكامل على قاعدة بيانات نظيفة
```

---

## 2. إصلاح B2 — تضييق نطاق CORS

### المشكلة
ملف `backend/.env` كان يحتوي السطر `CORS_ORIGINS=["*"]` (مفتوح لأي مصدر) بينما السطر الصحيح بالنطاقات المحددة كان مُعلّقًا (commented out).

### الإصلاح
**الملف المُعدَّل:** `backend/.env` (أُعيدت كتابته بالكامل مع تنظيم الأسطر وتصحيح المسافة الزائدة في `ADMIN_EMAIL`).

```diff
-#CORS_ORIGINS=["https://wafir.gleeze.com", "https://facility.wafir.gleeze.com", "https://admin.wafir.gleeze.com", "https://wafir.gleeze.com", "https://facility-wafir.vercel.app", "https://admin-wafir.vercel.app", "http://localhost:3000"]
-CORS_ORIGINS=["*"]
+CORS_ORIGINS=["https://wafir.gleeze.com", "https://facility.wafir.gleeze.com", "https://admin.wafir.gleeze.com", "https://facility-wafir.vercel.app", "https://admin-wafir.vercel.app", "http://localhost:3000"]
```

### النطاقات المسموح بها الآن
1. `https://wafir.gleeze.com` — تطبيق العميل (إنتاج)
2. `https://facility.wafir.gleeze.com` — تطبيق المالك (إنتاج)
3. `https://admin.wafir.gleeze.com` — لوحة الإدارة (إنتاج)
4. `https://facility-wafir.vercel.app` — معاينة Vercel للمالك
5. `https://admin-wafir.vercel.app` — معاينة Vercel للإدارة
6. `http://localhost:3000` — التطوير المحلي

### تحسين إضافي
- أُصلحت المسافة الزائدة في `ADMIN_EMAIL="admin@wafir.gleeze.com "` (كانت تسبب مشاكل في المطابقة).

### الاختبار
```
PASS: B2 no wildcard CORS
PASS: B2 all 3 prod domains present
```

---

## 3. إصلاح B5 — توثيق استجابة لوحة التحكم

### المشكلة
نقطة النهاية `GET /admin/dashboard` كانت تُرجع dict خاماً بدون `response_model`، فلم تكن موثقة بشكل صريح في OpenAPI (رغم أن البيانات كانت صحيحة). بعض الوثائق الداخلية ادّعت أن الحقول "ناقصة" — تأكدنا بالفحص المباشر أن خدمة `dashboard_stats()` تُرجع كل الحقول الثمانية، لكن أضفنا العقد (contract) الرسمي.

### الإصلاح

**ملف جديد:** `backend/app/schemas/response/dashboard.py`
```python
class DashboardStatsOut(BaseModel):
    regions: int
    cards: int
    published_cards: int
    facilities: int
    customers: int
    owners: int
    products: int
    available_products: int
    model_config = {"from_attributes": True}
```

**الملف المُعدَّل:** `backend/app/schemas/response/__init__.py` — إضافة `DashboardStatsOut` للتصدير.

**الملف المُعدَّل:** `backend/app/api/v1/endpoints/admin/dashboard.py`
```diff
-from app.schemas.response.auth import TokenOut
+from app.schemas.response.auth import TokenOut
+from app.schemas.response.dashboard import DashboardStatsOut
 ...
-@router.get("/dashboard")
+@router.get("/dashboard", response_model=DashboardStatsOut)
 async def admin_dashboard(...):
```

### الاختبار
```
PASS: dashboard 200
PASS: dashboard has regions
PASS: dashboard has cards
PASS: dashboard has published_cards
PASS: dashboard has facilities
PASS: dashboard has customers
PASS: dashboard has owners
PASS: dashboard has products
PASS: dashboard has available_products
PASS: OpenAPI has /admin/dashboard
```

التأكد من وجود الـ schema في مواصفات OpenAPI:
```python
>>> spec["components"]["schemas"]["DashboardStatsOut"]
# ✅ موجود مع كل الحقول الثمانية موثقة
```

---

## 4. إصلاحات B6 + B7 — إعدادات المشرف الوهمية + ربط الملف الشخصي بـ API

### المشكلة
صفحة `frontend/src/app/(admin)/admin/settings/page.tsx` كانت تحتوي على:
- `email` مثبّت على `admin@wafir.gleeze.com`
- `displayName` فارغ غير مربوط بأي بيانات حقيقية
- 4 معالجات حفظ `handleSaveProfile`/`handleChangePassword`/`handleSaveAll`/`handleSaveNotifications` تُظهر `toast` فقط دون أي استدعاء API
- زر "حفظ جميع التغييرات" عائم لا يفعل شيئاً
- زر "تغيير الصورة" يُظهر toast "قريبًا"

### الإصلاح — الباك‑إند (Backend)

**ملف جديد:** `backend/app/api/v1/endpoints/admin/profile.py` — ثلاث نقاط نهاية جديدة:
| الطريقة | المسار | الوظيفة |
|---|---|---|
| `GET` | `/admin/me` | إرجاع بيانات المشرف الحالي (id, full_name, email, phone, role, is_admin, created_at) |
| `PUT` | `/admin/me` | تحديث `full_name` و/أو `phone` (البريد غير قابل للتعديل) |
| `POST` | `/admin/me/password` | تغيير كلمة المرور بعد التحقق من كلمة المرور الحالية + تسجيلها في `AuditLog` |

**الملف المُعدَّل:** `backend/app/schemas/response/user.py` — إضافة 3 schemas:
```python
class AdminMeOut(BaseModel): ...      # استجابة GET /admin/me
class AdminUpdateMe(BaseModel): ...   # جسم طلب PUT /admin/me
class AdminChangePassword(BaseModel): ...  # جسم طلب POST /admin/me/password
```

**الملف المُعدَّل:** `backend/app/services/admin_service.py` — إضافة 3 دوال خدمة:
- `get_admin_me(db, admin)` — إرجاع بيانات المشرف كـ dict
- `update_admin_me(db, admin, full_name, phone)` — تحديث + commit + refresh
- `change_admin_password(db, admin, current_password, new_password, ip_address)` — التحقق من كلمة المرور الحالية بـ `bcrypt` + تطبيق طول أدنى 8 أحرف + تسجيل `ADMIN_PASSWORD_CHANGED` في سجل التدقيق

**الملف المُعدَّل:** `backend/app/api/v1/endpoints/admin/__init__.py` — إضافة `profile_router` للتصدير.

**الملف المُعدَّل:** `backend/app/api/v1/__init__.py` — تسجيل الراوتر:
```python
api_router.include_router(profile_router, prefix="/admin", tags=["admin-profile"])
```

### الإصلاح — الواجهة (Frontend)

**ملف مُعدَّل:** `frontend/src/types/api.generated.ts` — إضافة الأنواع:
```typescript
export interface AdminMe { id; full_name; email; phone; role; is_admin; created_at; }
export interface AdminUpdateMePayload { full_name?; phone?; }
export interface AdminChangePasswordPayload { current_password; new_password; }
```

**الملف المُعدَّل:** `frontend/src/services/admin.service.ts` — إضافة 3 دوال:
```typescript
getMe: () => apiClient.get<AdminMe>("/admin/me"),
updateMe: (payload) => apiClient.put<AdminMe>("/admin/me", payload),
changePassword: (payload) => apiClient.post<MessageOut>("/admin/me/password", payload),
```

**ملف جديد:** `frontend/src/hooks/useAdminMe.ts` — 3 React Query hooks:
- `useAdminMe()` — جلب الملف الشخصي (staleTime 30s)
- `useUpdateAdminMe()` — mutation + تحديث الكاش + toast
- `useChangeAdminPassword()` — mutation + toast + إعادة تعيين الحقول

**ملف مُعاد كتابته بالكامل:** `frontend/src/app/(admin)/admin/settings/page.tsx`:
- ❌ حُذف كل الـ stubs: `handleSaveAll`، `handleSaveNotifications`، زر "حفظ جميع التغييرات"، زر تغيير الصورة "قريبًا".
- ❌ حُذف `email`/`displayName` المثبّتة.
- ✅ الصورة الرمزية تعرض الآن الحرف الأول من `adminMe.full_name` الحقيقي.
- ✅ زر "حفظ الملف الشخصي" مربوط بـ `useUpdateAdminMe` (معطل حتى يتغير الحقل).
- ✅ زر "تغيير كلمة المرور" مربوط بـ `useChangeAdminPassword` مع تحقق من كلمة المرور الحالية + طول ≥ 8 + تطابق التأكيد.
- ✅ تفضيلات الإشعارات تُحفظ في `localStorage` (لا يوجد مخزن خلفي بعد) — مع توضيح ذلك للمستخدم بنص صريح.
- ✅ أُعيد هيكلتها إلى مكوّنات فرعية (`ProfileAvatar`, `ProfileForm`, `ChangePasswordSection`) باستخدام `key` prop لإعادة تعيين الحالة عند تغيّر بيانات الـ API — حلّ أخطاء lint `react-hooks/set-state-in-effect`.

### الاختبار (Backend — FastAPI TestClient)
```
=== B7: /admin/me ===
PASS: /admin/me returns full_name            → {"id":1,"full_name":"Site Administrator",...}
PASS: /admin/me returns email                → admin@wafir.gleeze.com
PASS: /admin/me returns is_admin             → true
PASS: /admin/me returns phone                → +966500000000

=== B7: PUT /admin/me ===
PASS: PUT returns updated name               → "المشرف العام المحدّث"
PASS: PUT returns updated phone              → "+966555555555"
PASS: update persisted                       → التغيير ظاهر في GET لاحقاً

=== B7: password wrong current ===
PASS: wrong current 401                      → {"detail":"كلمة المرور الحالية غير صحيحة"}

=== B7: password too short ===
PASS: short new rejected                     → {"detail":"كلمة المرور الجديدة يجب أن تكون 8 أحرف على الأقل"}

=== B7: password valid ===
PASS: password changed                       → {"detail":"تم تغيير كلمة المرور بنجاح"}
PASS: login with new password                → access_token (تأكيد أن bcrypt حفظها فعلاً)
```

### الاختبار (Frontend)
```
$ bun run typecheck  → ✅ 0 errors
$ bun run lint       → ✅ 0 errors (4 pre-existing warnings only)
```

---

## 5. إصلاح B8 — استبدال الإشعارات الوهمية بسجل النشاط الحقيقي

### المشكلة
`frontend/src/app/(admin)/admin/layout.tsx` كان يحتوي مصفوفة ثابتة `NOTIFICATIONS: NotificationItem[]` بأربعة عناصر وهمية ("تم إضافة منشأة جديدة / تسجيل عميل جديد / تم تعليق بطاقة / تم تحديث بيانات المنطقة") مع زر "عرض جميع الإشعارات" يُظهر toast "قريبًا". العدّاد كان مثبّتاً على `3`.

### الإصلاح
**الملف المُعدَّل:** `frontend/src/app/(admin)/admin/layout.tsx` (إعادة كتابة كاملة لمنطق الإشعارات):

1. ❌ حُذفت مصفوفة `NOTIFICATIONS` الثابتة.
2. ✅ أُضيف استدعاء `useAdminAuditLogs(1, 5)` لجلب آخر 5 سجلات تدقيق.
3. ✅ أُضيف جدول `ACTION_META` يربط كل `action_type` بـ:
   - أيقونة مناسبة (Store / UserPlus / AlertTriangle / MapPin / ShieldCheck / CreditCard / Package / FileEdit / Bell)
   - لون خلفية ولون أيقونة
   - دالة `label()` تُولّد نصاً عربياً وصفياً (قد تستخدم `details` من السجل، مثل اسم المنتج أو بريد المالك)
4. ✅ أُضيفت دالة `timeAgoAr(iso)` تُحوّل الطابع الزمني إلى نص عربي ("الآن" / "منذ 5 دقائق" / "منذ ساعة" / "منذ 3 أيام" / تاريخ كامل).
5. ✅ العدّاد أصبح ديناميكياً (`notifCount = notifications.length`) — يظهر فقط إذا > 0.
6. ✅ زر "عرض جميع الإشعارات" أصبح `router.push("/admin/audit-logs")` بدل toast "قريبًا".
7. ✅ عُنوان القائمة تغيّر من "الإشعارات" إلى "النشاط الأخير" (أدق).
8. ✅ حالة فارغة: "لا يوجد نشاط حديث" عند عدم وجود سجلات.

### أنواع الأحداث المدعومة في `ACTION_META` (18 نوعاً)
`OWNER_LOGIN`, `ADMIN_LOGIN`, `ADMIN_PASSWORD_CHANGED`, `ADMIN_PROFILE_UPDATED`, `OWNER_REGISTERED`, `OWNER_FACILITY_UPDATED`, `PRODUCT_CREATED`, `PRODUCT_UPDATED`, `PRODUCT_DELETED`, `PRODUCT_AVAILABILITY_TOGGLED`, `PRODUCT_IMPORT`, `FACILITY_CREATED`, `FACILITY_UPDATED`, `FACILITY_DELETED`, `CARD_CREATED`, `CARD_UPDATED`, `CARD_DELETED`, `REGION_UPDATED`, `DASHBOARD_VIEW` — وأي نوع غير معروف يقع على fallback عام.

### الاختبار
```
=== B8: audit logs real activity ===
PASS: audit has ADMIN_PASSWORD_CHANGED    → ✅ (من اختبار B7)
PASS: audit has ADMIN_PROFILE_UPDATED      → ✅ (من اختبار B7)
PASS: audit has ADMIN_LOGIN                → ✅ (تسجيل دخول المشرف)
```

هذا يؤكد أن الإشعارات في الواجهة ستحتوي الآن على:
- "تسجيل دخول مشرف" — عند كل دخول
- "تحديث ملف المشرف" — عند حفظ الملف الشخصي
- "تغيير كلمة مرور المشرف" — عند تغيير كلمة المرور
- وكل أحداث المنتجات والمنشآت والبطاقات والمناطق الفعلية

---

## 6. قائمة الملفات المُعدَّلة والمُضافة

### ملفات مُضافة (جديدة)
| # | المسار | الوصف |
|:---:|---|---|
| 1 | `backend/app/schemas/response/dashboard.py` | `DashboardStatsOut` — عقد استجابة لوحة التحكم |
| 2 | `backend/app/api/v1/endpoints/admin/profile.py` | نقاط نهاية `GET/PUT /admin/me` و `POST /admin/me/password` |
| 3 | `frontend/src/hooks/useAdminMe.ts` | React Query hooks لملف المشرف الشخصي |

### ملفات مُعدَّلة
| # | المسار | التغيير |
|:---:|---|---|
| 4 | `backend/migrations/versions/001_add_membership_fields.py` | `down_revision = "0002_owner_products"` (إصلاح B1) |
| 5 | `backend/.env` | تفعيل CORS المحدد + حذف `["*"]` + تنظيم الأسطر + إزالة المسافة الزائدة في `ADMIN_EMAIL` (إصلاح B2) |
| 6 | `backend/app/schemas/response/__init__.py` | تصدير `DashboardStatsOut` |
| 7 | `backend/app/api/v1/endpoints/admin/dashboard.py` | إضافة `response_model=DashboardStatsOut` (إصلاح B5) |
| 8 | `backend/app/schemas/response/user.py` | إضافة `AdminMeOut`, `AdminUpdateMe`, `AdminChangePassword` |
| 9 | `backend/app/services/admin_service.py` | إضافة `get_admin_me`, `update_admin_me`, `change_admin_password` + استيراد `hash_password` |
| 10 | `backend/app/api/v1/endpoints/admin/__init__.py` | تصدير `profile_router` |
| 11 | `backend/app/api/v1/__init__.py` | تسجيل `profile_router` تحت `/admin` |
| 12 | `frontend/src/types/api.generated.ts` | إضافة `AdminMe`, `AdminUpdateMePayload`, `AdminChangePasswordPayload` |
| 13 | `frontend/src/services/admin.service.ts` | إضافة `getMe`, `updateMe`, `changePassword` |
| 14 | `frontend/src/app/(admin)/admin/settings/page.tsx` | إعادة كتابة كاملة — ربط بـ API حقيقي + حذف stubs + تقسيم لمكوّنات (إصلاح B6 + B7) |
| 15 | `frontend/src/app/(admin)/admin/layout.tsx` | استبدال `NOTIFICATIONS` الثابتة بـ `useAdminAuditLogs` (إصلاح B8) |

**الإجمالي:** 3 ملفات جديدة + 12 ملف مُعدَّل = **15 ملفاً**.

---

## 7. سجل الاختبارات التفصيلي

### 7.1 اختبارات الباك‑إند (FastAPI TestClient)
أُجريت عبر سكربت Python يستخدم `TestClient` على قاعدة بيانات SQLite نظيفة + `init_db` + seed المشرف الافتراضي.

```
=== B1: Alembic single head ===
PASS: B1 single alembic head (count=1)

=== B2: CORS not wildcard in .env ===
PASS: B2 no wildcard CORS
PASS: B2 all 3 prod domains present

PASS: admin login

=== B5: dashboard 8 fields ===
PASS: field regions
PASS: field cards
PASS: field published_cards
PASS: field facilities
PASS: field customers
PASS: field owners
PASS: field products
PASS: field available_products

=== B7: /admin/me ===
PASS: /admin/me returns full_name
PASS: /admin/me returns email
PASS: /admin/me returns is_admin
PASS: /admin/me returns phone

=== B7: PUT /admin/me ===
PASS: PUT returns updated name
PASS: PUT returns updated phone
PASS: update persisted

=== B7: password wrong current ===
PASS: wrong current 401

=== B7: password too short ===
PASS: short new rejected

=== B7: password valid ===
PASS: password changed
PASS: login with new password

=== B8: audit logs real activity ===
PASS: audit has ADMIN_PASSWORD_CHANGED
PASS: audit has ADMIN_PROFILE_UPDATED
PASS: audit has ADMIN_LOGIN

=== SUMMARY ===
PASS: 26
FAIL: 0
```

### 7.2 اختبارات الواجهة الأمامية
```
$ bun run typecheck
$ tsc --noEmit
# ✅ 0 أخطاء أنواع

$ bun run lint
# ✅ 0 أخطاء (4 تحذيرات سابقة في ملفات لم تُمَس: register/page.tsx, AdminAuthGuard.tsx, OwnerAuthGuard.tsx)
```

### 7.3 اختبار ترحيل Alembic على قاعدة بيانات نظيفة
```
$ rm -f /tmp/test_alembic.db
$ DATABASE_URL="sqlite+aiosqlite:////tmp/test_alembic.db" python3 -m alembic upgrade head
INFO  [alembic.runtime.migration] Running upgrade  -> 0002_owner_products
INFO  [alembic.runtime.migration] Running upgrade 0002_owner_products -> 001_add_membership_fields
INFO  [alembic.runtime.migration] Running upgrade 001_add_membership_fields -> 002_add_facility_approval_fields
# ✅ نجح التطبيق الكامل للسلسلة الموحدة
```

### 7.4 التحقق من تسجيل نقاط النهاية الجديدة في OpenAPI
```python
>>> routes = [r for r in app.routes if hasattr(r,'methods') and hasattr(r,'path')]
>>> admin_routes = [r for r in routes if '/admin' in r.path]
# 24 نقطة نهاية تحت /admin (كانت 21، أضفنا 3)
# ✅ GET  /api/v1/admin/me
# ✅ PUT  /api/v1/admin/me
# ✅ POST /api/v1/admin/me/password

>>> "DashboardStatsOut" in spec["components"]["schemas"]
True
>>> "AdminMeOut" in spec["components"]["schemas"]
True
>>> "AdminChangePassword" in spec["components"]["schemas"]
True
>>> "AdminUpdateMe" in spec["components"]["schemas"]
True
```

---

## 8. ما يجب عمله بعد هذه الإصلاحات (خطوات النشر)

الآن بعد إصلاح **B1 + B2 + B5 + B6 + B7 + B8**، المشروع جاهز لـ:

### 8.1 خطوات فورية (على جهازك)
1. **انسخ الملفات المعدّلة إلى مشروعك الأصلي** (القسم 6 يوضح كل الملفات).
2. **شغّل ترحيلات Alembic** على قاعدة بياناتك الحالية:
   ```bash
   cd backend
   alembic upgrade head   # الآن ستعمل لأن السلسلة موحدة
   ```
3. **أعد بناء الواجهة**:
   ```bash
   cd frontend
   bun install
   bun run build
   ```
4. **تأكد من نجاح lint + typecheck**:
   ```bash
   bun run lint
   bun run typecheck
   ```

### 8.2 خطوات النشر على Google Play
1. اتبع دليل `دليل_التغليف_المتاجر.md` لبناء AAB عبر PWABuilder.
2. ارفع النسخة `1.0.0` على Google Play Console (Internal Testing → Closed Testing → Production).
3. انتظر مراجعة Google (ساعات إلى 7 أيام).

### 8.3 بعد قبول التطبيق على Google Play
- قدّم لشركة الدفع بالمصادقات المطلوبة + رابط التطبيق المنشور.
- بعد الحصول على بيانات البوابة → ادمجها في النسخة `1.1.0` (حسب خطة التقرير الشامل `تقرير_الجاهزية_للنشر_في_قوقل_بلاي.md` القسم 5).

---

## 9. ملاحظات ومحاذيات

1. **لا تنسَ تدوير الأسرار** (B3 من التقرير الشامل): ملف `.env` ما زال يحتوي على `SECRET_KEY` و `ADMIN_PASSWORD` و DB password حقيقية — هذه خطوة منفصلة يجب فعلها قبل الرفع الفعلي للإنتاج، لكنها خارج نطاق هذه الجولة من الإصلاحات.

2. **تفضيلات الإشعارات محلية فقط حالياً**: حفظناها في `localStorage` لأن الباك‑إند لا يملك نموذج إعدادات إشعارات بعد. عند الحاجة لربطها بحساب المشرف عبر الأجهزة، أنشئ جدول `admin_notification_prefs` وأضف `GET/PUT /admin/me/notif-prefs`.

3. **التغيير في `ADMIN_EMAIL` (إزالة المسافة الزائدة)**: إذا كان المشرف الحالي في قاعدة بياناتك الإنتاجية مسجّلاً بـ `admin@wafir.gleeze.com ` (بمسافة)، حدّثه يدوياً:
   ```sql
   UPDATE users SET email = 'admin@wafir.gleeze.com' WHERE email LIKE '%admin@wafir.gleeze.com %';
   ```

4. **اختبار يدوي مقترح بعد النشر**:
   - سجّل دخول المشرف ← اذهب لـ `/admin/settings` ← غيّر الاسم ← اضغط حفظ ← تحقق أن toast "تم حفظ الملف الشخصي" يظهر وأن الاسم تحديث في الصورة الرمزية.
   - غيّر كلمة المرور بكلمة خاطئة أولاً ← تحقق من رسالة "كلمة المرور الحالية غير صحيحة".
   - اضغط جرس الإشعارات ← تحقق أن العناصر تحتوي على "تحديث ملف المشرف" و "تغيير كلمة مرور المشرف" (من نشاطك للتو).

---

**نهاية التقرير. المشروع جاهز الآن للنشر على Google Play بعد نقل هذه الملفات الـ 15 إلى مشروعك الأصلي.**
