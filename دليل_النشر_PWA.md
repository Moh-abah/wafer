# دليل نشر تطبيقي «وفر» (PWA) — خطوة بخطوة للمشرف

> هذا الدليل مكتوب لمشرف غير تقني. لا تحتاج إلى كتابة أي كود — كل ما عليك هو الضغط على الأزرار الصحيحة بالترتيب.
> الوقت المتوقع للإنجاز الكامل: **من 30 إلى 60 دقيقة** (عدا وقت انتشار DNS).

---  

## نظرة سريعة: ماذا ستنشر؟

المشروع كله **موقع واحد** يُنشر مرة واحدة، ويقدّم من خلاله **ثلاثة خدمات**:

| الخدمة | الرابط | من يستخدمها | هل هي تطبيق؟ |
|---|---|---|---|
| وفر — بطاقة الخصومات | `https://wafir.gleeze.com` | العملاء (المستخدمون النهائيون) | ✅ نعم — تطبيق PWA |
| وفر — بوابة المنشآت | `https://facility.wafir.gleeze.com` | أصحاب المنشآت | ✅ نعم — تطبيق PWA |
| لوحة تحكم الأدمن | `https://admin.wafir.gleeze.com` | إدارة المنصة | ❌ لا — موقع إداري فقط |

**النقطة الأهم:** التطبيقان (العميل والمالك) يخرجان من **نشر واحد** — النظام يفرّق بينهما تلقائياً حسب الرابط الذي فُتح منه. لذلك لن تنشر إلا مرة واحدة.

---

## ١. نشر الموقع على Vercel

### المتطلب الوحيد قبل البدء

يجب أن يكون كود المشروع موجوداً في مستودع **GitHub** (اطلب من المطور رفعه إن لم يكن موجوداً). Vercel يسحب الكود من هناك.

### الخطوة ١: إنشاء حساب Vercel

1. افتح في المتصفح:

   ```
   https://vercel.com
   ```

2. اضغط **Sign Up**.
3. اختر **Continue with GitHub** (الأسهل — لأن الكود عندك على GitHub أصلاً) وسجّل الدخول بنفس حساب GitHub.

### الخطوة ٢: استيراد المشروع (Import Git Repository)

1. بعد تسجيل الدخول، اضغط زر **Add New…** ثم **Project**.
2. ستظهر قائمة بمستودعاتك في GitHub — اختر مستودع مشروع وفر.
   - إن لم يظهر، اضغط **Adjust GitHub App Permissions** وامنح Vercel صلاحية الوصول للمستودع.
3. سيأخذك Vercel لصفحة الإعدادات قبل النشر.

### الخطوة ٣: إعدادات المشروع — الافتراضي يكفي تماماً

راجع الإعدادات التالية ولا تُغيّر شيئاً:

| الإعداد | القيمة الصحيحة | ملاحظة |
|---|---|---|
| Framework Preset | **Next.js** | Vercel يكتشفها تلقائياً |
| Build Command | **اتركه فارغاً (الافتراضي)** | لا حاجة لأي أمر بناء مخصص |
| Output Directory | **الافتراضي** | تلقائي |
| Environment Variables | **لا تضف شيئاً** | اقرأ الملاحظة أدناه |

> **لماذا لا نحتاج متغيرات بيئة؟**
> الباك إند حيّ على الرابط `https://api.wafir.gleeze.com` والربط به **مدمج داخل ملف الكود `next.config.ts`** (كل طلب `/api/*` يمرّ عبر نفس الموقع إلى الباك إند)، وأسماء النطاقات الثلاثة لها قيم افتراضية مكتوبة في الكود نفسه. ملف `.env` الموجود في المشروع يحتوي متغيرات `NEXT_PUBLIC_SITE_URL` و`NEXT_PUBLIC_ADMIN_URL` و`NEXT_PUBLIC_OWNER_URL` و`NEXT_PUBLIC_API_URL` لكنها **للتجاوز عند الحاجة فقط** — لا تضيفها في Vercel إلا إذا طلب منك المطور ذلك صراحة.

### الخطوة ٤: اضغط Deploy

1. اضغط زر **Deploy** وانتظر (عادة 2–5 دقائق).
2. عند الانتهاء سيعطيك Vercel رابطاً مؤقتاً بهذا الشكل:

   ```
   https://wafir-xxxxx.vercel.app
   ```

3. افتح هذا الرابط وتأكد أن الصفحة الرئيسية تعمل وتظهر المنشآت والعروض (هذه بيانات حية من الباك إند — وجودها يعني أن كل شيء سليم).
4. جرّب أيضاً `/owner/login` على نفس الرابط المؤقت — يجب أن تظهر صفحة دخول بوابة المنشآت.

> **مهم:** كل مرة تُرفع فيها نسخة جديدة من الكود إلى GitHub مستقبلاً، سيعيد Vercel النشر **تلقائياً** — لن تكرر هذه الخطوات أبداً.

---

## ٢. ربط النطاقات الثلاثة في Vercel

النطاقات الثلاثة تشير كلها إلى **نفس النشر**. سنسجّلها واحداً واحداً.

### الخطوة ١: إضافة النطاقات في لوحة Vercel

1. من صفحة مشروعك في Vercel افتح **Settings** → **Domains**.
2. اكتب في الحقل النطاق الأول ثم اضغط **Add**:

   ```
   wafir.gleeze.com
   ```

3. كرر نفس الشيء للنطاقين الآخرين:

   ```
   facility.wafir.gleeze.com
   ```

   ```
   admin.wafir.gleeze.com
   ```

4. بجانب كل نطاق سيُظهر Vercel **تعليمات DNS خاصة به** — ستحتاجها في الخطوة التالية.

> اكتب الأسماء **بدون** `https://` وبدون `www` — تماماً كما في الصناديق أعلاه.

### الخطوة ٢: إضافة سجلات DNS عند مزود الدومين

**أين تضيفها؟** اذهب إلى الشركة التي اشتريت منها دومين `gleze.com` (مزوّد الدومين)، وافتح في لوحة تحكمها قسم **DNS** أو **إدارة السجلات** (DNS Records / DNS Management). إن لم تجده فاسأل دعم الشركة: «أين أضيف سجل DNS؟».

**ماذا تضيف؟** ثلاثة سجلات — واحد لكل نطاق فرعي:

| # | النوع (Type) | الاسم (Name/Host) | القيمة (Value) |
|---|---|---|---|
| 1 | CNAME | `wafir` | `cname.vercel-dns.com` |
| 2 | CNAME | `facility` | `cname.vercel-dns.com` |
| 3 | CNAME | `admin` | `cname.vercel-dns.com` |

> **بديل بسجل A:** إذا طلب منك Vercel (أو لم يقبل مزوّد الدومين) سجل **A** بدلاً من CNAME، فاستخدم:
>
> | النوع | الاسم | القيمة |
> |---|---|---|
> | A | `wafir` (أو `@` للنطاق الرئيسي) | `76.76.21.21` |
>
> **القاعدة الذهبية:** انسخ السجل **بالضبط كما يعرضه لك Vercel** في صفحة Domains — هو يعرف ما يحتاجه كل نطاق.

### الخطوة ٣: انتظر انتشار DNS

- بعد إضافة السجلات، يتحقق Vercel تلقائياً من كل نطاق، وتظهر بجانبه **علامة صح خضراء** عندما يجهز.
- الانتشار قد يستغرق **حتى 24–48 ساعة في أسوأ الحالات**، لكن **غالباً يكتمل خلال دقائق**.
- لا تقلق إن ظل أحدها برتقالياً — انتظر ثم أعد تحديث الصفحة.

### الخطوة ٤: فحص سريع بعد جاهزية النطاقات

افتح الروابط التالية وتأكد من استجابتها:

| الرابط | المتوقع |
|---|---|
| `https://wafir.gleeze.com` | الرئيسية تعمل وتظهر المنشآت |
| `https://facility.wafir.gleeze.com` | صفحة دخول بوابة المنشآت (المالك) |
| `https://admin.wafir.gleeze.com` | صفحة دخول لوحة الأدمن |

---

## ٣. شهادة HTTPS — تلقائية بالكامل

تطبيقات PWA **تتطلب HTTPS** ليعمل التثبيت وService Worker — هذا شرط من المتصفحات وليس خياراً.

**الخبر الجيد:** Vercel يُصدر شهادات SSL تلقائياً لكل نطاق تربطه، بمجرد اكتمال DNS. لا توجد أي خطوة يدوية ولا أي تكلفة. إذا فتحت أي رابط من الروابط أعلاه وظهر **القفل 🔒** بجانبه في شريط المتصفح — فكل شيء تمام.

---

## ٤. اختبار تثبيت التطبيقين بعد النشر

هذا هو اختبار «اللحظة الحاسمة»: هل يظهر تطبيق «وفر» على الشاشة الرئيسية للجوال كتطبيق حقيقي؟
 
### تحقق سريع من ملفات التطبيق قبل التثبيت

افتح هذه الروابط — يجب أن يظهر محتوى (وليس خطأ):

| الفحص | رابط العميل | رابط المالك |
|---|---|---|
| ملف Manifest | `https://wafir.gleeze.com/manifest.webmanifest` | `https://facility.wafir.gleeze.com/manifest.webmanifest` |
| ملف Service Worker | `https://wafir.gleeze.com/sw.js` | `https://facility.wafir.gleeze.com/sw.js` |

> لاحظ: ملف Manifest **يختلف تلقائياً** حسب الرابط — رابط العميل يعطي بيانات وأيقونة العميل، ورابط المالك يعطي بيانات وأيقونة المالك. هذا سلوك مقصود ومُختبَر.

### أ) على أندرويد (متصفح كروم)

**تطبيق العميل:**

1. افتح كروم وادخل إلى:

   ```
   https://wafir.gleeze.com
   ```

2. الطريقة الأولى: اضغط قائمة المتصفح **⋮** ثم **«تثبيت التطبيق»** (أو «إضافة إلى الشاشة الرئيسية»).
3. الطريقة الثانية: استخدم **زر التثبيت داخل التطبيق نفسه** — تجده في صفحة **«حسابي»** وفي بطاقة التثبيت التي تظهر في الصفحة الرئيسية على الجوال.
4. اضغط **تثبيت** → تظهر أيقونة «وفر» على الشاشة الرئيسية → افتحها → يجب أن يعمل التطبيق **بملء الشاشة بدون شريط عنوان المتصفح**.

**تطبيق المالك:**

1. افتح كروم وادخل إلى:

   ```
   https://facility.wafir.gleeze.com
   ```

2. ثبّته بنفس الطريقتين (زر ⋮ في المتصفح، أو زر التثبيت الموجود **فوق نموذج الدخول مباشرة** وفي صفحة إعدادات البوابة).
3. ستظهر أيقونة مختلفة عن أيقونة العميل (حلقة ذهبية) — هذا مقصود لتمييز التطبيقين عند تثبيتهما جنباً إلى جنب.

### ب) على الآيفون (متصفح سفاري)

> ⚠️ على الآيفون التثبيت **يدوي دائماً** — لا يوجد زر تثبيت تلقائي (شرح السبب في قسم قيود iOS أدناه).

**تطبيق العميل:**

1. افتح **سفاري** (وليس كروم — الآيفون لا يسمح بالتثبيت إلا من سفاري) وادخل إلى:

   ```
   https://wafir.gleeze.com
   ```

2. اضغط زر **المشاركة** (المربع مع السهم الصاعد ⬆️).
3. اختر من القائمة **«إضافة إلى الشاشة الرئيسية»** (Add to Home Screen).
4. اضغط **إضافة** — تظهر أيقونة «وفر» على الشاشة الرئيسية.

**تطبيق المالك:**

كرر نفس الخطوات تماماً ولكن من الرابط:

```
https://facility.wafir.gleeze.com
```

> داخل تطبيق العميل على iOS يظهر زر التثبيت كحوار إرشادي يشرح هذه الخطوات («شارك ← إضافة إلى الشاشة الرئيسية») — لأن آبل لا تسمح بأكثر من ذلك.

### جدول ملخص اختبار التثبيت

| التطبيق | أندرويد (كروم) | آيفون (سفاري) | الرابط |
|---|---|---|---|
| وفر — العميل | قائمة ⋮ أو زر داخل التطبيق | مشاركة ← إضافة إلى الشاشة الرئيسية | `wafir.gleeze.com` |
| وفر — المالك | قائمة ⋮ أو زر فوق نموذج الدخول | مشاركة ← إضافة إلى الشاشة الرئيسية | `facility.wafir.gleeze.com` |

---

## ٥. كيف تُحدَّث التطبيقات بعد النشر؟

هناك نوعان من «التحديث» — يجب التفريق بينهما جيداً:

| نوع التحديث | مثال | هل يحتاج إعادة نشر؟ | متى يصل للمستخدم؟ |
|---|---|---|---|
| **تحديث المحتوى** | منشأة جديدة، منتج جديد، تغيير سعر، تفعيل عرض | ❌ لا شيء إطلاقاً — يُدار من لوحة الأدمن ويأتي من الـ API | **فوراً وبشكل تلقائي** |
| **تحديث التطبيق** | شكل جديد، ميزة جديدة، إصلاح خلل في الكود | ✅ نعم (انظر الخطوات أدناه) | خلال ساعة أو عند العودة للتطبيق |

> **لماذا تحديثات المحتوى فورية؟** لأن الكاش يعمل بنمط StaleWhileRevalidate: يُعرض آخر ما لدى المستخدم فوراً، ويُسحب الجديد في الخلفية لكل زيارة تالية. لا تحتاج لمس أي شيء.

### خطوات إطلاق نسخة جديدة من التطبيق

1. **عدّل الكود** (أنت أو المطور) — الميزة أو الإصلاح الجديد.
2. **ارفع رقم الإصدار** في ملف واحد فقط داخل المشروع:

   ```
   src/lib/pwa/version.ts
   ```

   غيّر الرقم من:

   ```ts
   export const APP_VERSION = "1.0.0";
   ```

   إلى:

   ```ts
   export const APP_VERSION = "1.1.0";
   ```

3. **ارفع التعديل إلى GitHub** (Push) → **Vercel ينشر تلقائياً** خلال دقائق.
4. **ماذا يحدث عند المستخدمين؟** بعد النشر، يظهر لهم شريط صغير أعلى الشاشة:

   > **«يتوفر تحديث لتطبيق وفر»** مع زر **«تحديث الآن»**

   الضغط على الزر يفعّل التحديث فوراً (يعتمد العامل الجديد ويعيد تحميل الشاشة مرة واحدة). وإن لم يضغطه المستخدم، يظل التطبيق يعمل وسيُعرض له الشريط عند عودته للتطبيق. النظام يفحص وجود تحديث **كل ساعة** تلقائياً وعند عودة الاتصال بالإنترنت.

> **خلاصة للمشرف:** البيانات تتحديث وحدها دائماً. أما «نسخة جديدة» فتُطلق بثلاث خطوات: تعديل الكود ← رفع رقم الإصدار في `src/lib/pwa/version.ts` ← رفع إلى GitHub. والباقي يتم وحده.

---

## ٦. قيود الآيفون (iOS) — بصدق كامل

عند التسويق للتطبيق عليك معرفة حدود آبل الحقيقية (كلها تنطبق على أي PWA وليس على وفر فقط):

| القيد | الشرح العملي |
|---|---|
| **لا يوجد زر تثبيت تلقائي** | آبل لا تدعم خاصية beforeinstallprompt — التثبيت يدوي من **سفاري فقط** عبر «مشاركة ← إضافة إلى الشاشة الرئيسية». زر التثبيت داخل التطبيق على iOS يعرض حوار إرشاد بالمخطط الصحيح بدلاً من التثبيت المباشر. |
| **مساحة الكاش محدودة** | نظام iOS يحدّ من حجم ما يخزّنه التطبيق (نحو ~50MB تقريباً)، وقد يحذف الكاش **تحت ضغط التخزين** (إذا امتلأ الجهال). النتيجة: عند فتح التطبيق بلا اتصال بعد حذف الكاش، يطلب الاتصال من جديد. لا خطر على البيانات نفسها — كل شيء محفوظ في الباك إند. |
| **شاشات الانطلاق ثابتة** | شاشة البداية على iOS صورة ثابتة (جهّزنا 6 مقاسات منها في المشروع) — لا حركة ولا أنيميشن فيها. |
| **الإشعارات تتطلب شروطاً** | إشعارات Push على الويب تتطلب **iOS 16.4 فأحدث** + أن يكون التطبيق **مثبتاً** على الشاشة الرئيسية. **المشروع حالياً لا يستخدم إشعارات إطلاقاً** — فلا تقلق من هذه النقطة الآن، فقط اذكرها لو خططت مستقبلاً لها. |

> كل هذه القيود لا تؤثر على أندرويد — تجربة أندرويد كاملة: زر تثبيت مباشر وكاش مستقر.

---

## ٧. استكشاف أخطاء سريع

| المشكلة | الحل خطوة بخطوة |
|---|---|
| **خطأ في ملف الـ Manifest** (التطبيق لا يُثبَّت والمتصفح يقول مشكلة في manifest) | افتح مباشرة في المتصفح: `https://wafir.gleeze.com/manifest.webmanifest` (وللمالك: `https://facility.wafir.gleeze.com/manifest.webmanifest`). يجب أن ترى نصاً منظماً يبدأ بـ `{"id":...` — إن ظهر خطأ أو صفحة فارغة فأعد النشر وتأكد أن النشر نجح في Vercel. |
| **زر التثبيت لا يظهر على أندرويد** | (1) تأكد أنك على الرابط الإنتاجي الصحيح وليس رابط معاينة. (2) شغّل فحص Lighthouse (من قائمة أدوات المطور في كروم ← Lighthouse ← فئة PWA) على الرابط المباشر — يجب أن تكون النتيجة نظيفة (المشروع محقّق 100/100 مسبقاً). (3) امسح بيانات الموقع: إعدادات كروم ← إعدادات المواقع ← امسح البيانات، ثم أعد فتح الرابط وحاول مجدداً. |
| **النطاق لا يفتح بعد الربط** | انتظر انتشار DNS (حتى 24–48 ساعة، غالباً دقائق). تأكد أن سجلات DNS مطابقة تماماً لما يعرضه Vercel في صفحة Domains. |
| **رسالة «يتوفر تحديث» لا تظهر بعد نشر نسخة** | تأكد أولاً أن النشر نجح (Vercel ← Deployments ← حالة Ready). ثم تأكد أن رقم الإصدار في `src/lib/pwa/version.ts` رُفع فعلاً وأُدرج في نفس النشر. افتح `https://wafir.gleeze.com/sw.js` وتأكد أن رقم الإصدار الجديد ظاهر في بداية الملف. |
| **التطبيق يفتح بشريط عنوان** (بدل ملء الشاشة) | هذا طبيعي عند الفتح من المتصفح مباشرة. ملء الشاشة يظهر **بعد التثبيت** على الشاشة الرئيسية. ولحزم المتاجر يوجد دليل منفصل: `دليل_التغليف_المتاجر.md`. |
| **بيانات لا تظهر نهائياً** | افتح `https://wafir.gleeze.com/api/regions` — إن ظهرت بيانات فالربط بالباك إند سليم والمشكلة مؤقتة في الاتصال؛ وإن ظهر خطأ فراجع حالة الباك إند `https://api.wafir.gleeze.com`. |

---

## خلاصة الدليل في سطر واحد

**انشر مرة واحدة على Vercel ← اربط ثلاثة نطاقات بسجلات DNS ← افتح روابط التطبيقين على الجوال وثبّتهما ← التحديثات بعد ذلك: بيانات فورية بلا لمس، ونسخ جديدة برفع رقم واحد في ملف واحد.**

---

*للتغليف كتطبيقات متاجر (Google Play / Huawei / App Store) انتقل إلى الدليل الثاني: `دليل_التغليف_المتاجر.md`*
